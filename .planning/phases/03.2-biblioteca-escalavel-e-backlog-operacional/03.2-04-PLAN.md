---
phase: "03.2-biblioteca-escalavel-e-backlog-operacional"
plan: "04"
type: execute
wave: 4
depends_on: ["03.2-01", "03.2-02", "03.2-03"]
files_modified:
  - "apps/web/tests/catalog-library-ui.test.tsx"
  - "apps/web/tests/library-domain.test.ts"
  - "apps/web/tests/library-application.test.ts"
  - "apps/web/tests/phase-03-2-e2e.spec.ts"
  - "apps/web/tests/accessibility.spec.ts"
  - "packages/db/tests/library-rls.test.ts"
  - ".planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-BROWSER-REVIEW.md"
autonomous: true
requirements: [LIB-02, LIB-03, LIB-04, DISC-12, BRND-08, BRND-09, BRND-10, DATA-11, SEC-02]
user_setup:
  - service: biblioteca-e2e-fixtures
    why: "Authenticated browser validation needs a ready duo with enough library games to exercise pagination, filters, Jogando and another duo for isolation checks."
    env_vars:
      - name: E2E_BASE_URL
        source: "Local or preview deployment URL"
      - name: E2E_READY_USER_EMAIL
        source: "Verified ready-duo member with populated library"
      - name: E2E_READY_USER_PASSWORD
        source: "Ready-duo member password"
      - name: E2E_READY_PARTNER_EMAIL
        source: "Verified ready-duo partner"
      - name: E2E_READY_PARTNER_PASSWORD
        source: "Ready-duo partner password"
      - name: E2E_OTHER_DUO_USER_EMAIL
        source: "Verified user in another duo for isolation checks"
      - name: E2E_OTHER_DUO_USER_PASSWORD
        source: "Other-duo user password"
      - name: E2E_PHASE3_2_LIBRARY_QUERY
        source: "Game-name query known to match a ready-duo library item"
must_haves:
  truths:
    - "D-01 through D-04: Browser/tests prove no dead Archive or terminal-status controls appear before real archive behavior exists."
    - "D-05 through D-09: Tests prove Biblioteca is not a three-column board and cards do not expose all actions at once."
    - "D-10 through D-14: Tests prove URL-backed pagination/filter/sort state survives refresh/back/share and page size stays bounded."
    - "D-15 through D-18: Browser/tests prove mobile cards, filter bar and secondary actions are compact, reachable and non-overlapping."
    - "Impeccable audit/polish: visual evidence checks hierarchy, text fit, focus, touch targets, reduced motion and anti-patterns."
    - "Security: DB integration evidence covers RLS isolation for paginated/filterable library reads or records exact `TEST_DATABASE_URL` blocker."
  artifacts:
    - path: "apps/web/tests/phase-03-2-e2e.spec.ts"
      provides: "Authenticated Biblioteca desktop/mobile/reduced-motion browser flow"
      min_lines: 160
    - path: "apps/web/tests/catalog-library-ui.test.tsx"
      provides: "Fast UI/source guards for scalable Biblioteca"
      min_lines: 360
    - path: ".planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-BROWSER-REVIEW.md"
      provides: "Browser evidence or named fixture blockers"
      contains: "Result:"
  key_links:
    - from: "apps/web/tests/phase-03-2-e2e.spec.ts"
      to: "apps/web/src/app/app/biblioteca/page.tsx"
      via: "authenticated browser route checks"
      pattern: "library-operational-shell"
    - from: "apps/web/tests/catalog-library-ui.test.tsx"
      to: "apps/web/src/modules/library/presentation/library-status-controls.tsx"
      via: "source and rendered no-dead-controls guards"
      pattern: "library-action-sheet"
---

<objective>
Verify Biblioteca scalability, mobile quality and no-dead-control regressions.

Purpose: Phase 03.2 is a UX and scalability gate before Phase 4. The work is not complete until tests and browser evidence prove the Biblioteca behaves like an operational backlog on desktop/mobile/reduced-motion, with bounded reads and no future-feature deception.
Output: focused unit/UI/E2E/accessibility/integration coverage and a browser review evidence file.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-CONTEXT.md
@.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-RESEARCH.md
@.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-01-SUMMARY.md
@.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-02-SUMMARY.md
@.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-03-SUMMARY.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fast regression guards for bounded data and scoped UI</name>
  <files>apps/web/tests/library-domain.test.ts, apps/web/tests/library-application.test.ts, apps/web/tests/catalog-library-ui.test.tsx</files>
  <read_first>apps/web/tests/library-domain.test.ts, apps/web/tests/library-application.test.ts, apps/web/tests/catalog-library-ui.test.tsx, apps/web/src/modules/library/infrastructure/library-repository.ts, apps/web/src/app/app/biblioteca/page.tsx</read_first>
  <action>Add tests/source guards that fail if Biblioteca regresses to the old board or unbounded read path. Assert domain/application behavior for active/archive views, page-size clamping, sort normalization, `Todas` excluding archive, and repository contract normalization. Add source assertions that `/app/biblioteca/page.tsx` uses `getLibraryQueue`, does not use `getLibraryOverview`, does not contain `.library-board`, and does not contain Phase 4-only words: `Principal`, `secundario`, `sessao`, `timer`, `capitulo`, `porcentagem`, `milestone`. Add rendered UI assertions for `Proximos da fila`, `Jogando`, URL pagination, filter controls, compact card fields, one visible primary action, `library-action-sheet`, and absence of `Zerado bloqueado`/`Dropado bloqueado`.</action>
  <verify>
    <automated>pnpm --filter @queue/web test -- library-domain library-application catalog-library-ui</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `catalog-library-ui.test.tsx` asserts `Proximos da fila` appears before main results.
    - Tests assert terminal blocked button copy is absent.
    - Tests assert the page source uses `getLibraryQueue` and not `getLibraryOverview`.
    - Tests assert no old `.library-board` route structure is required.
    - Tests assert filter UI does not include Discovery-only filters.
  </acceptance_criteria>
  <done>Fast tests catch the highest-risk Biblioteca regressions before browser work.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated Biblioteca E2E and accessibility checks</name>
  <files>apps/web/tests/phase-03-2-e2e.spec.ts, apps/web/tests/accessibility.spec.ts</files>
  <read_first>apps/web/tests/phase-2-e2e.spec.ts, apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md</read_first>
  <action>Create `phase-03-2-e2e.spec.ts` using the existing authenticated fixture pattern. When env vars exist, log in as the ready user and verify `/app/biblioteca` on desktop and mobile: `Proximos da fila` visible, `Jogando` visible and capped, filters update the URL, pagination preserves query/sort/view, compact cards link to detail, primary status action exists, secondary action sheet/menu is reachable, no terminal blocked controls render, and browser console has no hydration errors. Add reduced-motion coverage through `page.emulateMedia({ reducedMotion: "reduce" })` and ensure the route still exposes focus-visible controls and no overlap between card title, cover and action cluster using bounding boxes. Extend `accessibility.spec.ts` with axe/focus checks for Biblioteca when fixtures are available. Keep skip output explicit with every missing env var; never summarize fixture skips as pass.</action>
  <verify>
    <automated>pnpm --filter @queue/web test:e2e -- tests/phase-03-2-e2e.spec.ts tests/accessibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `phase-03-2-e2e.spec.ts` contains `library-operational-shell`, `setViewportSize`, `reducedMotion: \"reduce\"`, `Hydration`, and `boundingBox`.
    - Missing env output lists `E2E_BASE_URL`, ready user, ready partner, other-duo user and `E2E_PHASE3_2_LIBRARY_QUERY`.
    - Accessibility spec checks `/app/biblioteca`, keyboard focus and axe when fixtures exist.
    - If fixtures are missing, the plan summary and browser review mark browser evidence as blocked, not passed.
  </acceptance_criteria>
  <done>Browser and accessibility checks can prove the Biblioteca UX when fixtures exist and diagnose missing setup honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Verify RLS/isolation and record browser evidence</name>
  <files>packages/db/tests/library-rls.test.ts, .planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-BROWSER-REVIEW.md</files>
  <read_first>packages/db/tests/library-rls.test.ts, packages/db/tests/library-concurrency.test.ts, .planning/SECURITY.md, apps/web/tests/phase-03-2-e2e.spec.ts</read_first>
  <action>Extend library RLS/integration coverage so paginated/filterable reads do not leak another duo's library rows. Add at least one integration test that creates two duos with overlapping catalog games and verifies a runtime user sees only their own duo results when querying through the same SQL shape or repository path introduced in Plan 01. Create `03.2-BROWSER-REVIEW.md` with sections `## Environment`, `## Desktop Review`, `## Mobile Review`, `## Reduced Motion Review`, `## URL State`, `## Data Isolation`, `## Focus And Controls`, `## Text Overlap`, `## Result`, and `## Remaining Gates`. Run feasible commands and record exact pass/fail/skip lines. If browser or DB fixtures are missing, set `Result: BLOCKED - missing fixture evidence` and list exact variable names. If all fixtures exist and commands pass, set `Result: PASSED` and include viewport sizes used.</action>
  <verify>
    <automated>pnpm check:architecture</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm --filter @queue/web test -- library-domain library-application catalog-library-ui</automated>
    <automated>pnpm --filter @queue/web test:e2e -- tests/phase-03-2-e2e.spec.ts tests/accessibility.spec.ts</automated>
    <automated>pnpm --filter @queue/db test:integration -- library</automated>
  </verify>
  <acceptance_criteria>
    - `03.2-BROWSER-REVIEW.md` contains all ten required headings.
    - `03.2-BROWSER-REVIEW.md` contains either `Result: PASSED` or `Result: BLOCKED - missing fixture evidence`.
    - If the review contains `skipped`, it also contains `Remaining Gates` and exact missing env var names.
    - RLS/integration tests pass when `TEST_DATABASE_URL` exists or record missing `TEST_DATABASE_URL` as a blocker.
    - `pnpm check:architecture` and focused Vitest commands pass.
  </acceptance_criteria>
  <done>Phase 03.2 has honest verification evidence for UI quality, bounded reads and duo isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser fixture credentials -> private duo data | E2E users access real authenticated private routes and must remain test-only. |
| Skipped browser/DB checks -> release evidence | Missing fixtures are blockers, not passing evidence. |
| Bounded library SQL -> RLS isolation | Pagination/filter/search must not bypass duo-scoped RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-13 | Information Disclosure | Paginated library reads | mitigate | Add cross-duo RLS/integration coverage for the new query shape. |
| T-03.2-14 | Repudiation | Browser review evidence | mitigate | Record exact command results, viewport sizes, missing fixture names and final Result status. |
| T-03.2-15 | Denial of Service | Browser UI on mobile | mitigate | E2E bounding-box and focus checks catch overlapping controls and unreachable actions. |
| T-03.2-16 | Spoofing | Fixture skip handling | mitigate | Missing env vars are printed and review remains BLOCKED instead of PASSED. |
</threat_model>

<verification>
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- library-domain library-application catalog-library-ui` passes.
- [ ] `pnpm --filter @queue/web test:e2e -- tests/phase-03-2-e2e.spec.ts tests/accessibility.spec.ts` passes or records missing fixture blockers by exact env var name.
- [ ] `pnpm --filter @queue/db test:integration -- library` passes or records missing `TEST_DATABASE_URL`.
- [ ] `03.2-BROWSER-REVIEW.md` clearly says PASSED or BLOCKED.
</verification>

<success_criteria>
- Fast tests protect the bounded data contract and operational route hierarchy.
- Authenticated browser checks cover desktop, mobile and reduced-motion Biblioteca.
- Accessibility checks cover focus, touch targets, axe and text overlap risks.
- RLS/integration evidence covers the new query shape or records exact blocker names.
- Phase 4 starts only after Biblioteca evidence is honest and actionable.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-04-SUMMARY.md`.
</output>
