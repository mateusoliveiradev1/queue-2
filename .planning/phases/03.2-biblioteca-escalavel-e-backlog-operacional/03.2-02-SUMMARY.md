---
phase: 03.2-biblioteca-escalavel-e-backlog-operacional
plan: "02"
subsystem: library-ui
tags: [nextjs, biblioteca, pagination, url-state, vitest, css]

requires:
  - phase: 03.2-01
    provides: "Bounded getLibraryQueue contract, queue records and queue view models."
provides:
  - "Biblioteca URL parser and path builder for view, search, platform, sort, page and page size."
  - "Operational Biblioteca route using getLibraryQueue with Proximos da fila, capped Jogando and paginated active queue."
  - "Route-level CSS for operational shell, priority strip, playing strip, results and pagination."
affects: [phase-03.2-03, biblioteca, library-ui, mobile-ui]

tech-stack:
  added: []
  patterns:
    - "Biblioteca URL filters use `visao`, `q`, `plataforma`, `ordenar`, `pagina` and `tamanho`."
    - "Changing Biblioteca filters resets `pagina`; pagination preserves the current filter state."
    - "Route source guards reject the old full overview path, `.library-board` and dead terminal controls."

key-files:
  created:
    - apps/web/src/app/app/biblioteca/library-route-params.ts
    - .planning/phases/03.2-biblioteca-escalavel-e-backlog-operacional/03.2-02-SUMMARY.md
  modified:
    - apps/web/src/app/app/biblioteca/page.tsx
    - apps/web/src/app/globals.css
    - apps/web/tests/catalog-library-ui.test.tsx

key-decisions:
  - "Biblioteca route state uses Portuguese URL keys and omits default values from generated paths."
  - "Proximos da fila is the first content section in Biblioteca, ahead of filters and page results."
  - "Archive count is explanatory only; no dead archive tab, button or locked terminal surface is rendered."

patterns-established:
  - "Authenticated routes should delegate Biblioteca queue semantics to `getLibraryQueue` and `toLibraryQueueView`."
  - "Fast UI tests read route/CSS source to block old board and future-feature regressions."
  - "Route path builders preserve URL state and reset page only when filters change."

requirements-completed: [LIB-03, LIB-04, DISC-12, BRND-08, BRND-09, BRND-10]

duration: 8 min
completed: 2026-06-04
---

# Phase 03.2 Plan 02: Operational Biblioteca Route Summary

**Biblioteca now renders as an operational backlog surface with URL-backed filters, Proximos da fila, capped Jogando and paginated active results.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-06-04T21:07:00Z
- **Completed:** 2026-06-04T21:15:00Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added `parseLibraryRouteParams` and `buildLibraryPath` for stable Biblioteca URL state.
- Rebuilt `/app/biblioteca` to call `getLibraryQueue` instead of `getLibraryOverview`.
- Removed the old status-column board and locked future status surface from the route.
- Added route/CSS guards for the operational shell, Proximos ordering, pagination state and no terminal blocked controls.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Biblioteca route parameter parsing and path builders** - `27c9512` (feat)
2. **Task 2: Rebuild the route around Proximos, Jogando and paginated active queue** - `f89e61d` (feat)
3. **Task 3: Add pagination, empty states and route-level CSS hierarchy** - `25bf1fa` (feat)

## Files Created/Modified

- `apps/web/src/app/app/biblioteca/library-route-params.ts` - Pure URL-state parser and path builder.
- `apps/web/src/app/app/biblioteca/page.tsx` - Operational Biblioteca route composition.
- `apps/web/src/app/globals.css` - Operational shell, priority strip, playing strip, result and pagination styles.
- `apps/web/tests/catalog-library-ui.test.tsx` - Route-param, source and rendered UI coverage.

## Decisions Made

- `plataforma=comum` represents the common-platform filter; specific platform keys still work as explicit filters.
- Page size defaults to 12 and can be represented by `tamanho=24` for denser views.
- The route keeps only explanatory archive copy when archive rows exist; it does not expose an archive destination yet.

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed.
**Impact on plan:** No scope change.

## Issues Encountered

None.

## Verification

- `pnpm --filter @queue/web test -- catalog-library-ui` - passed.
- `pnpm --filter @queue/web typecheck` - passed.
- `pnpm check:architecture` - passed.
- Source guards confirm `getLibraryQueue` is used, `getLibraryOverview` is absent, `.library-board` is absent, and Phase 4-only terms stay out of the route.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for Plan 03 to extract compact card/filter/status-control components and make mobile interaction density explicit.

---
*Phase: 03.2-biblioteca-escalavel-e-backlog-operacional*
*Completed: 2026-06-04*
