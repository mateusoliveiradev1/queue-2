# Phase 03.2 Browser Review: Biblioteca Escalavel e Backlog Operacional

## Environment

- Review date: 2026-06-04
- Browser command: `pnpm --filter @queue/web test:e2e -- tests/phase-03-2-e2e.spec.ts tests/accessibility.spec.ts`
- Browser result: 19 skipped because authenticated fixtures are missing.
- Database command: `pnpm --filter @queue/db test:integration -- library`
- Database result: 2 files skipped, 5 tests skipped because `TEST_DATABASE_URL` is missing.
- Fast verification available without fixtures:
  - `pnpm --filter @queue/web test -- library-domain library-application catalog-library-ui` passed.
  - `pnpm --filter @queue/web typecheck` passed.
  - `pnpm --filter @queue/db typecheck` passed.

Missing browser fixture variables:

- `E2E_BASE_URL`
- `E2E_READY_USER_EMAIL`
- `E2E_READY_USER_PASSWORD`
- `E2E_READY_PARTNER_EMAIL`
- `E2E_READY_PARTNER_PASSWORD`
- `E2E_OTHER_DUO_USER_EMAIL`
- `E2E_OTHER_DUO_USER_PASSWORD`
- `E2E_PHASE3_2_LIBRARY_QUERY`

Missing database fixture variable:

- `TEST_DATABASE_URL`

## Desktop Review

Blocked by missing browser fixtures. The new `phase-03-2-e2e.spec.ts` will validate `/app/biblioteca` at `1440x1000` once fixtures exist, including `library-operational-shell`, `Proximos da fila`, `Jogando`, compact card links, one primary status action, secondary `library-action-sheet`, URL filters and no hydration errors.

## Mobile Review

Blocked by missing browser fixtures. The new E2E spec will validate `/app/biblioteca` at `390x844`, including compact cards, `library-filter-bar`, `library-filter-sheet`, mobile bottom navigation, reachable secondary actions and bounding-box overlap checks for cover/title/action clusters.

## Reduced Motion Review

Blocked by missing browser fixtures. The E2E spec calls `page.emulateMedia({ reducedMotion: "reduce" })` and checks the route hierarchy, filter sheet and card bounding boxes under reduced motion.

## URL State

Fast route-param tests passed. They verify Biblioteca preserves `visao`, `q`, `plataforma=comum`, `ordenar`, `tamanho` and `pagina`, and resets page when filters change.

Browser URL-state evidence remains blocked by missing E2E fixtures.

## Data Isolation

Database integration evidence remains blocked by missing `TEST_DATABASE_URL`.

Added integration coverage in `packages/db/tests/library-rls.test.ts` for the paginated/filterable library SQL shape. When `TEST_DATABASE_URL` exists, the test creates two duos with overlapping catalog games and verifies a runtime user sees only their own duo rows, including when a malicious query uses the other duo id.

## Focus And Controls

Fast UI tests passed for visible accessible names on rendered controls, one visible primary status submit per card, secondary `library-action-sheet`, filter controls and absence of `Zerado bloqueado`/`Dropado bloqueado`.

Browser keyboard focus and axe evidence remain blocked by missing E2E fixtures.

## Text Overlap

Source/CSS guards passed for compact card and sheet hooks. Browser bounding-box evidence remains blocked by missing E2E fixtures.

## Result

Result: BLOCKED - missing fixture evidence

## Remaining Gates

- Provide `E2E_BASE_URL`, `E2E_READY_USER_EMAIL`, `E2E_READY_USER_PASSWORD`, `E2E_READY_PARTNER_EMAIL`, `E2E_READY_PARTNER_PASSWORD`, `E2E_OTHER_DUO_USER_EMAIL`, `E2E_OTHER_DUO_USER_PASSWORD` and `E2E_PHASE3_2_LIBRARY_QUERY`, then rerun `pnpm --filter @queue/web test:e2e -- tests/phase-03-2-e2e.spec.ts tests/accessibility.spec.ts`.
- Provide `TEST_DATABASE_URL` for an isolated Neon/test Postgres database, then rerun `pnpm --filter @queue/db test:integration -- library`.
- Replace this blocked review with `Result: PASSED` only after browser and database commands run against real fixtures and pass.
