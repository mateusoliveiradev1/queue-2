---
phase: 01.1-polimento-auth-e-landing-intermediaria
plan: 01.1-01
status: complete
created: 2026-06-03T17:56:59Z
---

# Plan 01.1-01: Public Auth And Interim Landing Polish

## Objective

Improve the interim public home and public auth/duo entry surfaces while preserving Phase 1 auth, security and database behavior.

## Tasks

1. Update planning/state metadata for Phase 01.1.
2. Add a reusable public brand/home link and apply it to login, signup, verify email, recover password and pairing pages.
3. Replace the placeholder home with an intentional interim landing that shows QUEUE/2, the `/2` promise and practical entry actions.
4. Refine CSS for public panels, brand link, ritual preview and mobile layout.
5. Add/update tests for the interim home and brand navigation.
6. Run focused verification and write a summary.

## Verification

- `pnpm --filter @queue/web typecheck`
- `pnpm --filter @queue/web test`
- `pnpm --filter @queue/web build`
- `pnpm --filter @queue/web test:e2e -- tests/accessibility.spec.ts -g "Phase 1 public accessibility"` with `E2E_BASE_URL=http://127.0.0.1:3000`
- Inspected `/`, `/login`, `/cadastro`, `/verificar-email`, `/recuperar-senha`, `/parear` through generated browser screenshots.

## Risks

- Public copy could imply unavailable Phase 2+ functionality. Keep claims restrained.
- Brand link inside form panels must not interfere with form submission.
- Layout must not regress mobile text fit.
