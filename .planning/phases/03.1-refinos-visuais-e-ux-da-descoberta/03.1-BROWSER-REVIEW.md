---
status: blocked
phase: "03.1-refinos-visuais-e-ux-da-descoberta"
source: ["03.1-04-PLAN.md"]
updated: "2026-06-04"
---

# Phase 03.1 Browser Review

## Environment

Local feasible checks ran from `C:\Users\Liiiraa\Documents\estudos\queue`.

- `pnpm check:secrets` passed.
- `pnpm check:architecture` passed.
- `pnpm --filter @queue/web typecheck` passed.
- `pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search` passed: 3 files, 25 tests.
- `pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts` exited 0 with 23 skipped tests.
- `pnpm --filter @queue/db test:integration -- discovery` exited 0 with 2 skipped files and 7 skipped tests.

The in-app browser could open `http://localhost:3001/app/descobrir`, but without authenticated fixture credentials it redirected to `/login`. Real authenticated visual evidence remains blocked by the missing variables below.

## Desktop Review

Blocked for real browser evidence because authenticated ready-duo fixtures are missing.

Required desktop coverage is present in `apps/web/tests/phase-3-e2e.spec.ts`:

- Desktop viewport: `1440x1000`.
- Hydration console guard for `Hydration`, `hydration`, `Text content does not match`, and `Expected server HTML`.
- `.discovery-card-stage` appears before filters/search.
- Bounding-box overlap checks for active card title and decision buttons.
- Visible focus checks for deck decisions, search and filters.

Local code/test review found and fixed two desktop UX regressions before closing this evidence file:

- Exhausted deck no longer says only `Sem cartas prontas para este filtro`; it explains that the current member finished their decisions and waits for the partner vote.
- `Matches recentes` no longer stays trapped in a narrow support tray; it spans the support grid and uses wider match cards.

## Mobile Review

Blocked for real browser evidence because authenticated ready-duo fixtures are missing.

Required mobile coverage is present in `apps/web/tests/phase-3-e2e.spec.ts`:

- Mobile viewport: `390x844`.
- Bottom navigation exposes active `Descobrir` with `aria-current="page"`.
- Discovery uses bottom navigation on mobile instead of a compressed desktop sidebar.
- Decision/search/filter focus checks remain explicit.

The desktop sidebar remains `sticky` as agreed in the Phase 03.1 discussion; mobile keeps the bottom nav.

## Reduced Motion Review

Blocked for real browser evidence because authenticated ready-duo fixtures are missing.

Required reduced-motion coverage is present in `apps/web/tests/phase-3-e2e.spec.ts` and `apps/web/tests/accessibility.spec.ts`:

- `page.emulateMedia({ reducedMotion: "reduce" })`.
- Discovery decision state remains visible.
- Search result context remains visible.
- Match celebration and library handoff controls remain understandable.
- The match celebration animation is disabled under `prefers-reduced-motion: reduce`.

## Hydration Errors

No authenticated browser run was possible in this environment.

The E2E spec now collects browser console errors and fails on hydration patterns when fixtures are present:

- `Hydration`
- `hydration`
- `Text content does not match`
- `Expected server HTML`

## Focus And Controls

Local component coverage passed for Discovery controls.

The browser specs require visible focus for:

- `Quero jogar`
- `Agora nao`
- `Pular`
- Discovery search input
- Discovery platform filters
- RAWG source links
- Mobile active `Descobrir` bottom nav

## Text Overlap

Real browser overlap evidence is blocked by missing fixtures.

The E2E spec contains bounding-box checks for the active Discovery card title and decision buttons. The local layout regression shown during execution was fixed by making the match history tray span the full support grid and by increasing match-card width constraints.

## Result

Result: BLOCKED - missing fixture evidence

Local implementation checks passed, but Phase 03.1 does not have real authenticated desktop, mobile, reduced-motion or database evidence yet.

## Remaining Gates

Missing browser fixture variables:

- `E2E_BASE_URL`
- `E2E_READY_USER_EMAIL`
- `E2E_READY_USER_PASSWORD`
- `E2E_READY_PARTNER_EMAIL`
- `E2E_READY_PARTNER_PASSWORD`
- `E2E_OTHER_DUO_USER_EMAIL`
- `E2E_OTHER_DUO_USER_PASSWORD`
- `E2E_PHASE3_DISCOVERY_QUERY`

Missing accessibility/detail fixture variables reported by the shared accessibility suite:

- `E2E_FLOW_OWNER_EMAIL`
- `E2E_FLOW_OWNER_PASSWORD`
- `E2E_PHASE2_CATALOG_SLUG`

Missing database fixture:

- `TEST_DATABASE_URL`

Run these after fixtures exist:

```bash
pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts
pnpm --filter @queue/db test:integration -- discovery
```
