---
phase: 03.1-refinos-visuais-e-ux-da-descoberta
plan: "02"
subsystem: discovery-ui
tags: [nextjs, react, css, accessibility, discovery, mobile]

requires:
  - phase: 03.1-01
    provides: "Discovery application-safe read models and same-module architecture enforcement"
provides:
  - "Deck-first `/app/descobrir` route composition with compact orbit controls and subordinate support trays"
  - "Authenticated app shell with refined desktop rail and mobile bottom navigation"
  - "Discovery CSS/source guards against dashboard hierarchy and viewport-scaled display typography regressions"
affects: [03.1-03, 03.1-04, phase-4-jogando-agora]

tech-stack:
  added: []
  patterns:
    - "Discovery route composition keeps public module APIs and server actions authoritative while reshaping only UI hierarchy."
    - "Authenticated shell navigation renders desktop and mobile views from the same route model with matching `aria-current` state."
    - "Visual anti-patterns are guarded through component DOM order assertions and CSS source assertions."

key-files:
  created: []
  modified:
    - apps/web/src/app/app/descobrir/page.tsx
    - apps/web/src/components/app-shell.tsx
    - apps/web/src/app/globals.css
    - apps/web/tests/discovery-ui.test.tsx
    - apps/web/tests/accessibility.spec.ts

key-decisions:
  - "Discovery now renders as top strip, orbit controls, card stage and secondary trays so the active card owns the first fold."
  - "Mobile navigation is rendered from the same authenticated navigation model as the desktop rail, preserving route hrefs and active state."
  - "The Phase 03.1 CSS contract is enforced with source assertions for old four-mode grids, `clamp()` Discovery display headings and negative Discovery letter spacing."

patterns-established:
  - "Route-level Discovery support surfaces live after the deck in DOM order and are tested as subordinate to the card stage."
  - "Mobile app-shell work adds bottom navigation without changing protected route authorization."
  - "Discovery CSS uses fixed 14/16/20/28px typography roles for the polished route surface."

requirements-completed: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08, DISC-09, DISC-10, DISC-11, DISC-12, BRND-02, BRND-04, BRND-05, BRND-06, BRND-11, BRND-13, SAFE-05]

duration: 10 min
completed: 2026-06-04
---

# Phase 03.1 Plan 02: Deck-First Discovery Route Summary

**Discovery now opens on a dominant card stage with compact orbit controls and thumb-friendly authenticated navigation.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-06-04T19:49:33Z
- **Completed:** 2026-06-04T19:59:13Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Replaced the old Discovery dashboard hierarchy with `discovery-stage`, `discovery-orbit-controls`, `discovery-card-stage` and subordinate trays for filters, live, search, quiz and history.
- Added a mobile-only `app-bottom-nav` with visible labels, safe-area padding, active `Descobrir` state and desktop rail active markers limited to the current page.
- Tightened Discovery CSS around fixed typography roles, first-fold card constraints, focus/pressed/disabled states and source assertions that block the old four-mode row and heading `clamp()` regressions.

## Task Commits

Each task was committed atomically:

1. **Task 1: Recompose `/app/descobrir` around a dominant card stage** - `fdc9efb` (feat)
2. **Task 2: Add mobile bottom navigation and refine the desktop rail** - `1ddb9ef` (feat)
3. **Task 3: Apply the Phase 03.1 visual contract to the route CSS and structure tests** - `2e31ada` (test)

**Plan metadata:** pending final commit

## Files Created/Modified

- `apps/web/src/app/app/descobrir/page.tsx` - Reordered Discovery into deck-first route composition using public Discovery APIs and existing server actions.
- `apps/web/src/components/app-shell.tsx` - Added mobile bottom navigation from the authenticated nav model and refined active rail pointer rendering.
- `apps/web/src/app/globals.css` - Added responsive Discovery stage, mobile shell and Phase 03.1 typography/control-state CSS.
- `apps/web/tests/discovery-ui.test.tsx` - Updated route assertions and added CSS anti-pattern guards.
- `apps/web/tests/accessibility.spec.ts` - Updated authenticated Discovery accessibility expectations for mobile nav and deck-first heading.

## Decisions Made

- The route was reshaped without moving decision, filter or handoff business rules into `page.tsx`; it still composes `getDiscoveryDeck`, `getMatchHistory` and `getLiveSession`.
- Mobile navigation is layout only: protected pages still rely on their server-side session gates.
- CSS source assertions are used for visual contract regressions that component rendering alone would not catch reliably.

## Verification

- `pnpm --filter @queue/web test -- discovery-ui` - passed, 5 tests.
- `pnpm --filter @queue/web test -- discovery-ui accessibility` - passed, 5 tests through the Vitest filter command.
- `pnpm --filter @queue/web typecheck` - passed.
- `pnpm check:architecture` - passed.
- Source guard check - passed: page no longer contains `discovery-route-grid` or `discovery-side-rail`, CSS does not contain a `.discovery-mode-actions` four-column grid, and tests assert deck DOM order before `Mais filtros` and `Buscar jogo`.

## Acceptance Criteria

- `apps/web/src/app/app/descobrir/page.tsx` contains `discovery-stage`, `discovery-card-stage` and `discovery-orbit-controls`.
- `apps/web/src/app/app/descobrir/page.tsx` no longer contains `discovery-route-grid` or `discovery-side-rail`.
- `apps/web/tests/discovery-ui.test.tsx` asserts the deck group appears before `Mais filtros` and `Buscar jogo` in DOM order.
- `apps/web/tests/discovery-ui.test.tsx` rejects `Modos de descoberta` navigation/group and `.discovery-mode-actions`.
- `apps/web/src/components/app-shell.tsx` contains `app-bottom-nav`, `Navegacao principal mobile`, `/app/descobrir` and `Descobrir`.
- `apps/web/src/app/globals.css` contains `.app-bottom-nav` and `padding-bottom: env(safe-area-inset-bottom)`.
- `apps/web/tests/discovery-ui.test.tsx` asserts active `Descobrir` state in both desktop and mobile authenticated navs.
- `apps/web/src/app/globals.css` contains `.discovery-stage`, `.discovery-card-stage`, `.discovery-orbit-controls` and `.app-bottom-nav`.
- `apps/web/tests/discovery-ui.test.tsx` reads `src/app/globals.css` and blocks `.discovery-mode-actions { grid-template-columns: repeat(4`.
- `apps/web/tests/discovery-ui.test.tsx` asserts no Discovery CSS selector uses negative letter spacing.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Known Stubs

None. Stub scan found only intentional `label=""` values on `aria-hidden` decorative pointer icons and a default empty route prop object; no UI placeholder data or unwired read models were introduced.

## Threat Flags

None. The plan changed route/shell layout and tests only; it introduced no new endpoint, schema, file access pattern, auth path or network boundary.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for `03.1-03`: interaction, motion, search, match celebration and handoff polish can build on the deck-first route and mobile shell foundation.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-02-SUMMARY.md`.
- Key modified files found: `apps/web/src/app/app/descobrir/page.tsx`, `apps/web/src/components/app-shell.tsx`, `apps/web/src/app/globals.css`, `apps/web/tests/discovery-ui.test.tsx`, `apps/web/tests/accessibility.spec.ts`.
- Task commits found: `fdc9efb`, `1ddb9ef`, `2e31ada`.
- Plan verification commands passed after the final task commit.

---
*Phase: 03.1-refinos-visuais-e-ux-da-descoberta*
*Completed: 2026-06-04*
