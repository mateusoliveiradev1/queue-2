---
phase: "03.1-refinos-visuais-e-ux-da-descoberta"
plan: "04"
type: execute
wave: 4
depends_on: ["03.1-01", "03.1-02", "03.1-03"]
files_modified:
  - "apps/web/tests/discovery-ui.test.tsx"
  - "apps/web/tests/phase-3-e2e.spec.ts"
  - "apps/web/tests/accessibility.spec.ts"
  - ".env.example"
  - ".planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md"
  - ".planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-BROWSER-REVIEW.md"
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08, DISC-09, DISC-10, DISC-11, DISC-12, BRND-02, BRND-04, BRND-05, BRND-06, BRND-11, BRND-13, SAFE-05]
user_setup:
  - service: discovery-e2e-fixtures
    why: "Authenticated browser review requires a ready duo, partner account, other-duo account and stable discovery query."
    env_vars:
      - name: E2E_BASE_URL
        source: "Local or preview deployment URL"
      - name: E2E_READY_USER_EMAIL
        source: "Verified ready-duo member"
      - name: E2E_READY_USER_PASSWORD
        source: "Verified ready-duo member password"
      - name: E2E_READY_PARTNER_EMAIL
        source: "Verified ready-duo partner"
      - name: E2E_READY_PARTNER_PASSWORD
        source: "Verified ready-duo partner password"
      - name: E2E_OTHER_DUO_USER_EMAIL
        source: "Verified user in another duo for isolation checks"
      - name: E2E_OTHER_DUO_USER_PASSWORD
        source: "Other duo user password"
      - name: E2E_PHASE3_DISCOVERY_QUERY
        source: "Catalog query that returns an eligible coop card"
  - service: discovery-database-fixtures
    why: "Integration and browser checks need applied migrations and seeded Discovery/catalog data."
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Isolated Neon/test Postgres direct connection"
      - name: DATABASE_URL
        source: "Least-privileged app runtime connection for local/preview app"
      - name: DIRECT_DATABASE_URL
        source: "Owner/migrator direct connection for migrations"
      - name: VAPID_PUBLIC_KEY
        source: "Generated public key for Match Live push opt-in"
      - name: VAPID_PRIVATE_KEY
        source: "Generated private key kept server-side"
must_haves:
  truths:
    - "D-46 and D-47: Authenticated desktop, mobile and reduced-motion browser review is executed when fixtures exist or recorded as a blocking missing-fixture gate."
    - "Success criterion 1: Browser review explicitly checks hydration errors, text overlap, visible focus, broken controls, mobile usability and reduced-motion equivalents."
    - "Success criterion 2: E2E and component tests cover deck, filters, Match Live, Surpresa, Quiz, search, history and handoff under the new visual contract."
    - "Success criterion 3: Fixture documentation explains how to turn current skips into real E2E evidence."
    - "D-48: Setup documentation covers migrations, VAPID, `TEST_DATABASE_URL`, `E2E_BASE_URL`, ready duo fixtures and `E2E_PHASE3_DISCOVERY_QUERY`."
    - "SAFE-05: Setup docs preserve server-only handling for RAWG, VAPID private key, auth and database secrets."
  artifacts:
    - path: "apps/web/tests/phase-3-e2e.spec.ts"
      provides: "Authenticated Discovery desktop/mobile/reduced-motion E2E flow"
      min_lines: 180
    - path: "apps/web/tests/accessibility.spec.ts"
      provides: "Axe, focus and reduced-motion Discovery coverage"
      min_lines: 150
    - path: ".planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md"
      provides: "Discovery fixture and env setup runbook"
      contains: "E2E_PHASE3_DISCOVERY_QUERY"
    - path: ".planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-BROWSER-REVIEW.md"
      provides: "Desktop, mobile and reduced-motion browser evidence or blocking fixture gaps"
      contains: "Hydration errors"
  key_links:
    - from: "apps/web/tests/phase-3-e2e.spec.ts"
      to: "apps/web/src/app/app/descobrir/page.tsx"
      via: "authenticated browser route checks"
      pattern: "discovery-card-stage"
    - from: "apps/web/tests/accessibility.spec.ts"
      to: "apps/web/src/modules/discovery/presentation/discovery-deck.tsx"
      via: "keyboard and reduced-motion assertions"
      pattern: "Quero jogar"
    - from: ".env.example"
      to: ".planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md"
      via: "documented fixture variables"
      pattern: "TEST_DATABASE_URL|E2E_BASE_URL|VAPID_PRIVATE_KEY"
---

<objective>
Create blocking browser evidence and Discovery setup documentation.

Purpose: Phase 03.1 is a quality gate before Jogando. It must convert the current skipped browser checks into executable evidence when fixtures exist, and clearly record any remaining environment gates instead of treating skips as passes.
Output: updated E2E/accessibility tests, fixture documentation, `.env.example` updates and a browser review evidence file.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-UI-SPEC.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-RESEARCH.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-VALIDATION.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-PATTERNS.md
@.planning/phases/03-descoberta-e-matches/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update browser specs for desktop, mobile and reduced-motion Discovery review</name>
  <files>apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, apps/web/tests/discovery-ui.test.tsx</files>
  <read_first>.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-VALIDATION.md, apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, apps/web/tests/discovery-ui.test.tsx, apps/web/src/app/app/descobrir/page.tsx</read_first>
  <action>Per D-46 and D-47, update `phase-3-e2e.spec.ts` so it verifies the new Phase 03.1 contract in real authenticated browser flows when fixtures exist. Add explicit desktop viewport, mobile viewport, and `page.emulateMedia({ reducedMotion: "reduce" })` cases for `/app/descobrir`. Each case must check: no console errors containing `Hydration`, `hydration`, `Text content does not match`, or `Expected server HTML`; `.discovery-card-stage` is visible before filters/search; no locator overlap for the active card title and decision buttons using bounding boxes; visible focus after tabbing to deck decisions/search/filter controls; mobile bottom nav has active `Descobrir`; reduced motion still shows decision state, search result, match celebration and handoff information. Keep existing skip diagnostics, but make skipped output list every missing env variable. Update `accessibility.spec.ts` to include reduced-motion focus, source links and axe checks for the new selectors. Update `discovery-ui.test.tsx` with fast structural coverage so the E2E spec is not the only signal.</action>
  <verify>
    <automated>pnpm --filter @queue/web test -- discovery-ui</automated>
    <automated>pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/tests/phase-3-e2e.spec.ts` contains `setViewportSize`, `reducedMotion: \"reduce\"`, `discovery-card-stage`, `Hydration`, and `boundingBox`.
    - `apps/web/tests/phase-3-e2e.spec.ts` keeps `reportMissingEnv` and includes `E2E_READY_PARTNER_EMAIL`, `E2E_OTHER_DUO_USER_EMAIL`, and `E2E_PHASE3_DISCOVERY_QUERY` in the missing-env list.
    - `apps/web/tests/accessibility.spec.ts` contains `/app/descobrir` and checks focus on `Quero jogar`, `Agora nao`, and `Pular`.
    - `pnpm --filter @queue/web test -- discovery-ui` exits 0.
    - Playwright E2E command either exits 0 with real fixtures or exits/skips with missing variable names printed; no skip may be summarized as a pass in the plan summary.
  </acceptance_criteria>
  <done>Browser specs can prove the visual/UX contract when fixtures exist and diagnose missing setup honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Write Discovery fixture setup and environment runbook</name>
  <files>.env.example, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md</files>
  <read_first>.env.example, .planning/SECURITY.md, .planning/phases/03-descoberta-e-matches/03-04-USER-SETUP.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md</read_first>
  <action>Per D-48, update `.env.example` and create `03.1-USER-SETUP.md`. The runbook must include exact sections: `## Required Database Setup`, `## Required Browser Fixtures`, `## Required Push Keys`, `## Commands`, `## Blocking Evidence Rules`, and `## Before Phase 4`. Document migrations with `DIRECT_DATABASE_URL` and `pnpm --filter @queue/db drizzle:migrate` only if generated Drizzle metadata exists; otherwise document applying checked-in SQL through the project's direct SQL/integration harness as established in Phase 02.1. Include `TEST_DATABASE_URL`, `DATABASE_URL`, `DIRECT_DATABASE_URL`, `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `E2E_BASE_URL`, `E2E_READY_USER_EMAIL`, `E2E_READY_USER_PASSWORD`, `E2E_READY_PARTNER_EMAIL`, `E2E_READY_PARTNER_PASSWORD`, `E2E_OTHER_DUO_USER_EMAIL`, `E2E_OTHER_DUO_USER_PASSWORD`, and `E2E_PHASE3_DISCOVERY_QUERY`. State that `RAWG_API_KEY`, VAPID private key, auth secrets and privileged database URLs stay server-only and must not be exposed through `NEXT_PUBLIC_*`. Include commands `pnpm check:architecture`, `pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search`, `pnpm --filter @queue/db test:integration -- discovery`, and `pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts`.</action>
  <verify>
    <automated>pnpm check:secrets</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md` contains all six required headings.
    - `.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md` contains `TEST_DATABASE_URL`, `E2E_BASE_URL`, `E2E_READY_PARTNER_EMAIL`, `E2E_OTHER_DUO_USER_EMAIL`, `E2E_PHASE3_DISCOVERY_QUERY`, `VAPID_PRIVATE_KEY`, and `RAWG_API_KEY`.
    - `.env.example` contains `E2E_PHASE3_DISCOVERY_QUERY` and `VAPID_PRIVATE_KEY`.
    - `.env.example` does not add any new `NEXT_PUBLIC_*` variable for RAWG, VAPID private, auth secret, `DATABASE_URL`, `DIRECT_DATABASE_URL`, or `TEST_DATABASE_URL`.
    - `pnpm check:secrets` exits 0.
  </acceptance_criteria>
  <done>Discovery E2E and local setup requirements are explicit before Phase 4 starts.</done>
</task>

<task type="auto">
  <name>Task 3: Run final feasible verification and record browser evidence</name>
  <files>.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-BROWSER-REVIEW.md, apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts</files>
  <read_first>.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-VALIDATION.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-USER-SETUP.md, apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts</read_first>
  <action>Create `03.1-BROWSER-REVIEW.md` with sections `## Environment`, `## Desktop Review`, `## Mobile Review`, `## Reduced Motion Review`, `## Hydration Errors`, `## Focus And Controls`, `## Text Overlap`, `## Result`, and `## Remaining Gates`. Start a local or configured app only if `E2E_BASE_URL`/fixtures are available or `E2E_START_SERVER=1` is configured by the environment. Run the feasible commands from `03.1-VALIDATION.md`: `pnpm check:architecture`, `pnpm --filter @queue/web typecheck`, `pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search`, `pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts`, and `pnpm --filter @queue/db test:integration -- discovery` when `TEST_DATABASE_URL` exists. In the review file, record exact pass/fail/skip lines. If any browser or DB command skips because env is missing, mark `Result: BLOCKED - missing fixture evidence` and list variable names. If all required fixtures exist and commands pass, mark `Result: PASSED` and include viewport sizes used for desktop and mobile. Do not claim skipped browser review passed.</action>
  <verify>
    <automated>pnpm check:architecture</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search</automated>
    <automated>pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts</automated>
    <automated>pnpm --filter @queue/db test:integration -- discovery</automated>
  </verify>
  <acceptance_criteria>
    - `03.1-BROWSER-REVIEW.md` contains all nine required headings.
    - `03.1-BROWSER-REVIEW.md` contains either `Result: PASSED` or `Result: BLOCKED - missing fixture evidence`.
    - If the review file contains `skipped`, it also contains `Remaining Gates` and at least one exact missing env var name.
    - `pnpm check:architecture` exits 0.
    - `pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search` exits 0.
    - The plan summary must state whether E2E/browser/DB evidence passed or remains blocked by fixture gaps.
  </acceptance_criteria>
  <done>Phase 03.1 has honest browser/setup evidence before execution can move to Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> private duo data | E2E credentials access real authenticated routes and must remain test-only. |
| Environment docs -> secret handling | Setup docs must not normalize exposing privileged secrets to browser code. |
| Skipped checks -> release evidence | Missing fixtures are blockers, not passing evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-11 | Information Disclosure | `.env.example` and setup docs | mitigate | Keep RAWG, VAPID private, auth and database credentials server-only; do not add `NEXT_PUBLIC_*` secret variables. |
| T-03.1-12 | Spoofing | E2E ready-duo accounts | mitigate | Require verified ready duo, partner and other-duo fixtures; tests log in through product auth instead of bypass backdoors. |
| T-03.1-13 | Repudiation | Browser review evidence | mitigate | Record exact command result lines, viewport sizes, skipped variable names and final result status in `03.1-BROWSER-REVIEW.md`. |
| T-03.1-14 | Elevation of Privilege | Test database and runtime database URLs | mitigate | Document separate `TEST_DATABASE_URL`, `DATABASE_URL` and `DIRECT_DATABASE_URL`; never use owner/migrator URL as browser/public config. |
</threat_model>

<verification>
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search` passes.
- [ ] `pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts` passes or records missing fixture blockers by exact env var name.
- [ ] `pnpm --filter @queue/db test:integration -- discovery` passes or records missing `TEST_DATABASE_URL`.
- [ ] `03.1-BROWSER-REVIEW.md` clearly says PASSED or BLOCKED.
</verification>

<success_criteria>
- Desktop, mobile and reduced-motion browser checks are executable and honest.
- Fixture/setup documentation is sufficient to run real authenticated Discovery E2E.
- Missing browser/database fixtures cannot be mistaken for passing evidence.
- Server-only secret boundaries remain explicit.
- Phase 4 starts with Discovery visual debt either passed or clearly blocked by named environment gates.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-04-SUMMARY.md`.
</output>
