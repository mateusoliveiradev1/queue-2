# Phase 03.1: User Setup Required

**Generated:** 2026-06-04
**Phase:** 03.1-refinos-visuais-e-ux-da-descoberta
**Status:** Incomplete

Complete these items to turn the Discovery browser and database skips into real blocking evidence before Phase 4.

## Required Database Setup

Use separate Neon branches or local Postgres databases for development, preview/test and production. Never run Discovery evidence against unsanitized production data.

| Status | Variable | Source | Add to |
|--------|----------|--------|--------|
| [ ] | `DATABASE_URL` | Least-privileged pooled app runtime connection | `.env.local`, preview env |
| [ ] | `DIRECT_DATABASE_URL` | Direct owner/migrator connection for reviewed SQL/admin operations | `.env.local`, migration shell only |
| [ ] | `TEST_DATABASE_URL` | Isolated Neon/test Postgres direct connection | `.env.local`, CI test env |

Current migration path:

- The repo currently has hand-authored SQL migrations in `packages/db/src/migrations`.
- There is no generated Drizzle metadata journal for these migrations, so `pnpm --filter @queue/db drizzle:migrate` is not Phase 03.1 evidence right now.
- The established Phase 02.1 path is the direct SQL/integration harness: `packages/db/src/testing/migrate-empty.ts` applies checked-in `000*_*.sql` files in order through `pg` when `TEST_DATABASE_URL` is set.
- `DIRECT_DATABASE_URL` must remain a direct owner/migrator URL for reviewed migration/admin work, not the pooled runtime URL and never a browser/public variable.

## Required Browser Fixtures

Browser review needs real authenticated users. The tests log in through product auth; do not bypass the app with test-only session backdoors.

| Status | Variable | Source | Add to |
|--------|----------|--------|--------|
| [ ] | `E2E_BASE_URL` | Local app URL or preview deployment URL | `.env.local`, CI |
| [ ] | `E2E_READY_USER_EMAIL` | Verified ready-duo member | `.env.local`, CI secrets |
| [ ] | `E2E_READY_USER_PASSWORD` | Ready-duo member password | `.env.local`, CI secrets |
| [ ] | `E2E_READY_PARTNER_EMAIL` | Verified partner in the same duo | `.env.local`, CI secrets |
| [ ] | `E2E_READY_PARTNER_PASSWORD` | Ready-duo partner password | `.env.local`, CI secrets |
| [ ] | `E2E_OTHER_DUO_USER_EMAIL` | Verified user in another duo | `.env.local`, CI secrets |
| [ ] | `E2E_OTHER_DUO_USER_PASSWORD` | Other-duo user password | `.env.local`, CI secrets |
| [ ] | `E2E_PHASE3_DISCOVERY_QUERY` | Query returning an eligible coop Discovery card | `.env.local`, CI |

Fixture requirements:

- Ready user and partner are verified, named and paired in exactly one duo.
- Other-duo user is verified and belongs to a different duo for live-session isolation checks.
- Discovery/catalog data includes at least one eligible coop card matching `E2E_PHASE3_DISCOVERY_QUERY`.
- The ready duo can reach `/app/descobrir` without pairing or naming redirects.

## Required Push Keys

Match Live push opt-in needs VAPID keys even when browser review focuses on Discovery layout and reduced motion.

| Status | Variable | Source | Add to |
|--------|----------|--------|--------|
| [ ] | `VAPID_PUBLIC_KEY` | Generated Web Push public key | `.env.local`, preview env |
| [ ] | `VAPID_PRIVATE_KEY` | Generated Web Push private key | `.env.local`, server/preview env only |
| [ ] | `VAPID_SUBJECT` | Contact URI such as `mailto:admin@example.com` | `.env.local`, preview env |

Server-only boundary:

- `RAWG_API_KEY`, `VAPID_PRIVATE_KEY`, Better Auth secrets, email credentials, `DATABASE_URL`, `DIRECT_DATABASE_URL` and `TEST_DATABASE_URL` stay server-only.
- Do not add `NEXT_PUBLIC_*` variants for RAWG, VAPID private, auth secrets or database URLs.
- Browser code receives only the public VAPID key through the authenticated Discovery push endpoint.

## Commands

Run from the repository root after `.env.local` or the CI environment has the variables above:

```bash
pnpm check:secrets
pnpm check:architecture
pnpm --filter @queue/web test -- discovery-ui discovery-application discovery-search
pnpm --filter @queue/db test:integration -- discovery
pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts tests/accessibility.spec.ts
```

Expected command behavior:

- `pnpm check:secrets` passes and does not report private secrets in client code or client bundles.
- `pnpm check:architecture` passes with Discovery application code still separated from presentation internals.
- Web tests pass and preserve the exhausted-deck partner-wait state as distinct from filter-only empty results.
- DB integration tests run against `TEST_DATABASE_URL`; if `TEST_DATABASE_URL` is missing, the skip is a blocker.
- Browser specs run against `E2E_BASE_URL`; if E2E fixture variables are missing, the skip is a blocker.

## Blocking Evidence Rules

- A skipped browser review is not a pass.
- A skipped database integration run is not a pass.
- Missing fixtures must be recorded by exact variable name: `TEST_DATABASE_URL`, `E2E_BASE_URL`, `E2E_READY_USER_EMAIL`, `E2E_READY_USER_PASSWORD`, `E2E_READY_PARTNER_EMAIL`, `E2E_READY_PARTNER_PASSWORD`, `E2E_OTHER_DUO_USER_EMAIL`, `E2E_OTHER_DUO_USER_PASSWORD` or `E2E_PHASE3_DISCOVERY_QUERY`.
- Phase 03.1 browser evidence must explicitly cover desktop, mobile and reduced motion for `/app/descobrir`.
- Evidence must mention hydration errors, visible focus, text overlap, mobile active `Descobrir` navigation, decision controls, search, match celebration and handoff information.

## Before Phase 4

- [ ] `03.1-BROWSER-REVIEW.md` says `Result: PASSED` after real fixtures run, or `Result: BLOCKED - missing fixture evidence` with exact missing env vars.
- [ ] Browser evidence includes desktop and mobile viewport sizes.
- [ ] Reduced-motion browser evidence confirms decisions, search result, match celebration and handoff remain understandable.
- [ ] `pnpm --filter @queue/db test:integration -- discovery` runs with `TEST_DATABASE_URL` or the missing `TEST_DATABASE_URL` gate is explicitly carried forward.
- [ ] No server-only secret is copied into `NEXT_PUBLIC_*`, source files rendered to the browser, logs or review screenshots.
