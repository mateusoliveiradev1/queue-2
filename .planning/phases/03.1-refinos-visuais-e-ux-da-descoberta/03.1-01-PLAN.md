---
phase: "03.1-refinos-visuais-e-ux-da-descoberta"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "apps/web/src/modules/discovery/application/view-models.ts"
  - "apps/web/src/modules/discovery/application/get-discovery-deck.ts"
  - "apps/web/src/modules/discovery/application/search-discovery-games.ts"
  - "apps/web/src/modules/discovery/application/get-surprise-recommendation.ts"
  - "apps/web/src/modules/discovery/application/ports.ts"
  - "apps/web/src/modules/discovery/presentation/view-models.ts"
  - "apps/web/src/modules/discovery/index.ts"
  - "apps/web/tests/discovery-application.test.ts"
  - "apps/web/tests/discovery-search.test.ts"
  - "scripts/check-architecture.mjs"
autonomous: true
requirements: [DISC-01, DISC-03, DISC-05, DISC-10, DISC-11, DISC-12, SAFE-05]
user_setup: []
must_haves:
  truths:
    - "D-45 and success criterion 4: Discovery application use cases no longer import `../presentation/view-models`."
    - "D-07 through D-10: `Quero jogar`, `Agora nao` and `Pular` keep the existing server-authoritative decision semantics after the read-model move."
    - "D-25 and DISC-05: Search/autocomplete still returns Discovery card data that can become the focused card context."
    - "D-03, D-05, DISC-10 and DISC-11: Recommendation card facts still preserve source/freshness, common platform, time, availability and reason labels."
    - "SAFE-05: No RAWG, VAPID, database or auth secret-bearing module becomes importable by a Client Component."
    - "Architecture: `pnpm check:architecture` fails on same-module `application -> presentation` imports."
  artifacts:
    - path: "apps/web/src/modules/discovery/application/view-models.ts"
      provides: "Application-safe Discovery deck card and recommendation fact builders"
      contains: "buildDiscoveryDeckCards"
    - path: "scripts/check-architecture.mjs"
      provides: "Same-module layer-order architecture enforcement"
      contains: "module-layer-direction"
    - path: "apps/web/tests/discovery-application.test.ts"
      provides: "Use-case coverage after read-model relocation"
      min_lines: 160
  key_links:
    - from: "apps/web/src/modules/discovery/application/get-discovery-deck.ts"
      to: "apps/web/src/modules/discovery/application/view-models.ts"
      via: "application-safe read model import"
      pattern: "buildDiscoveryDeckCards"
    - from: "scripts/check-architecture.mjs"
      to: "apps/web/src/modules/discovery/application/search-discovery-games.ts"
      via: "static import analysis"
      pattern: "application -> presentation"
    - from: "apps/web/src/modules/discovery/index.ts"
      to: "apps/web/src/modules/discovery/application/ports.ts"
      via: "public type exports"
      pattern: "DiscoveryDeckCard"
---

<objective>
Resolve the Discovery application-to-presentation architecture debt.

Purpose: Phase 03.1 cannot polish Discovery on top of a known module-layer inversion. This plan moves card/read-model builders into an application-safe layer and makes the architecture checker block the same regression.
Output: application-safe Discovery view-model helpers, updated use cases/tests, and same-module layer-order enforcement.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-RESEARCH.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-VALIDATION.md
@.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-PATTERNS.md
@.planning/phases/03-descoberta-e-matches/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Discovery card builders into the application layer</name>
  <files>apps/web/src/modules/discovery/application/view-models.ts, apps/web/src/modules/discovery/presentation/view-models.ts, apps/web/src/modules/discovery/application/get-discovery-deck.ts, apps/web/src/modules/discovery/application/search-discovery-games.ts, apps/web/src/modules/discovery/application/get-surprise-recommendation.ts, apps/web/src/modules/discovery/application/ports.ts, apps/web/src/modules/discovery/index.ts</files>
  <read_first>.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-PATTERNS.md, apps/web/src/modules/discovery/presentation/view-models.ts, apps/web/src/modules/discovery/application/get-discovery-deck.ts, apps/web/src/modules/discovery/application/search-discovery-games.ts, apps/web/src/modules/discovery/application/get-surprise-recommendation.ts, apps/web/src/modules/discovery/application/ports.ts</read_first>
  <action>Per D-45, create `apps/web/src/modules/discovery/application/view-models.ts` and move the pure functions currently in `presentation/view-models.ts`: `buildDiscoveryDeckCards`, `toDiscoveryDeckCardView`, `toRecommendationFacts`, `getReadableGameState`, `getLibraryActionState`, `compactFallbackReasons`, `parseEstimatedMinutes`, and `parseAvailability`. Preserve the current function signatures and return shapes. Update `get-discovery-deck.ts`, `search-discovery-games.ts`, and `get-surprise-recommendation.ts` to import from `./view-models` instead of `../presentation/view-models`. Keep `presentation/view-models.ts` only if it re-exports from `../application/view-models` for compatibility; otherwise delete it and update all imports. Do not move business rules into React components and do not add database, auth, RAWG, VAPID or server-only imports to the new application read-model file.</action>
  <verify>
    <automated>pnpm --filter @queue/web test -- discovery-application discovery-search</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/modules/discovery/application/view-models.ts` contains `export function buildDiscoveryDeckCards`.
    - `rg '../presentation/view-models|\"../presentation/view-models\"' apps/web/src/modules/discovery/application` returns no matches.
    - `apps/web/src/modules/discovery/application/view-models.ts` does not contain `from "react"`, `from "next`, `server-only`, `drizzle-orm`, `@neondatabase`, `RAWG_API_KEY`, `VAPID_PRIVATE_KEY`, or `DATABASE_URL`.
    - `pnpm --filter @queue/web test -- discovery-application discovery-search` exits 0.
  </acceptance_criteria>
  <done>Discovery application use cases build deck/search/surprise cards without importing presentation internals.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the architecture checker for same-module layer direction</name>
  <files>scripts/check-architecture.mjs</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-CONTEXT.md, .planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-PATTERNS.md, scripts/check-architecture.mjs</read_first>
  <action>Per D-45, add a same-module layer-order rule to `scripts/check-architecture.mjs`. Reuse `collectImportSpecifiers`, `getModuleInfo`, `getImportModuleTarget`, `resolveImportPath`, and `formatViolation`. Add a `checkSameModuleLayerDirection(relativePath, filePath, moduleInfo, imports)` pass inside `analyzeSourceFile` after cross-module deep-import checks. Enforce this order: `presentation` may import `application` and `domain`; `application` may import `domain`; `infrastructure` may import `application`, `domain`, and `events`; `domain` may not import other internal layers; `events` may import `domain` only. For same-domain imports where source and target are both internal layers and the target is not allowed, report rule `module-layer-direction` with detail `Module "<domain>" <sourceLayer> cannot import <targetLayer> via "<specifier>". Allowed direction is presentation -> application -> domain and infrastructure -> application ports/domain.` Ensure this catches `apps/web/src/modules/discovery/application/get-discovery-deck.ts` importing `../presentation/view-models`.</action>
  <verify>
    <automated>pnpm check:architecture</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-architecture.mjs` contains `module-layer-direction`.
    - `scripts/check-architecture.mjs` contains `checkSameModuleLayerDirection`.
    - `scripts/check-architecture.mjs` contains the text `presentation -> application -> domain`.
    - A temporary manual inspection confirms `application` imports of `presentation` would be reported by the new rule before any final commit; do not leave temporary violating files in the repo.
    - `pnpm check:architecture` exits 0 after the real source is fixed.
  </acceptance_criteria>
  <done>The architecture gate blocks the exact Discovery regression and equivalent same-module layer inversions.</done>
</task>

<task type="auto">
  <name>Task 3: Update application and search tests for the new boundary</name>
  <files>apps/web/tests/discovery-application.test.ts, apps/web/tests/discovery-search.test.ts</files>
  <read_first>apps/web/tests/discovery-application.test.ts, apps/web/tests/discovery-search.test.ts, apps/web/src/modules/discovery/application/view-models.ts, apps/web/src/modules/discovery/application/get-discovery-deck.ts, scripts/check-architecture.mjs</read_first>
  <action>Update the Discovery application tests to prove behavior survived the read-model move and to make the architecture debt observable. Add source-level assertions that `get-discovery-deck.ts`, `search-discovery-games.ts`, and `get-surprise-recommendation.ts` do not contain `../presentation/view-models`. Keep the existing behavior coverage for deck filtering, search bounds, surprise unseen-card eligibility, source/freshness propagation, library status, valid handoff statuses and membership-required results. Use the PATTERNS.md analog of existing `readFileSync` source assertions from `discovery-ui.test.tsx`; do not introduce a new test framework.</action>
  <verify>
    <automated>pnpm --filter @queue/web test -- discovery-application discovery-search</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/tests/discovery-application.test.ts` or `apps/web/tests/discovery-search.test.ts` contains `../presentation/view-models`.
    - The test assertion expects the application source not to contain `../presentation/view-models`.
    - Existing assertions for `want`, `not_now`, `skip`, search bounds, surprise and valid library handoff still pass.
    - `pnpm --filter @queue/web test -- discovery-application discovery-search` exits 0.
    - `pnpm check:architecture` exits 0.
  </acceptance_criteria>
  <done>Tests protect both behavior and the resolved architecture boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discovery application -> presentation | Application use cases must not import React/UI code or presentation-only adapters. |
| Browser -> Discovery Server Actions/API | Browser state remains untrusted; server actions and route handlers stay authoritative. |
| Client bundle -> server-only secrets | RAWG, VAPID private key, auth and database credentials must not enter client imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | `apps/web/src/modules/discovery/application/*` | mitigate | Move card builders to `application/view-models.ts` and run `pnpm check:architecture` so `application -> presentation` imports fail. |
| T-03.1-02 | Information Disclosure | `application/view-models.ts` and Client Components | mitigate | Keep the read-model file pure and free of `server-only`, database, RAWG, VAPID and auth secret imports; verify with source grep and typecheck. |
| T-03.1-03 | Repudiation | Architecture gate output | mitigate | Add explicit `module-layer-direction` violation messages naming file, line, source layer, target layer and import specifier. |
</threat_model>

<verification>
- [ ] `pnpm --filter @queue/web test -- discovery-application discovery-search` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] `rg '../presentation/view-models|\"../presentation/view-models\"' apps/web/src/modules/discovery/application` returns no matches.
</verification>

<success_criteria>
- The known Discovery architecture debt is removed.
- Same-module layer-order regressions are blocked by `check:architecture`.
- Discovery deck, search and surprise read models preserve Phase 3 behavior and source/freshness data.
- No server-only secret boundary is weakened.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-refinos-visuais-e-ux-da-descoberta/03.1-01-SUMMARY.md`.
</output>
