---
phase: 02.1-localizacao-e-qualidade-do-catalogo
plan: "01"
subsystem: catalog
tags: [postgres, drizzle, localization, pt-br, rawg, catalog]
requires:
  - phase: "02-catalogo-e-biblioteca"
    provides: "RAWG-backed catalog facts, source metadata and catalog module public APIs"
provides:
  - "Versioned catalog.game_localizations table for reviewed PT-BR descriptions"
  - "Published localization hydration on catalog search and detail records"
  - "Server-only curated localization seed representation"
  - "Domain fallback policy that never displays raw English RAWG text"
affects: [catalog, discovery, rawg-sync, source-freshness-ui]
tech-stack:
  added: []
  patterns:
    - "Public catalog localization data uses least-privilege grants instead of duo RLS"
    - "Normal catalog reads hydrate only latest published pt-BR localization"
    - "Presentation view models consume domain description state, not hardcoded copy maps"
key-files:
  created:
    - "packages/db/src/migrations/0006_catalog_localizations.sql"
    - "packages/db/tests/catalog-localization.test.ts"
    - "apps/web/src/modules/catalog/infrastructure/localization-seeds.ts"
  modified:
    - "packages/db/src/schema/catalog.ts"
    - "packages/db/tests/catalog-schema.test.ts"
    - "apps/web/src/modules/catalog/application/ports.ts"
    - "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
    - "apps/web/src/modules/catalog/domain/catalog-policy.ts"
    - "apps/web/src/modules/catalog/presentation/view-models.ts"
    - "apps/web/tests/catalog-domain.test.ts"
    - "apps/web/tests/catalog-library-ui.test.tsx"
key-decisions:
  - "Reviewed PT-BR descriptions are public catalog metadata, so least-privilege grants are the protection boundary instead of duo-scoped RLS."
  - "Only published pt-BR localization rows are visible through normal catalog reads; draft, review and rejected states remain worker/admin workflow data."
  - "Missing reviewed localization shows honest PT-BR unavailable copy and does not fall back to RAWG description text."
patterns-established:
  - "Catalog source facts and QUEUE/2 localization copy stay separate in records and UI labels."
  - "Curated seed text lives in server-only infrastructure for future import/sync paths."
requirements-completed: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, SAFE-05]
duration: 6 min
completed: 2026-06-03
---

# Phase 02.1 Plan 01: Localization Data Contract Summary

**Versioned PT-BR catalog localization rows with published-only reads and no raw-English fallback**

## Performance

- **Duration:** 6 min
- **Started:** 2026-06-03T20:44:45-03:00
- **Completed:** 2026-06-03T20:50:22-03:00
- **Tasks:** 4
- **Files modified:** 12

## Accomplishments

- Added `catalog.game_localizations` with status/version/provenance/review checklist metadata, published-read index and runtime SELECT-only grants.
- Extended catalog read records and repository hydration so search/detail can carry the newest published `pt-BR` localization while hiding draft/review/rejected rows.
- Migrated accepted PT-BR curated descriptions out of presentation lookup code into a server-only infrastructure seed representation.
- Added domain and use-case tests that lock the `Descricao revisada: QUEUE/2` label, missing localization copy, and no raw English RAWG fallback.

## Task Commits

1. **Task 1: Add versioned catalog localization schema** - `4eee90d`
2. **Task 2: Extend catalog records and repository reads with published localization** - `39f9153`
3. **Task 3: Replace hardcoded presentation descriptions with seeded persisted copy** - `52b3312`
4. **Task 4: Add localization domain policy tests** - `2c2ab4c`

## Files Created/Modified

- `packages/db/src/migrations/0006_catalog_localizations.sql` - Creates localization table, constraints, comments, indexes and grants.
- `packages/db/src/schema/catalog.ts` - Adds Drizzle schema for `catalog.game_localizations`.
- `packages/db/tests/catalog-localization.test.ts` - Covers status constraints and newest published lookup assumptions.
- `packages/db/tests/catalog-schema.test.ts` - Covers localization columns and runtime/worker privileges.
- `apps/web/src/modules/catalog/application/ports.ts` - Adds `CatalogLocalizationRecord` and `localization` to detail records.
- `apps/web/src/modules/catalog/infrastructure/catalog-repository.ts` - Hydrates latest published `pt-BR` localization.
- `apps/web/src/modules/catalog/infrastructure/localization-seeds.ts` - Holds accepted curated PT-BR seed rows with quality metadata.
- `apps/web/src/modules/catalog/domain/catalog-policy.ts` - Adds description state policy and readiness based on reviewed localization.
- `apps/web/src/modules/catalog/presentation/view-models.ts` - Uses domain description state instead of presentation hardcoded copy.
- `apps/web/tests/catalog-domain.test.ts` - Locks published localization, invisible draft behavior, missing copy and no English fallback.

## Decisions Made

- Public catalog localization rows do not use RLS because they contain no duo-private data; role grants enforce runtime read-only behavior.
- PT-BR source label is locked as `Descricao revisada: QUEUE/2` for published rows.
- `catalog.games.description` remains available for RAWG source storage but is not product-facing fallback copy.

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed. **Impact:** No scope changes.

## Issues Encountered

- `TEST_DATABASE_URL` is not configured, so DB integration tests reported the planned explicit skip instead of running against Postgres.
- Adding `localization` to the catalog record type required updating existing test fixtures so the new contract is explicit.

## Verification

- `pnpm --filter @queue/db typecheck` passed.
- `pnpm --filter @queue/web typecheck` passed.
- `pnpm --filter @queue/web test -- catalog-domain` passed.
- `pnpm check:architecture` passed.
- `pnpm --filter @queue/db test:integration -- catalog` skipped explicitly because `TEST_DATABASE_URL` is not configured.
- `pnpm check:secrets` passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02.1-02 can use the localization seed representation and published-only read contract to build the auditable RAWG sync and localization draft workflow.

---
*Phase: 02.1-localizacao-e-qualidade-do-catalogo*
*Completed: 2026-06-03*
