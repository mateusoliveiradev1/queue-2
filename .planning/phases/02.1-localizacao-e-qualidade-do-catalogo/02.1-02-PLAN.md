---
phase: 02.1-localizacao-e-qualidade-do-catalogo
plan: "02"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - ".env.example"
  - "package.json"
  - "scripts/check-secrets.mjs"
  - "scripts/catalog-sync.ts"
  - "packages/db/src/schema/ops.ts"
  - "packages/db/src/schema/index.ts"
  - "packages/db/src/migrations/0007_catalog_sync_ops.sql"
  - "packages/db/tests/catalog-sync-ops.test.ts"
  - "apps/web/src/modules/catalog/application/ports.ts"
  - "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
  - "apps/web/src/modules/catalog/infrastructure/rawg-client.ts"
  - "apps/web/src/modules/catalog/infrastructure/catalog-sync.ts"
  - "apps/web/src/modules/catalog/infrastructure/catalog-sync-allowlist.ts"
  - "apps/web/src/modules/catalog/infrastructure/localization-draft-provider.ts"
  - "apps/web/src/modules/catalog/infrastructure/localization-review-checklist.ts"
  - "apps/web/tests/catalog-sync.test.ts"
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, SAFE-05]
user_setup:
  - service: rawg
    why: "RAWG remains the sourced catalog provider and the sync command needs a server-side key."
    env_vars:
      - name: RAWG_API_KEY
        source: "RAWG account API key"
  - service: localization-provider
    why: "Optional only if provider-assisted draft generation is enabled; published v1 copy can ship from QUEUE/2 curated seeds."
    env_vars:
      - name: DEEPL_API_KEY
        source: "DeepL API key for draft generation"
        optional: true
      - name: OPENAI_API_KEY
        source: "OpenAI API key for optional review/rewriting assistance"
        optional: true
must_haves:
  truths:
    - "CAT-01: Catalog synchronization is a repeatable server/worker command that can populate or update RAWG-backed catalog games."
    - "CAT-02: Sync preserves active RAWG URLs needed for attribution on every page using RAWG data or images."
    - "CAT-03: Sync keeps RAWG facts needed by detail pages: cover, description storage, genres, release information and platforms."
    - "CAT-04 and CAT-05: Sync can update sourced time/availability records only when a trusted source is available; it must not invent them."
    - "SAFE-05 and D-20: RAWG and translation-provider secrets stay server-only and are scanned against client-source/bundle leakage."
    - "D-02: Provider choice is documented after comparing DeepL, OpenAI, Google and Azure against cost, PT-BR quality, operational fit, terms, secret handling and review workflow."
    - "D-05: Provider-assisted draft output cannot publish unless the mandatory quality checklist passes."
    - "D-07: Only internal operation/admin technical paths can approve or publish PT-BR descriptions."
    - "D-15: Phase 02.1 provides a versioned command/worker path for catalog sync with allowlist/seeds, dry-run support, structured logs and transactional upserts."
    - "D-16: Initial sync input is a curated allowlist of confirmed or candidate two-player coop games; broad RAWG search results do not automatically enter the main catalog."
    - "D-17: RAWG sync may update RAWG-owned facts but must not overwrite QUEUE/2 curation such as coop confirmation, main-flow eligibility, localizations, curated time estimates or curated availability."
    - "D-18: Sync runs and failures are auditable through ops-owned persistence with run id, timestamps, item counts, per-item errors and relevant changes."
    - "D-19: The sync path is prepared for future cron, but daily production cron activation is not required."
  artifacts:
    - path: "scripts/catalog-sync.ts"
      provides: "Versioned Node 24 CLI entrypoint for dry-run/apply catalog sync"
      contains: "--dry-run"
    - path: "apps/web/src/modules/catalog/infrastructure/catalog-sync.ts"
      provides: "Server-only sync orchestration and curation-preserving upserts"
      contains: "preserve"
    - path: "apps/web/src/modules/catalog/infrastructure/catalog-sync-allowlist.ts"
      provides: "Curated coop 2p RAWG sync input"
      min_lines: 20
    - path: "packages/db/src/migrations/0007_catalog_sync_ops.sql"
      provides: "Ops audit tables for catalog sync runs/items"
      contains: "ops.catalog_sync_runs"
    - path: "apps/web/tests/catalog-sync.test.ts"
      provides: "Dry-run, apply, curation preservation and secret boundary tests"
      min_lines: 80
  key_links:
    - from: "scripts/catalog-sync.ts"
      to: "apps/web/src/modules/catalog/infrastructure/catalog-sync.ts"
      via: "Node 24 type-stripped CLI command importing the shared worker module by explicit .ts path"
      pattern: "runCatalogSync"
    - from: "apps/web/src/modules/catalog/infrastructure/catalog-sync.ts"
      to: "apps/web/src/modules/catalog/infrastructure/rawg-client.ts"
      via: "server-only RAWG fetches"
      pattern: "createRawgClient"
    - from: "apps/web/src/modules/catalog/infrastructure/catalog-sync.ts"
      to: "packages/db/src/schema/ops.ts"
      via: "sync run/item audit persistence"
      pattern: "catalog_sync_runs"
---

<objective>
Create the auditable RAWG synchronization and localization draft workflow.

Purpose: Phase 02.1 needs catalog updates to stop being ad hoc, protect QUEUE/2 curation from RAWG overwrites, and record a documented provider/draft decision without requiring runtime translation.
Output: curated allowlist, dry-run/apply sync command, curation-preserving repository methods, ops sync audit tables, optional draft-provider port, provider decision/checklist code and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-CONTEXT.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-RESEARCH.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catalog sync ops schema</name>
  <files>packages/db/src/schema/ops.ts, packages/db/src/schema/index.ts, packages/db/src/migrations/0007_catalog_sync_ops.sql, packages/db/tests/catalog-sync-ops.test.ts</files>
  <read_first>.planning/SECURITY.md, packages/db/src/schema/ops.ts, packages/db/src/migrations/0001_foundation.sql</read_first>
  <action>Add non-duo-scoped operational tables for catalog sync: `ops.catalog_sync_runs` and `ops.catalog_sync_run_items`. Include source, mode, dry-run flag, run status, counts, timestamps, requested-by metadata, per-item status, change JSON and redacted error fields. Grant app runtime no mutation privileges. Grant worker INSERT/UPDATE needed for sync. Document why these records are operational catalog data rather than duo-scoped audit events.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and `pnpm --filter @queue/db test:integration -- catalog-sync-ops` when `TEST_DATABASE_URL` is configured; otherwise preserve explicit skip output.</verify>
  <acceptance_criteria>
    - Sync run/item tables exist under `ops` with comments, constraints and useful indexes.
    - Runtime role cannot insert/update sync audit records.
    - Worker role can record run/item lifecycle.
    - Error fields are suitable for redacted messages, not raw secret-bearing payloads.
  </acceptance_criteria>
  <done>Catalog sync has persistent operational audit storage.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve QUEUE/2 curation during RAWG upserts</name>
  <files>apps/web/src/modules/catalog/application/ports.ts, apps/web/src/modules/catalog/infrastructure/catalog-repository.ts, apps/web/src/modules/catalog/infrastructure/rawg-client.ts, apps/web/tests/catalog-sync.test.ts</files>
  <read_first>apps/web/src/modules/catalog/infrastructure/catalog-repository.ts, apps/web/src/modules/catalog/infrastructure/rawg-client.ts, .planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-RESEARCH.md</read_first>
  <action>Split RAWG-owned fact updates from QUEUE/2-owned curation updates. Adjust repository upsert methods so RAWG sync updates name, slug, description storage, release, cover, RAWG URL, source timestamps, platforms and genres, while preserving existing coop confirmation, player range, confirmation source/date, main-flow eligibility, published localizations, curated time estimates and curated availability unless a curated allowlist entry explicitly updates those fields. Add tests that would fail if a normalized RAWG payload resets curation to false/null.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-sync` and `pnpm --filter @queue/web typecheck`.</verify>
  <acceptance_criteria>
    - A synced RAWG payload cannot erase existing main-flow eligibility.
    - Existing published localizations are untouched by RAWG sync.
    - Curated time/availability records are not overwritten by absent RAWG data.
    - Explicit allowlist curation updates remain possible.
  </acceptance_criteria>
  <done>Sync can refresh RAWG facts without damaging product curation.</done>
</task>

<task type="auto">
  <name>Task 3: Implement curated allowlist sync command</name>
  <files>.env.example, package.json, scripts/catalog-sync.ts, apps/web/src/modules/catalog/infrastructure/catalog-sync.ts, apps/web/src/modules/catalog/infrastructure/catalog-sync-allowlist.ts, apps/web/tests/catalog-sync.test.ts</files>
  <read_first>apps/web/src/modules/catalog/infrastructure/rawg-client.ts, apps/web/src/modules/catalog/infrastructure/catalog-repository.ts, scripts/check-secrets.mjs</read_first>
  <action>Create a root script command such as `pnpm catalog:sync -- --dry-run` that calls a versioned TypeScript entrypoint through Node 24 type stripping, for example `node --conditions react-server --experimental-strip-types scripts/catalog-sync.ts`. The `react-server` condition is required because shared catalog infrastructure imports the `server-only` marker package. Import the shared sync worker by explicit `.ts` path so the CLI and tests exercise the same orchestration code without adding a new TS runner dependency. The sync command must load a curated allowlist, fetch each RAWG id/slug through the server-only client, normalize facts, compute structured changes, support dry-run without writes, apply transactional upserts in apply mode, and record run/item audit rows. Keep output structured enough for CI/log review and avoid printing secrets.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-sync`, `pnpm typecheck` and `pnpm check:secrets`.</verify>
  <acceptance_criteria>
    - Dry-run prints planned create/update/skip/fail counts and item outcomes without mutating the database.
    - Apply mode writes RAWG facts and sync audit rows transactionally.
    - Allowlist entries are explicit two-player coop candidates/confirmed games; broad RAWG search does not auto-enroll catalog items.
    - Missing `RAWG_API_KEY` fails as a server/CLI configuration error.
  </acceptance_criteria>
  <done>Catalog sync is repeatable, logged and safe to run manually before future cron.</done>
</task>

<task type="auto">
  <name>Task 4: Add provider decision, draft port and checklist enforcement</name>
  <files>.env.example, scripts/check-secrets.mjs, apps/web/src/modules/catalog/infrastructure/localization-draft-provider.ts, apps/web/src/modules/catalog/infrastructure/localization-review-checklist.ts, apps/web/tests/catalog-sync.test.ts</files>
  <read_first>.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-RESEARCH.md, scripts/check-secrets.mjs, .env.example</read_first>
  <action>Document the execution decision in code comments or module-level docs: published v1 copy is QUEUE/2 curated, DeepL is the preferred future translation-draft provider, and OpenAI may be an optional review/rewriting assistant. Add a `LocalizationDraftProvider` interface but keep provider integration optional unless credentials are present. Add a quality checklist helper that must be satisfied before any draft can transition to publish. Extend secret scanning to include future provider env names such as `DEEPL_API_KEY`, `OPENAI_API_KEY`, `GOOGLE_TRANSLATE_API_KEY` and `AZURE_TRANSLATOR_KEY`.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-sync`, `pnpm check:secrets` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Provider-assisted text is represented only as draft/review input.
    - No provider key appears in client source or built client bundles.
    - Publication helper requires checklist fields matching D-05.
    - The phase can ship without configuring any translation provider.
  </acceptance_criteria>
  <done>Provider choice is documented and future draft generation has a safe boundary.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- catalog-sync` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm check:secrets` passes with future provider env names covered.
- [ ] Catalog sync ops integration tests run against `TEST_DATABASE_URL` or report explicit skip output.
- [ ] Dry-run command is exercised locally with mocked or configured RAWG access, and apply mode behavior is covered by tests.
</verification>

<success_criteria>
- Catalog sync is a versioned, repeatable server/worker path with dry-run support.
- RAWG updates cannot overwrite QUEUE/2 curation or published localization.
- Sync runs and per-item failures are auditable in `ops`.
- Provider-assisted localization remains draft-only and optional.
- Secrets for RAWG and future translation providers remain server-only.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-02-SUMMARY.md`.
</output>
