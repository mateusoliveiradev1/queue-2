---
phase: 02.1-localizacao-e-qualidade-do-catalogo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "packages/db/src/schema/catalog.ts"
  - "packages/db/src/schema/index.ts"
  - "packages/db/src/migrations/0006_catalog_localizations.sql"
  - "packages/db/tests/catalog-schema.test.ts"
  - "packages/db/tests/catalog-localization.test.ts"
  - "apps/web/src/modules/catalog/domain/catalog-policy.ts"
  - "apps/web/src/modules/catalog/application/ports.ts"
  - "apps/web/src/modules/catalog/application/get-catalog-game.ts"
  - "apps/web/src/modules/catalog/application/search-catalog.ts"
  - "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
  - "apps/web/src/modules/catalog/infrastructure/localization-seeds.ts"
  - "apps/web/src/modules/catalog/presentation/localized-descriptions.ts"
  - "apps/web/src/modules/catalog/presentation/view-models.ts"
  - "apps/web/src/modules/catalog/index.ts"
  - "apps/web/tests/catalog-domain.test.ts"
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, SAFE-05]
user_setup: []
must_haves:
  truths:
    - "CAT-01 and CAT-03: Catalog detail reads include persisted PT-BR localization data while preserving RAWG-backed cover, genres, release information and platforms."
    - "CAT-02: RAWG attribution remains an active source URL even when the displayed description is QUEUE/2-owned PT-BR copy."
    - "CAT-04 and CAT-05: Time estimate and availability contracts remain sourced optional facts with explicit missing states."
    - "SAFE-05: No localization path introduces provider secrets into browser bundles or Client Components."
    - "D-01: Published PT-BR descriptions come from reviewed QUEUE/2 curation or reviewed provider-assisted drafts, never unreviewed provider output."
    - "D-03: Runtime pages read cached/published localization data and never translate descriptions during page render."
    - "D-04 and D-13: If no reviewed PT-BR description exists, detail/card view models must not fall back to raw English RAWG description."
    - "D-05: Localization publication records quality checklist metadata: coop facts checked, spoilers avoided, facts not invented, natural PT-BR, controlled QUEUE/2 tone and reviewer/provenance metadata."
    - "D-06: QUEUE/2 curation controls product-facing description text while RAWG remains the external data/image source."
    - "D-07 and D-14: Only worker/admin technical paths can write drafts, review transitions or published localization rows; app runtime reads only."
    - "D-08: Description copy is informative PT-BR with restrained QUEUE/2 personality, not hype copy or a generic encyclopedia entry."
    - "D-09: Genre/platform label localization can use simple PT-BR mappings when necessary, but full label localization is not a blocker for this plan."
    - "D-10: Reviewed descriptions live in dedicated `catalog.game_localizations`, not `catalog.games`."
    - "D-11: Localization status supports at least `draft`, `review`, `published` and `rejected`."
    - "D-12: Localization history is versioned and product reads the newest valid published localization for `pt-BR`."
    - "D-28: Author/reviewer identifiers stay internally auditable and are not exposed to end users."
  artifacts:
    - path: "packages/db/src/migrations/0006_catalog_localizations.sql"
      provides: "Versioned localization table, constraints, indexes and role grants"
      contains: "catalog.game_localizations"
    - path: "packages/db/src/schema/catalog.ts"
      provides: "Drizzle schema for catalog localizations"
      contains: "gameLocalizations"
    - path: "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
      provides: "Catalog detail/search reads joined with newest published PT-BR localization"
      contains: "game_localizations"
    - path: "apps/web/src/modules/catalog/domain/catalog-policy.ts"
      provides: "Pure localization fallback and source decision policy"
      contains: "Descricao em portugues"
    - path: "apps/web/tests/catalog-domain.test.ts"
      provides: "No-English-fallback and published-localization tests"
      min_lines: 80
  key_links:
    - from: "apps/web/src/modules/catalog/index.ts"
      to: "apps/web/src/modules/catalog/application"
      via: "public catalog read APIs"
      pattern: "getCatalogGameDetail|searchCatalogGames"
    - from: "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
      to: "packages/db/src/schema/catalog.ts"
      via: "published localization join"
      pattern: "game_localizations"
    - from: "packages/db/src/migrations/0006_catalog_localizations.sql"
      to: "queue2_app_runtime"
      via: "SELECT-only runtime grants"
      pattern: "GRANT SELECT"
---

<objective>
Create the persistent PT-BR localization contract for catalog reads.

Purpose: Phase 02.1 needs reviewed, versioned Portuguese descriptions that are served from Postgres and never generated at page runtime.
Output: `catalog.game_localizations`, Drizzle schema, least-privilege grants, published-localization repository reads, migrated seed data for current curated descriptions, no-English-fallback policies and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-CONTEXT.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-RESEARCH.md
@.planning/phases/02-catalogo-e-biblioteca/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned catalog localization schema</name>
  <files>packages/db/src/schema/catalog.ts, packages/db/src/schema/index.ts, packages/db/src/migrations/0006_catalog_localizations.sql, packages/db/tests/catalog-schema.test.ts, packages/db/tests/catalog-localization.test.ts</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/SECURITY.md, packages/db/src/schema/catalog.ts, packages/db/src/migrations/0004_catalog_source.sql, packages/db/tests/catalog-schema.test.ts</read_first>
  <action>Add `catalog.game_localizations` as catalog-owned public metadata. Include `game_id`, `locale`, `version`, `status`, `description`, `source`, optional `source_url`, raw-source hash/provenance, author/reviewer metadata, review notes, `quality_check` JSON, and timestamps for create/update/review/publish. Add constraints for status, non-empty description when published, unique `(game_id, locale, version)`, and an index that makes newest published `pt-BR` reads cheap. Grant app runtime SELECT only; grant worker INSERT/UPDATE. Keep the table non-duo-scoped and document why RLS is not required for public catalog localizations.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and `pnpm --filter @queue/db test:integration -- catalog` when `TEST_DATABASE_URL` is configured; otherwise preserve explicit skip output.</verify>
  <acceptance_criteria>
    - `catalog.game_localizations` exists with status/version/provenance/checklist fields.
    - Runtime role can SELECT but cannot INSERT or UPDATE localization rows.
    - Worker role can write localization workflow rows.
    - Database tests verify status constraints and newest-published lookup assumptions.
  </acceptance_criteria>
  <done>Localization storage exists with audit-ready metadata and least-privilege access.</done>
</task>

<task type="auto">
  <name>Task 2: Extend catalog records and repository reads with published localization</name>
  <files>apps/web/src/modules/catalog/application/ports.ts, apps/web/src/modules/catalog/application/get-catalog-game.ts, apps/web/src/modules/catalog/application/search-catalog.ts, apps/web/src/modules/catalog/infrastructure/catalog-repository.ts, apps/web/src/modules/catalog/index.ts</files>
  <read_first>apps/web/src/modules/catalog/application/ports.ts, apps/web/src/modules/catalog/infrastructure/catalog-repository.ts, apps/web/src/modules/catalog/index.ts</read_first>
  <action>Add `CatalogLocalizationRecord` and include `localization` on catalog detail/card records. Update search/detail queries to left join or hydrate the newest valid `published` `pt-BR` localization. Ensure repository reads never expose draft/review/rejected rows to the web runtime. Keep application use cases independent from SQL details and expose types through the catalog public entrypoint only.</action>
  <verify>Run `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Search and detail records can carry a published PT-BR localization.
    - Draft/review/rejected localizations are invisible to normal catalog reads.
    - Routes continue importing only from `apps/web/src/modules/catalog/index.ts`.
    - Existing RAWG facts remain available for attribution and source/freshness.
  </acceptance_criteria>
  <done>Catalog public APIs can serve persisted PT-BR localizations.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded presentation descriptions with seeded persisted copy</name>
  <files>apps/web/src/modules/catalog/infrastructure/localization-seeds.ts, apps/web/src/modules/catalog/presentation/localized-descriptions.ts, apps/web/src/modules/catalog/presentation/view-models.ts, apps/web/tests/catalog-domain.test.ts</files>
  <read_first>apps/web/src/modules/catalog/presentation/localized-descriptions.ts, apps/web/src/modules/catalog/presentation/view-models.ts, .planning/quick/260603-r9i-polir-catalogo-da-fase-2-corrigir-cards-/260603-r9i-SUMMARY.md</read_first>
  <action>Move the current curated PT-BR descriptions out of presentation lookup and into an infrastructure seed/import representation that creates `published` localization rows with QUEUE/2 provenance and quality checklist metadata. Remove `getPortugueseCatalogDescription` from view-model logic. Add a pure fallback policy so missing published localization returns honest PT-BR unavailable copy and source state instead of RAWG English text.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-domain` and `pnpm --filter @queue/web typecheck`.</verify>
  <acceptance_criteria>
    - View models prefer `game.localization.description` when published.
    - Missing localization renders an honest PT-BR unavailable message.
    - `game.description` from RAWG is not used as product-facing fallback.
    - Existing curated strings are preserved as seed content, not presentation code.
  </acceptance_criteria>
  <done>Product-facing descriptions are persisted or honestly unavailable.</done>
</task>

<task type="auto">
  <name>Task 4: Add localization domain policy tests</name>
  <files>apps/web/src/modules/catalog/domain/catalog-policy.ts, apps/web/src/modules/catalog/presentation/view-models.ts, apps/web/tests/catalog-domain.test.ts</files>
  <read_first>apps/web/tests/catalog-domain.test.ts, apps/web/src/modules/catalog/domain/catalog-policy.ts</read_first>
  <action>Add pure tests for published localization, draft invisibility via record shape, no raw English fallback, `Descricao revisada: QUEUE/2`, missing localization copy, and unchanged time/availability missing-source labels. Ensure tests do not import React, Next.js, Drizzle or secret-bearing adapters.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-domain`.</verify>
  <acceptance_criteria>
    - Tests fail if RAWG English description becomes fallback again.
    - Tests fail if published localization source label changes away from the locked user-facing label without deliberate update.
    - Tests keep CAT-04/CAT-05 missing-source behavior intact.
  </acceptance_criteria>
  <done>Localization read behavior is locked by fast domain tests.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- catalog-domain` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm --filter @queue/db test:integration -- catalog` runs against `TEST_DATABASE_URL` or reports explicit skip output.
- [ ] `pnpm check:secrets` passes after localization files are added.
</verification>

<success_criteria>
- PT-BR catalog descriptions are read from versioned persisted localization rows.
- Runtime pages never translate descriptions and never fall back to raw English RAWG text.
- The app runtime cannot publish or mutate localizations.
- Current curated descriptions are preserved as auditable seed data.
- RAWG attribution, estimated time and availability contracts remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-01-SUMMARY.md`.
</output>
