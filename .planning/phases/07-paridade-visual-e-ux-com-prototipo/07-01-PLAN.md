---
phase: "07-paridade-visual-e-ux-com-prototipo"
plan: "01"
type: "execute"
wave: 1
depends_on:
  - "07-00"
files_modified:
  - "apps/web/src/components/app-shell.tsx"
  - "apps/web/src/app/globals.css"
  - "apps/web/src/app/app/hall/page.tsx"
  - "packages/ui/src/tokens.css"
  - "apps/web/tests/brand-ui.test.tsx"
  - "apps/web/tests/accessibility.spec.ts"
  - "apps/web/tests/phase-7-visual.spec.ts"
autonomous: true
requirements:
  - "BRND-02"
  - "BRND-04"
  - "BRND-05"
  - "BRND-06"
  - "BRND-07"
  - "BRND-08"
  - "BRND-09"
  - "BRND-10"
  - "BRND-11"
  - "BRND-12"
  - "BRND-13"
  - "META-02"
  - "SAFE-05"
must_haves:
  truths:
    - "D-01: AppShell uses a global top navbar on internal routes instead of a sidebar-first shell."
    - "D-02: desktop primary nav exposes exactly Home, Biblioteca, Descobrir, Roleta, Desafios, Hall and Dupla."
    - "D-03: Perfil and Sair live on the right side; Catalogo and Conquistas remain contextual, not primary nav items."
    - "D-04: mobile uses a sticky top area plus horizontally scrollable route rail, not bottom nav or drawer-only navigation."
    - "D-05/D-06/D-07/D-08: active route uses lime slash/pointer plus thin underline, dry sticky top bar, and route controls stay separate."
    - "D-09: /app/hall exists as an honest prepared state, not full Hall behavior."
    - "D-15/D-24/D-25/D-26/D-27: shared visual primitives are dry, non-generic, accessible and verifiable."
    - "D-20: QueueLoading remains the /2 stroke loader."
  artifacts:
    - path: "apps/web/src/components/app-shell.tsx"
      provides: "horizontal authenticated shell"
    - path: "apps/web/src/app/globals.css"
      provides: "shared Phase 7 layout, panel, nav, empty-state and responsive classes"
    - path: "apps/web/src/app/app/hall/page.tsx"
      provides: "prepared Hall route target for the new nav"
  key_links:
    - from: "apps/web/src/components/app-shell.tsx"
      to: "apps/web/src/app/app/hall/page.tsx"
      via: "Hall primary nav route"
      pattern: "/app/hall"
---

<objective>
Replace the authenticated shell and shared visual primitives with the Phase 7 editorial/brutalist system.

Purpose: every authenticated route depends on `AppShell` and global visual primitives. This plan creates the new top navigation contract, shared dry layout language and minimal Hall route before route-specific redesigns begin.
Output: horizontal app shell, mobile route rail, shared CSS primitives and prepared `/app/hall`.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-CONTEXT.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-VISUAL-SPEC.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-RESEARCH.md
@apps/web/src/components/app-shell.tsx
@apps/web/src/app/globals.css
@packages/ui/src/tokens.css
@packages/ui/src/brand/wordmark.tsx
@packages/ui/src/brand/mark.tsx
@packages/ui/src/brand/loading.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sidebar and bottom nav with the horizontal shell</name>
  <files>apps/web/src/components/app-shell.tsx, apps/web/src/app/globals.css, apps/web/tests/brand-ui.test.tsx, apps/web/tests/accessibility.spec.ts, apps/web/tests/phase-7-visual.spec.ts</files>
  <read_first>
    - `apps/web/src/components/app-shell.tsx`
    - `apps/web/src/app/globals.css`
    - `packages/ui/src/brand/wordmark.tsx`
    - `packages/ui/src/brand/mark.tsx`
    - `apps/web/tests/brand-ui.test.tsx`
  </read_first>
  <action>
    Rework `AppShell` so it renders a sticky top shell with the `QueueWordmark` or compact mark as first signal, seven primary links (`Home`, `Biblioteca`, `Descobrir`, `Roleta`, `Desafios`, `Hall`, `Dupla`), and right-side `Perfil` and sign-out affordance. Keep existing `currentPage` support for contextual routes such as `catalogo`, `conquistas`, `perfil` and game detail pages without making them primary links. Add mobile CSS for a sticky header and horizontal route rail that exposes all seven primary routes without overlap. Remove the old sidebar/bottom-nav primary composition from rendered output while keeping route content children unchanged.
  </action>
  <acceptance_criteria>
    - `rg -n "Home|Biblioteca|Descobrir|Roleta|Desafios|Hall|Dupla|Perfil|Sair" apps/web/src/components/app-shell.tsx` returns all labels.
    - `rg -n "app-sidebar|app-bottom-nav" apps/web/src/components/app-shell.tsx` returns no rendered primary shell.
    - `rg -n "aria-current|aria-label=.Navegacao principal|/app/hall|/app/perfil" apps/web/src/components/app-shell.tsx` confirms accessible nav targets.
    - Browser checks prove mobile route rail exposes all seven primary routes at 390px width.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- brand-ui</automated>
    <automated>pnpm --filter @queue/web exec playwright test tests/phase-7-visual.spec.ts --grep "authenticated shell"</automated>
  </verify>
  <done>The old dashboard shell is gone and all protected routes still render through `AppShell`.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared Phase 7 visual primitives</name>
  <files>apps/web/src/app/globals.css, packages/ui/src/tokens.css, apps/web/tests/brand-ui.test.tsx</files>
  <read_first>
    - `apps/web/src/app/globals.css`
    - `packages/ui/src/tokens.css`
    - `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-VISUAL-SPEC.md`
  </read_first>
  <action>
    Add or refine shared classes for centered route canvases, dry panels, low tiles, uppercase/mono labels, large but bounded headings, disciplined lime slash/pointer accents, compact action rows, empty-state panels and responsive spacing. Preserve existing focus rings, touch-target-friendly button/input sizing, reduced-motion reset, grain treatment, `/2` loading visuals and roulette-scoped scanlines. Avoid broad resets that change domain behavior or move business logic into CSS/route components.
  </action>
  <acceptance_criteria>
    - `rg -n "queue2-shell|route-rail|dry-panel|empty-state|lime|prefers-reduced-motion|focus-visible|scanline" apps/web/src/app/globals.css packages/ui/src/tokens.css` returns shared primitives and preserved gates.
    - `rg -n "border-radius: 1[2-9]px|border-radius: 2[0-9]px|gradient orb|bokeh" apps/web/src/app/globals.css packages/ui/src/tokens.css` returns no new generic SaaS/orb styling.
    - Source tests confirm `QueueLoading`, `QueueWordmark`, `QueueMark`, `RoulettePointer` and focus tokens remain reachable.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- brand-ui</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
  </verify>
  <done>Route-specific plans can compose a shared QUEUE/2 visual language without one-off global hacks.</done>
</task>

<task type="auto">
  <name>Task 3: Create the prepared Hall route</name>
  <files>apps/web/src/app/app/hall/page.tsx, apps/web/tests/phase-7-visual.spec.ts, apps/web/tests/accessibility.spec.ts</files>
  <read_first>
    - `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-CONTEXT.md`
    - `apps/web/src/app/app/desafios/page.tsx`
    - `apps/web/src/app/app/dupla/page.tsx`
  </read_first>
  <action>
    Add `/app/hall` as a protected route using `AppShell currentPage=\"hall\"`. Render only the prepared empty state required by Phase 7: short honest PT-BR copy such as `ESTANTE VAZIA / POR ENQUANTO`, one restrained explanation, and no reviews, replay timeline, completed-game shelf or Phase 8 behavior. Keep the page data-free or use only already-authorized session/duo context required by the protected layout.
  </action>
  <acceptance_criteria>
    - `rg -n "ESTANTE VAZIA|POR ENQUANTO|currentPage=.hall|AppShell" apps/web/src/app/app/hall/page.tsx` returns the prepared route.
    - `rg -n "review|resenha|replay|timeline|completed" apps/web/src/app/app/hall/page.tsx` returns no Phase 8 behavior unless only in out-of-scope copy.
    - Playwright can navigate from the primary nav to `/app/hall` without 404.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm --filter @queue/web exec playwright test tests/phase-7-visual.spec.ts --grep "Hall"</automated>
  </verify>
  <done>The new primary nav has a truthful Hall destination without expanding Phase 7 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppShell -> protected route data | Navigation renders links and children; it must not fetch or expose cross-duo data. |
| Hall prepared route -> Phase 8 scope | Empty state must not introduce reviews, shelf replay or new data contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | shell links | mitigate | Use fixed route links only; do not pass duo ids through client URLs. |
| T-07-01-02 | Elevation of Privilege | protected pages | mitigate | Keep existing route/session guards and do not move authorization into `AppShell`. |
| T-07-01-03 | Tampering | sign-out/profile actions | mitigate | Preserve existing server/auth action behavior while changing placement only. |
</threat_model>

<verification>
Run focused source tests, typecheck, architecture check and the Phase 7 shell/browser grep. Confirm no schema, migration, auth rule, RLS or domain module files changed.
</verification>

<success_criteria>
- Internal navigation reads as QUEUE/2 top navigation, not a sidebar SaaS dashboard.
- Desktop and mobile expose the same seven primary routes.
- Perfil/Sair and contextual Catalogo/Conquistas access remain available.
- The prepared Hall route exists without Phase 8 behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-01-SUMMARY.md`.
</output>
