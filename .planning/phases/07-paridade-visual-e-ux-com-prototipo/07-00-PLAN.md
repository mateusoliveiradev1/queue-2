---
phase: "07-paridade-visual-e-ux-com-prototipo"
plan: "00"
type: "execute"
wave: 0
depends_on: []
files_modified:
  - "apps/web/tests/phase-7-visual.spec.ts"
  - "apps/web/tests/accessibility.spec.ts"
  - "apps/web/tests/brand-ui.test.tsx"
  - "apps/web/tests/discovery-ui.test.tsx"
  - "apps/web/tests/roulette-ui.test.tsx"
  - "scripts/phase-7-gate.mjs"
  - "package.json"
autonomous: true
requirements:
  - "BRND-02"
  - "BRND-04"
  - "BRND-05"
  - "BRND-06"
  - "BRND-07"
  - "BRND-08"
  - "BRND-09"
  - "BRND-10"
  - "BRND-11"
  - "BRND-12"
  - "BRND-13"
  - "META-01"
  - "META-02"
  - "SAFE-04"
  - "SAFE-05"
must_haves:
  truths:
    - "D-27: Phase 7 starts with executable visual, accessibility, reduced-motion, overflow, overlap and touch-target coverage."
    - "D-01/D-04/D-05/D-06: tests are updated away from sidebar/bottom-nav assumptions and toward the new horizontal shell contract."
    - "D-10/D-13/D-14: public landing, auth and pairing surfaces have browser coverage before implementation begins."
    - "D-20: the /2 SVG loading system remains covered and is not replaced by a generic spinner."
    - "SAFE-04/SAFE-05: the gate preserves existing server-side behavior checks and secret scanning."
  artifacts:
    - path: "apps/web/tests/phase-7-visual.spec.ts"
      provides: "Phase 7 browser screenshot, responsive, axe, reduced-motion and geometry scaffold"
    - path: "scripts/phase-7-gate.mjs"
      provides: "root Phase 7 visual evidence gate"
    - path: "package.json"
      provides: "phase:7:gate command"
  key_links:
    - from: "apps/web/tests/phase-7-visual.spec.ts"
      to: ".planning/phases/07-paridade-visual-e-ux-com-prototipo/07-VISUAL-SPEC.md"
      via: "route target coverage"
      pattern: "landing|public auth|authenticated shell|no overlap|touch target"
---

<objective>
Create the Phase 7 validation and evidence scaffold before visual implementation.

Purpose: Phase 7 is mostly UI composition, so regressions are visual and responsive. The executor needs RED or BLOCKED tests that name the expected surfaces before changing shared CSS and route layout.
Output: Playwright visual spec, updated source/component guards, root phase gate and package script.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-CONTEXT.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-VISUAL-SPEC.md
@.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-RESEARCH.md
@apps/web/tests/accessibility.spec.ts
@apps/web/tests/brand-ui.test.tsx
@apps/web/tests/discovery-ui.test.tsx
@apps/web/tests/roulette-ui.test.tsx
@scripts/phase-6-gate.mjs
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 7 visual browser scaffold</name>
  <files>apps/web/tests/phase-7-visual.spec.ts</files>
  <read_first>
    - `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-VISUAL-SPEC.md`
    - `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-RESEARCH.md`
    - `apps/web/tests/phase-6-e2e.spec.ts`
    - `apps/web/tests/accessibility.spec.ts`
  </read_first>
  <action>
    Create `apps/web/tests/phase-7-visual.spec.ts` with public route checks for `/`, `/login`, `/cadastro`, `/recuperar-senha`, `/verificar-email` and `/parear`, plus authenticated route checks for `/app`, `/app/biblioteca`, `/app/descobrir`, `/app/roleta`, `/app/desafios`, `/app/hall`, `/app/dupla` and `/app/perfil`. Use existing E2E env-loading conventions without printing secrets. The spec must capture desktop and mobile screenshots into a deterministic Phase 7 evidence directory, run axe on public and main authenticated surfaces, emulate reduced motion for Descobrir and Roleta, assert no visible horizontal overflow, assert no meaningful overlap among visible controls, and assert interactive controls meet the project's touch-target baseline.
  </action>
  <acceptance_criteria>
    - `rg -n "phase-7|landing|public auth|authenticated shell|reducedMotion|axe|screenshot|no horizontal overflow|touch target|no overlap" apps/web/tests/phase-7-visual.spec.ts` returns matches.
    - `rg -n "/app/hall|/app/dupla|/app/perfil|/app/roleta|/parear" apps/web/tests/phase-7-visual.spec.ts` returns all required routes.
    - Missing authenticated fixtures produce named `BLOCKED setup` output with variable names only, never secret values.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web exec playwright test tests/phase-7-visual.spec.ts</automated>
  </verify>
  <done>Browser scaffold is RED or BLOCKED until later plans implement the new shell, routes and visual states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update source and accessibility guards for the new visual contract</name>
  <files>apps/web/tests/accessibility.spec.ts, apps/web/tests/brand-ui.test.tsx, apps/web/tests/discovery-ui.test.tsx, apps/web/tests/roulette-ui.test.tsx</files>
  <read_first>
    - `apps/web/src/components/app-shell.tsx`
    - `apps/web/src/app/globals.css`
    - `apps/web/tests/accessibility.spec.ts`
    - `apps/web/tests/brand-ui.test.tsx`
    - `apps/web/tests/discovery-ui.test.tsx`
    - `apps/web/tests/roulette-ui.test.tsx`
  </read_first>
  <action>
    Replace old assertions that require `.app-sidebar`, `.app-bottom-nav`, nine-item mobile nav, or `Conquistas` as a primary nav item. Add assertions for a seven-route primary top nav (`Home`, `Biblioteca`, `Descobrir`, `Roleta`, `Desafios`, `Hall`, `Dupla`), right-side `Perfil` and `Sair`, contextual `Catalogo` and `Conquistas` access, lime slash/pointer active state, compact public auth tabs, `/2` loading preservation, reduced-motion rules and scanline scoping only inside roulette selectors.
  </action>
  <acceptance_criteria>
    - `rg -n "app-sidebar|app-bottom-nav|nine-item" apps/web/tests/accessibility.spec.ts apps/web/tests/brand-ui.test.tsx apps/web/tests/discovery-ui.test.tsx apps/web/tests/roulette-ui.test.tsx` returns no stale required assertions.
    - `rg -n "Home|Biblioteca|Descobrir|Roleta|Desafios|Hall|Dupla|Perfil|Sair|Catalogo|Conquistas" apps/web/tests/accessibility.spec.ts apps/web/tests/brand-ui.test.tsx apps/web/tests/discovery-ui.test.tsx apps/web/tests/roulette-ui.test.tsx` returns updated navigation coverage.
    - `rg -n "QueueLoading|prefers-reduced-motion|scanline|roulette" apps/web/tests/brand-ui.test.tsx apps/web/tests/roulette-ui.test.tsx` proves loading and scanline scope remain tested.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- brand-ui discovery-ui roulette-ui</automated>
    <automated>pnpm --filter @queue/web exec playwright test tests/accessibility.spec.ts</automated>
  </verify>
  <done>Existing tests now describe the Phase 7 contract instead of the old dashboard shell.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Phase 7 gate command</name>
  <files>scripts/phase-7-gate.mjs, package.json</files>
  <read_first>
    - `scripts/phase-6-gate.mjs`
    - `package.json`
    - `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-RESEARCH.md`
  </read_first>
  <action>
    Create `scripts/phase-7-gate.mjs` and add `"phase:7:gate": "node scripts/phase-7-gate.mjs"` to the root scripts. The gate must orchestrate `pnpm check:secrets`, `pnpm check:architecture`, focused web tests, `pnpm --filter @queue/web exec playwright test tests/phase-7-visual.spec.ts`, and a Phase 6 preservation command where feasible. Missing E2E credentials must be reported as `BLOCKED - missing external evidence` with variable names only. The gate must write or point to browser screenshots and a concise evidence summary without embedding secrets.
  </action>
  <acceptance_criteria>
    - `rg -n "phase:7:gate" package.json scripts/phase-7-gate.mjs` returns the script and command.
    - `rg -n "check:secrets|check:architecture|phase-7-visual|BLOCKED - missing external evidence|phase-6" scripts/phase-7-gate.mjs` returns all gate legs.
    - `node scripts/phase-7-gate.mjs` exits non-zero only for real local failures or named missing external evidence, never for silent skips.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/phase-7-gate.mjs</automated>
  </verify>
  <done>Later plans can use one root command to collect Phase 7 visual evidence honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser evidence -> local secrets | Screenshots and logs must not reveal `.env.local` values. |
| validation scaffold -> implementation | RED tests drive visual implementation without granting client authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | E2E setup | mitigate | Report missing fixture variable names only; never print values. |
| T-07-00-02 | Tampering | visual tests | mitigate | Tests assert existing server routes and public module behavior remain reachable. |
| T-07-00-03 | Repudiation | visual evidence | mitigate | Gate records commands, screenshots and named blockers in deterministic output. |
</threat_model>

<verification>
Run focused source tests, Playwright accessibility/visual specs and the new root gate. RED is acceptable only where the later Phase 7 implementation is missing; missing external fixtures must be named BLOCKED setup.
</verification>

<success_criteria>
- Phase 7 has a dedicated visual Playwright spec covering desktop, mobile, reduced motion, axe, overflow, overlap and touch targets.
- Existing tests no longer require the old sidebar/bottom-nav shell.
- `pnpm phase:7:gate` exists and preserves architecture, secrets and prior phase behavior checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-paridade-visual-e-ux-com-prototipo/07-00-SUMMARY.md`.
</output>
