---
phase: 02-catalogo-e-biblioteca
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - "packages/db/src/schema/app.ts"
  - "packages/db/src/migrations/0005_library_duo_state.sql"
  - "packages/db/src/rls/policies.sql"
  - "packages/db/src/roles.sql"
  - "packages/db/tests/library-rls.test.ts"
  - "packages/db/tests/library-concurrency.test.ts"
  - "apps/web/src/modules/library/domain/platforms.ts"
  - "apps/web/src/modules/library/domain/library-policy.ts"
  - "apps/web/src/modules/library/domain/match-score.ts"
  - "apps/web/src/modules/library/application/ports.ts"
  - "apps/web/src/modules/library/application/get-library-overview.ts"
  - "apps/web/src/modules/library/application/update-member-platforms.ts"
  - "apps/web/src/modules/library/application/add-game-to-wishlist.ts"
  - "apps/web/src/modules/library/application/move-library-game.ts"
  - "apps/web/src/modules/library/application/get-library-game-detail.ts"
  - "apps/web/src/modules/library/infrastructure/library-repository.ts"
  - "apps/web/src/modules/library/presentation/view-models.ts"
  - "apps/web/src/modules/library/index.ts"
  - "apps/web/tests/library-domain.test.ts"
autonomous: true
requirements: [CAT-06, CAT-07, LIB-01, LIB-02, LIB-03, LIB-04, LIB-05]
user_setup: []
must_haves:
  truths:
    - "CAT-06 and D-13: Each member can record simple playable platforms such as PC, PlayStation, Xbox, Switch and Steam Deck."
    - "CAT-07 and D-14: The duo can see the intersection of both members' platform choices."
    - "D-15: A game with no common platform is blocked from the main automatic recommendation flow but can still be added manually to Wishlist."
    - "LIB-01 and D-18: Any member can add a catalog game to the shared duo Wishlist."
    - "LIB-02, D-16 and D-17: Wishlist, Jogando and Pausado are usable in Phase 2; Zerado and Dropado are visible future states blocked until Phase 4 double confirmation."
    - "LIB-03 and D-21: Library reads support grouped/filtered status views for responsive mobile tabs and denser desktop layouts."
    - "D-19: Moving to Jogando enforces the future limit of at most three games in Jogando."
    - "D-20: Phase 2 does not implement Principal/secondary drag-and-drop ordering."
    - "LIB-04, D-22, D-23 and D-24: Match score is an explainable practical compatibility label/factor set, not fake numeric precision."
    - "LIB-05 and D-11: Library game detail exposes status, platforms, compatibility and a future-ready duo journey summary without pretending Phase 4 sessions exist."
    - "Security: Duo library tables are duo-scoped, forced-RLS protected and always accessed inside transaction-local user identity."
  artifacts:
    - path: "packages/db/src/migrations/0005_library_duo_state.sql"
      provides: "Duo-scoped platform/library schema, RLS, constraints and indexes"
      contains: "app.duo_library_games"
    - path: "apps/web/src/modules/library/domain/match-score.ts"
      provides: "Pure compatibility score rules"
      min_lines: 60
    - path: "apps/web/src/modules/library/infrastructure/library-repository.ts"
      provides: "Server-only duo library persistence"
      contains: "withAppUserTransaction"
    - path: "packages/db/tests/library-concurrency.test.ts"
      provides: "Jogando limit concurrency coverage"
      min_lines: 40
  key_links:
    - from: "apps/web/src/modules/library/index.ts"
      to: "apps/web/src/modules/library/application"
      via: "public use-case exports"
      pattern: "getLibraryOverview|addGameToWishlist|moveLibraryGame"
    - from: "apps/web/src/modules/library/infrastructure/library-repository.ts"
      to: "packages/db/src/schema/app.ts"
      via: "duo-scoped SQL queries"
      pattern: "duo_library_games|member_platforms"
    - from: "apps/web/src/modules/library/application/get-library-game-detail.ts"
      to: "apps/web/src/modules/catalog/index.ts"
      via: "catalog public detail facts composed by application use case; domain receives plain inputs"
      pattern: "getCatalogGameDetail"
---

<objective>
Create the duo library and platform domain.

Purpose: Phase 2 needs shared library state, member platform choices, common-platform visibility and practical compatibility rules protected by application authorization and forced RLS.
Output: Duo-scoped database tables, RLS policies, library module public API, domain rules, repository transactions, concurrency tests and library view models.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/02-catalogo-e-biblioteca/02-CONTEXT.md
@.planning/phases/02-catalogo-e-biblioteca/02-RESEARCH.md
@.planning/phases/02-catalogo-e-biblioteca/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duo platform and library tables with RLS</name>
  <files>packages/db/src/schema/app.ts, packages/db/src/migrations/0005_library_duo_state.sql, packages/db/src/rls/policies.sql, packages/db/src/roles.sql, packages/db/tests/library-rls.test.ts, packages/db/tests/library-concurrency.test.ts</files>
  <read_first>.planning/SECURITY.md, packages/db/src/schema/app.ts, packages/db/src/rls/policies.sql, packages/db/src/roles.sql</read_first>
  <action>Add `app.member_platforms` and `app.duo_library_games` with primary keys, foreign keys, check constraints, unique constraints, status indexes, catalog FK references and ownership comments. Enable and force RLS. Policies must let a member read duo platform/library state for their duo, update only their own platform rows, and mutate only library rows for their duo. Add a transaction-safe guard for at most three `jogando` games, using a security-definer function or explicit locked repository transaction with a database concurrency test.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and `pnpm --filter @queue/db test:integration -- library` when `TEST_DATABASE_URL` is configured; otherwise preserve explicit skip output.</verify>
  <acceptance_criteria>
    - Cross-duo SELECT/INSERT/UPDATE attempts fail under runtime role and RLS.
    - One member cannot overwrite the other member's platform rows.
    - A fourth concurrent `jogando` move cannot commit.
    - Runtime grants follow least privilege and do not allow migration privileges.
  </acceptance_criteria>
  <done>Duo library data is constrained, indexed and isolated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement library domain rules and use cases</name>
  <files>apps/web/src/modules/library/domain/platforms.ts, apps/web/src/modules/library/domain/library-policy.ts, apps/web/src/modules/library/domain/match-score.ts, apps/web/src/modules/library/application/ports.ts, apps/web/src/modules/library/application/get-library-overview.ts, apps/web/src/modules/library/application/update-member-platforms.ts, apps/web/src/modules/library/application/add-game-to-wishlist.ts, apps/web/src/modules/library/application/move-library-game.ts, apps/web/src/modules/library/application/get-library-game-detail.ts, apps/web/src/modules/library/index.ts, apps/web/tests/library-domain.test.ts</files>
  <read_first>.planning/phases/02-catalogo-e-biblioteca/02-CONTEXT.md, apps/web/src/modules/duo/domain/duo-policy.ts</read_first>
  <action>Create pure rules for supported platform enum labels, common-platform calculation, valid Phase 2 status transitions, blocked Zerado/Dropado states, Jogando limit errors and match-score labels/factors. Add application use cases that accept `userId`, authorize through repository lookups, and return typed result states for UI/server actions. Export only public functions and types from `library/index.ts`.</action>
  <verify>Run `pnpm --filter @queue/web test -- library-domain` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Platform validation is pure and rejects unknown labels.
    - Match score uses labels/factors and never emits fake percentage precision.
    - Zerado/Dropado mutations return a future-confirmation result in Phase 2.
    - Domain files do not import React, Next.js, Drizzle, database clients or catalog internals.
  </acceptance_criteria>
  <done>Library behavior is available through tested use cases.</done>
</task>

<task type="auto">
  <name>Task 3: Implement library repository and detail read models</name>
  <files>apps/web/src/modules/library/infrastructure/library-repository.ts, apps/web/src/modules/library/presentation/view-models.ts, apps/web/src/modules/library/application/get-library-overview.ts, apps/web/src/modules/library/application/get-library-game-detail.ts</files>
  <read_first>packages/db/src/client.ts, apps/web/src/modules/duo/infrastructure/duo-repository.ts, apps/web/src/modules/catalog/index.ts</read_first>
  <action>Implement server-only persistence with `withAppUserTransaction`. Compose catalog public facts with duo library state to build overview and detail read models. Add audit/domain events for library additions/status moves if the existing `ops.domain_events` pattern is cheap to reuse. Ensure repository methods never accept client-supplied `duoId` as authoritative without deriving/authorizing it from the current user's membership.</action>
  <verify>Run `pnpm --filter @queue/web typecheck`, `pnpm --filter @queue/web test -- library-domain` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Repository queries set transaction-local identity through existing db helpers.
    - Library overview returns groups/counts by Wishlist, Jogando and Pausado plus locked Zerado/Dropado metadata.
    - Game detail returns catalog data, library status, common platforms, score label and score factors.
    - Cross-module catalog access uses catalog public exports or stable catalog IDs, not deep imports.
  </acceptance_criteria>
  <done>Server use cases can read and mutate the duo library safely.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- library-domain` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] Library RLS and concurrency tests run against `TEST_DATABASE_URL` or report explicit skips.
</verification>

<success_criteria>
- Members can store platforms and see common platforms.
- A duo can add games to Wishlist and move them between Wishlist, Jogando and Pausado.
- Zerado/Dropado remain blocked until Phase 4 confirmation.
- Match score is explainable, conservative and compatible with future Phase 3 preference signals.
- Duo library state cannot leak across duos.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalogo-e-biblioteca/02-02-SUMMARY.md`.
</output>
