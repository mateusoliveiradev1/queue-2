---
phase: 02-catalogo-e-biblioteca
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ".env.example"
  - "packages/db/src/schema/catalog.ts"
  - "packages/db/src/schema/index.ts"
  - "packages/db/src/migrations/0004_catalog_source.sql"
  - "packages/db/src/roles.sql"
  - "packages/db/tests/catalog-schema.test.ts"
  - "apps/web/src/modules/catalog/domain/catalog-policy.ts"
  - "apps/web/src/modules/catalog/application/ports.ts"
  - "apps/web/src/modules/catalog/application/get-catalog-game.ts"
  - "apps/web/src/modules/catalog/application/search-catalog.ts"
  - "apps/web/src/modules/catalog/infrastructure/catalog-repository.ts"
  - "apps/web/src/modules/catalog/infrastructure/rawg-client.ts"
  - "apps/web/src/modules/catalog/presentation/view-models.ts"
  - "apps/web/src/modules/catalog/index.ts"
  - "apps/web/tests/catalog-domain.test.ts"
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05]
user_setup:
  - service: rawg
    why: "RAWG is the sourced catalog provider for Phase 2 and the API key must remain server-side."
    env_vars:
      - name: RAWG_API_KEY
        source: "RAWG account API key"
must_haves:
  truths:
    - "CAT-01: The server can read a synchronized catalog of games sourced from RAWG without calling RAWG from Client Components."
    - "CAT-02 and D-06: Every catalog record exposes attribution text and an active RAWG source URL for pages that use RAWG data or images."
    - "CAT-03 and D-10: Game detail data includes cover, description, genres, release information and platforms."
    - "CAT-04, CAT-05 and D-08: Estimated time and availability are optional sourced facts; when absent the data contract reports that no reliable source exists."
    - "D-09: Game Pass or free availability is treated as game metadata with source and last verification date, not as a personal platform setting."
    - "D-03, D-04 and D-05: Main-flow eligibility is owned by QUEUE/2 through explicit confirmation fields for 2-player campaign/story coop, not inferred blindly from RAWG tags."
    - "D-07: Source and freshness are exposed as quiet metadata suitable for cards and detail surfaces."
    - "SAFE-05: RAWG_API_KEY is read only by server-only infrastructure and never included in browser bundles."
    - "Security: Catalog tables contain external non-duo facts only; duo/private product state is not stored in the catalog schema."
  artifacts:
    - path: "packages/db/src/schema/catalog.ts"
      provides: "Drizzle schema for sourced catalog tables"
      min_lines: 80
    - path: "packages/db/src/migrations/0004_catalog_source.sql"
      provides: "Catalog SQL schema, indexes, comments and role grants"
      contains: "catalog.games"
    - path: "apps/web/src/modules/catalog/infrastructure/rawg-client.ts"
      provides: "Server-only RAWG adapter"
      contains: "RAWG_API_KEY"
    - path: "apps/web/src/modules/catalog/index.ts"
      provides: "Catalog module public entrypoint"
      min_lines: 10
    - path: "apps/web/tests/catalog-domain.test.ts"
      provides: "Catalog eligibility and source/freshness tests"
      min_lines: 60
  key_links:
    - from: "apps/web/src/modules/catalog/index.ts"
      to: "apps/web/src/modules/catalog/application"
      via: "public use-case exports"
      pattern: "searchCatalogGames|getCatalogGameDetail"
    - from: "apps/web/src/modules/catalog/infrastructure/rawg-client.ts"
      to: "packages/db/src/schema/catalog.ts"
      via: "normalized synced catalog records"
      pattern: "rawgId|sourceUpdatedAt|syncedAt"
    - from: "packages/db/src/roles.sql"
      to: "catalog.games"
      via: "least-privilege grants"
      pattern: "GRANT SELECT"
---

<objective>
Create the sourced catalog foundation for Phase 2.

Purpose: QUEUE/2 needs a server-mediated RAWG catalog contract with visible source/freshness and an explicit coop eligibility layer before the duo library and UI can depend on it.
Output: Catalog schema, migration, role grants, RAWG adapter, catalog module public API, source/freshness view models and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/02-catalogo-e-biblioteca/02-CONTEXT.md
@.planning/phases/02-catalogo-e-biblioteca/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catalog schema and migration</name>
  <files>packages/db/src/schema/catalog.ts, packages/db/src/schema/index.ts, packages/db/src/migrations/0004_catalog_source.sql, packages/db/src/roles.sql, packages/db/tests/catalog-schema.test.ts</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/SECURITY.md, packages/db/src/schema/app.ts, packages/db/src/roles.sql</read_first>
  <action>Create the `catalog` Drizzle schema and SQL migration for sourced game facts. Include `catalog.games`, `catalog.game_platforms`, `catalog.game_genres`, `catalog.game_time_estimates` and `catalog.game_availability`. Add comments naming the owner module. Add indexes for slug, RAWG id, main-flow eligibility, platform lookup and freshness. Grant the web runtime SELECT-only access to catalog facts, and reserve INSERT/UPDATE for the worker/migrator path. Keep catalog tables non-duo-scoped and free of private user state.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and `pnpm --filter @queue/db test:integration -- catalog-schema` when `TEST_DATABASE_URL` is configured; otherwise run the test command and preserve the explicit skip output.</verify>
  <acceptance_criteria>
    - `catalog.games` stores RAWG id, slug, name, description, release, cover URL, RAWG URL, source and freshness fields.
    - Main-flow coop eligibility is represented with explicit confirmation fields controlled by QUEUE/2.
    - Runtime role grants cannot mutate catalog rows.
    - Migration applies after existing numbered migrations and documents catalog ownership.
  </acceptance_criteria>
  <done>Catalog storage exists with least-privilege access and source/freshness metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Build server-only RAWG adapter and catalog repository</name>
  <files>.env.example, apps/web/src/modules/catalog/application/ports.ts, apps/web/src/modules/catalog/infrastructure/rawg-client.ts, apps/web/src/modules/catalog/infrastructure/catalog-repository.ts</files>
  <read_first>.planning/phases/02-catalogo-e-biblioteca/02-RESEARCH.md, apps/web/src/platform/server-only.ts, packages/db/src/client.ts</read_first>
  <action>Add `RAWG_API_KEY` to `.env.example` as a server-only secret. Implement a server-only RAWG adapter using `fetch`, typed parsing and bounded page sizes. Normalize RAWG records into catalog repository inputs without exposing the key. Implement repository reads for search and detail over Postgres catalog tables, and optional sync/upsert helpers for a single RAWG game or search result page. Keep all external calls in infrastructure and keep domain/application code independent of RAWG SDK details.</action>
  <verify>Run `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - No Client Component imports the RAWG adapter or any secret-bearing module.
    - RAWG request construction always includes the API key server-side and has bounded page size.
    - Adapter maps RAWG update/source data into freshness metadata.
    - Missing RAWG key fails with a server-side configuration error, not a browser-visible secret.
  </acceptance_criteria>
  <done>Catalog infrastructure can read/sync RAWG-backed data safely from the server.</done>
</task>

<task type="auto">
  <name>Task 3: Add catalog domain policies and public API</name>
  <files>apps/web/src/modules/catalog/domain/catalog-policy.ts, apps/web/src/modules/catalog/application/get-catalog-game.ts, apps/web/src/modules/catalog/application/search-catalog.ts, apps/web/src/modules/catalog/presentation/view-models.ts, apps/web/src/modules/catalog/index.ts, apps/web/tests/catalog-domain.test.ts</files>
  <read_first>.planning/phases/02-catalogo-e-biblioteca/02-CONTEXT.md, apps/web/src/modules/duo/index.ts</read_first>
  <action>Create pure catalog policy helpers for source attribution, freshness labels, missing estimated time, missing availability and main-flow eligibility. Add application use cases for catalog search and game detail that return presentation-ready data without importing React or Next.js. Expose only the public functions and types from `catalog/index.ts`.</action>
  <verify>Run `pnpm --filter @queue/web test -- catalog-domain` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - A game without confirmed 2-player campaign/story coop is excluded from main-flow suggestions.
    - Cards/detail can show RAWG attribution and freshness without loud badge semantics.
    - Estimated time and availability return honest missing-source states.
    - Domain policy tests do not import database, Next.js, React or RAWG infrastructure.
  </acceptance_criteria>
  <done>Catalog module exposes a safe, tested public contract for Phase 2 UI and library use.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- catalog-domain` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] Catalog integration tests run or report an explicit `TEST_DATABASE_URL` skip.
- [ ] No RAWG secret appears outside server-only files or `.env.example`.
</verification>

<success_criteria>
- Catalog facts are available through a module public API.
- RAWG attribution and freshness are first-class data, not ad-hoc UI text.
- Main-flow coop eligibility is explicit and conservative.
- Browser code cannot call RAWG or read `RAWG_API_KEY`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalogo-e-biblioteca/02-01-SUMMARY.md`.
</output>
