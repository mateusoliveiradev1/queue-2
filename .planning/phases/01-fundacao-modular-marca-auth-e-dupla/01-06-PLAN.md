---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "06"
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - "apps/web/src/security/headers.ts"
  - "apps/web/next.config.ts"
  - "apps/web/tests/security-headers.test.ts"
  - "apps/web/tests/phase-1-e2e.spec.ts"
  - "apps/web/tests/accessibility.spec.ts"
  - "apps/web/playwright.config.ts"
  - "packages/db/RESTORE.md"
  - "packages/db/tests/role-privileges.test.ts"
  - "scripts/check-secrets.mjs"
  - "scripts/phase-1-gate.mjs"
  - "package.json"
  - ".planning/SECURITY.md"
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, SEC-08, DATA-04, DATA-09, DATA-10, DATA-11, DATA-12, DUO-04, DUO-09, SAFE-05, SAFE-07, SAFE-08, SAFE-09]
user_setup: []
must_haves:
  truths:
    - "SEC-01: Threat model remains current for auth, pairing, duo data, RLS, roles and abuse cases."
    - "SEC-02: Every Server Action, Route Handler and server mutation validates input and authorizes current session."
    - "SEC-03: Proxy/middleware is not the sole authorization gate."
    - "SEC-04: Auth and pairing endpoints have persistent abuse rate limits."
    - "SEC-05: Production responses define CSP, HSTS, frame protection, content-type protection and restrictive referrer policy."
    - "SEC-06: User-generated display names and duo names render as text and cannot execute HTML/script."
    - "SEC-07: Logs and error responses redact sensitive values and still retain security-relevant audit events."
    - "SEC-08: Dependency, secret and static analysis scripts are part of root verification."
    - "DATA-04: Runtime role privilege tests prove no BYPASSRLS and no migration capability."
    - "DATA-09 through DATA-12: Migration, index/query-plan review and restore rehearsal documentation are included in the Phase 1 gate."
    - "DUO-04 and DUO-09: E2E/integration tests prove no third member and no cross-duo access."
    - "SAFE-05: Secret scanner and architecture checks prevent server secrets reaching browser code."
    - "SAFE-07 and SAFE-08: Auth attempts use persistent limits and secure cookie/trusted-origin config."
    - "SAFE-09: Environment docs require separate Neon branches for development, preview/test and production."
  artifacts:
    - path: "scripts/phase-1-gate.mjs"
      provides: "Single Phase 1 release gate command"
      min_lines: 60
    - path: "scripts/check-secrets.mjs"
      provides: "Secret leakage scan"
      min_lines: 50
    - path: "apps/web/tests/phase-1-e2e.spec.ts"
      provides: "End-to-end Phase 1 flow tests"
      min_lines: 120
    - path: "apps/web/src/security/headers.ts"
      provides: "Security header policy"
      min_lines: 40
  key_links:
    - from: "package.json"
      to: "scripts/phase-1-gate.mjs"
      via: "root verification script"
      pattern: "phase-1-gate"
    - from: "scripts/phase-1-gate.mjs"
      to: "pnpm check:architecture"
      via: "gate command"
      pattern: "check:architecture"
    - from: "apps/web/next.config.ts"
      to: "apps/web/src/security/headers.ts"
      via: "security headers configuration"
      pattern: "headers"
---

<objective>
Complete the Phase 1 security, recovery and end-to-end verification gate.

Purpose: Phase 1 cannot finish until the foundation proves auth, duo isolation, role limits, architecture boundaries, security headers, secret hygiene and restore readiness.
Output: Security header config, E2E/accessibility/security tests, secret/static checks, role privilege tests, restore rehearsal docs and one Phase 1 gate command.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-01-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-02-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-03-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-04-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security headers, secret scan and static gate</name>
  <files>apps/web/src/security/headers.ts, apps/web/next.config.ts, scripts/check-secrets.mjs, package.json, apps/web/tests/security-headers.test.ts</files>
  <read_first>.planning/SECURITY.md, apps/web/next.config.ts, package.json</read_first>
  <action>Implement explicit production-oriented security headers: Content Security Policy, HSTS for production, frame protection, content-type protection and restrictive referrer policy. Add `scripts/check-secrets.mjs` to scan source and client bundles for known secret env names and high-risk literal patterns. Wire dependency/static/secret checks into root `verify` or `phase:1:gate` script. Tests must assert header presence and that secret-bearing env vars are not referenced by client files.</action>
  <verify>Run `pnpm verify` or `pnpm phase:1:gate` once the gate script exists.</verify>
  <acceptance_criteria>
    - Header tests assert CSP, HSTS production behavior, frame protection, content type and referrer policy.
    - Secret scan checks `RAWG_API_KEY`, database URLs, auth secret and email keys are server-only.
    - Root package script runs secret scan and architecture check.
  </acceptance_criteria>
  <done>Security headers and static/secret checks are part of normal verification.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 1 E2E, accessibility and role privilege tests</name>
  <files>apps/web/playwright.config.ts, apps/web/tests/phase-1-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, packages/db/tests/role-privileges.test.ts</files>
  <read_first>.planning/SECURITY.md, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Add Playwright coverage for signup, verification, login, reset request, session revocation, pairing, naming duo, dashboard empty state, profile and duo pages. Add tests for cross-duo access attempts through app routes and API/actions, and for the concurrent pairing race if the test harness can simulate two users. Add axe/accessibility checks for auth/pairing/dashboard/profile/duo pages. Add DB role privilege tests proving runtime role has no BYPASSRLS, cannot migrate and is subject to forced RLS.</action>
  <verify>Run `pnpm --filter @queue/web test:e2e` and `pnpm --filter @queue/db test:integration` when env vars are configured; otherwise tests must skip with explicit missing-env diagnostics.</verify>
  <acceptance_criteria>
    - E2E suite covers custom auth pages and duo pairing flow.
    - Cross-duo app-level access attempts fail.
    - Accessibility tests cover visible focus, labels and reduced-motion-safe rendering for Phase 1 surfaces.
    - Runtime role privilege test fails if runtime can bypass RLS or create/drop schema objects.
  </acceptance_criteria>
  <done>Phase 1 has user-flow and adversarial verification beyond unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Create one Phase 1 gate command and update security status</name>
  <files>scripts/phase-1-gate.mjs, package.json, packages/db/RESTORE.md, .planning/SECURITY.md</files>
  <read_first>.planning/SECURITY.md, packages/db/RESTORE.md, package.json</read_first>
  <action>Create `scripts/phase-1-gate.mjs` that runs or orchestrates lint, typecheck, unit tests, integration tests, E2E tests, architecture check, secret scan and DB role/RLS checks with clear skip diagnostics for missing external env. Update `.planning/SECURITY.md` only if needed to record the Phase 1 verification procedure; do not weaken existing gates. Ensure `packages/db/RESTORE.md` includes a concrete restore rehearsal checklist using a Neon restore branch or documented local fallback for pre-production.</action>
  <verify>Run `pnpm phase:1:gate` and inspect output for each required gate section.</verify>
  <acceptance_criteria>
    - Root `package.json` has `phase:1:gate`.
    - Gate command includes architecture, typecheck, tests, RLS/role checks, E2E, accessibility and secret scan.
    - Security contract records the procedure without claiming impossible absolute security.
    - Restore rehearsal checklist is concrete enough to execute before launch.
  </acceptance_criteria>
  <done>Phase 1 has a single objective completion gate.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm phase:1:gate` passes or reports only explicit missing external env skips.
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm --filter @queue/db test:integration` passes when database env is available.
- [ ] `pnpm --filter @queue/web test:e2e` passes when app/database/email test env is available.
- [ ] No known critical or high security finding remains open.
</verification>

<success_criteria>
- Phase 1 completion can be judged by a concrete gate, not manual optimism.
- Auth, pairing, RLS, role, boundary, header, secret, restore and accessibility concerns are all covered.
- Security contract remains honest and measurable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-06-SUMMARY.md`.
</output>

