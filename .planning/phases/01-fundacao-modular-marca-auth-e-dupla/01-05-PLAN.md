---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "05"
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - "apps/web/src/modules/duo/domain/pairing-code.ts"
  - "apps/web/src/modules/duo/domain/duo-policy.ts"
  - "apps/web/src/modules/duo/application/create-pairing-code.ts"
  - "apps/web/src/modules/duo/application/join-duo.ts"
  - "apps/web/src/modules/duo/application/update-duo-settings.ts"
  - "apps/web/src/modules/duo/application/get-duo-dashboard.ts"
  - "apps/web/src/modules/duo/infrastructure/duo-repository.ts"
  - "apps/web/src/modules/duo/presentation/view-models.ts"
  - "apps/web/src/modules/duo/index.ts"
  - "apps/web/src/app/(public)/parear/page.tsx"
  - "apps/web/src/app/app/page.tsx"
  - "apps/web/src/app/app/perfil/page.tsx"
  - "apps/web/src/app/app/dupla/page.tsx"
  - "apps/web/src/platform/rate-limit/persistent.ts"
  - "apps/web/tests/duo-domain.test.ts"
  - "apps/web/tests/duo-flow.test.ts"
  - "apps/web/tests/duo-isolation.test.ts"
autonomous: true
requirements: [DUO-01, DUO-02, DUO-03, DUO-04, DUO-05, DUO-06, DUO-07, DUO-08, DUO-09, DUO-10, SEC-02, SEC-04, SEC-06, SEC-07]
user_setup: []
must_haves:
  truths:
    - "DUO-01 and D-10: Verified user without duo can create a six-character pairing code that expires in 24 hours and can be revoked."
    - "DUO-02 and D-09: Entering a valid active code forms the duo immediately."
    - "DUO-03, D-10 and D-15: Expired or revoked codes cannot be used and are surfaced as not active without differentiating revocation from expiration."
    - "DUO-04 and D-17: Concurrent attempts against the same code cannot create a third member; the losing user sees that the duo was just formed."
    - "DUO-05, D-18, D-19 and D-20: Duo has required name, naming happens after pairing, and duo page shows name, both members and pairing date."
    - "DUO-06 and D-25: User can edit own display name and see active sessions/logout in profile."
    - "DUO-07 and D-21: Duo timezone is detected by browser and confirmed by the user."
    - "DUO-08 and D-22: Shared notification and audio preferences exist; push permission is not requested in Phase 1."
    - "DUO-09, D-30 and D-31: Verified user without duo is always routed to `/parear`; authenticated app routes require duo membership."
    - "DUO-10 and D-14: A user already in a duo cannot create or join another code, and no solo app/progress path exists."
    - "D-16: Pairing code attempts use persistent rate limiting with progressive warning and wait state."
    - "D-18 through D-24: Duo/profile text fields are plain text with short limits, no HTML/Markdown, and no duo owner role."
    - "D-23: Either member can edit duo name and timezone with audit/feedback."
    - "SEC-02, SEC-06 and SEC-07: Duo mutations validate input, authorize session and sanitize/redact text and logs."
  artifacts:
    - path: "apps/web/src/modules/duo/domain/pairing-code.ts"
      provides: "Pure pairing-code validation and state mapping"
      min_lines: 50
    - path: "apps/web/src/modules/duo/application/join-duo.ts"
      provides: "Duo join use case"
      min_lines: 80
    - path: "apps/web/src/modules/duo/infrastructure/duo-repository.ts"
      provides: "Drizzle repository for duo operations"
      min_lines: 100
    - path: "apps/web/tests/duo-isolation.test.ts"
      provides: "Application-layer duo isolation tests"
      min_lines: 80
  key_links:
    - from: "apps/web/src/app/(public)/parear/page.tsx"
      to: "apps/web/src/modules/duo/index.ts"
      via: "public duo module actions/view models"
      pattern: "createPairingCode|joinDuo"
    - from: "apps/web/src/modules/duo/infrastructure/duo-repository.ts"
      to: "packages/db"
      via: "typed db access under transaction-local identity"
      pattern: "with.*Transaction|set.*User"
---

<objective>
Implement duo pairing, duo identity, profile/settings and app route-state enforcement.

Purpose: QUEUE/2 is only valid when every authenticated experience belongs to exactly two players and no solo path exists.
Output: Duo module domain/application/infrastructure code, pairing UI behavior, dashboard/profile/duo data wiring, persistent pairing rate limits and duo tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-02-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-03-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create duo domain rules and application use cases</name>
  <files>apps/web/src/modules/duo/domain/pairing-code.ts, apps/web/src/modules/duo/domain/duo-policy.ts, apps/web/src/modules/duo/application/create-pairing-code.ts, apps/web/src/modules/duo/application/join-duo.ts, apps/web/src/modules/duo/application/update-duo-settings.ts, apps/web/src/modules/duo/application/get-duo-dashboard.ts, apps/web/src/modules/duo/index.ts</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Implement pure domain rules for six-character codes, code states, text limits, duo membership, no-owner editing and no-solo state. Implement application use cases for creating a code, revoking a code, joining a duo, naming a duo, updating timezone/preferences and reading dashboard/profile/duo state. Use Portuguese user-facing messages through view models, not thrown infrastructure details. Export only the public use cases and view types from `modules/duo/index.ts`.</action>
  <verify>Run `pnpm --filter @queue/web test -- duo-domain` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Domain files import no Next.js, React, Drizzle, Better Auth or browser APIs.
    - Pairing code state maps to invalid, inactive, attempt-limited and race-lost messages.
    - Duo policy rejects users already in a duo from creating or joining another code.
    - Public module entrypoint exports use cases without exposing internals.
  </acceptance_criteria>
  <done>Duo rules are testable and isolated from framework code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement repository, transactions and rate limits</name>
  <files>apps/web/src/modules/duo/infrastructure/duo-repository.ts, apps/web/src/platform/rate-limit/persistent.ts, apps/web/tests/duo-flow.test.ts, apps/web/tests/duo-isolation.test.ts</files>
  <read_first>packages/db/src/schema/app.ts, packages/db/src/rls/policies.sql, .planning/SECURITY.md</read_first>
  <action>Implement Drizzle repository methods that run under server-validated session and transaction-local database identity. The join path must call the database's atomic pairing claim mechanism and map database constraint/race outcomes to user-facing states. Add persistent rate limit checks for pairing code attempts. Add tests for create code, revoke code, expired code, invalid code, already-in-duo rejection, race-lost state, cross-duo application denial and sanitized text fields.</action>
  <verify>Run `pnpm --filter @queue/web test -- duo` and database integration tests from Plan 02 if test DB is available.</verify>
  <acceptance_criteria>
    - Repository never bypasses RLS or uses migrator credentials.
    - Pairing attempt rate limit persists outside process memory.
    - Tests create two duos and prove one cannot load/update the other through application use cases.
    - Text input is length-limited and rendered as plain text only.
  </acceptance_criteria>
  <done>Duo use cases are backed by authoritative transactions and rate limits.</done>
</task>

<task type="auto">
  <name>Task 3: Wire pairing, dashboard, profile and duo pages</name>
  <files>apps/web/src/modules/duo/presentation/view-models.ts, apps/web/src/app/(public)/parear/page.tsx, apps/web/src/app/app/page.tsx, apps/web/src/app/app/perfil/page.tsx, apps/web/src/app/app/dupla/page.tsx</files>
  <read_first>apps/web/src/platform/auth/session.ts, apps/web/src/modules/duo/index.ts, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Wire Phase 1 pages to real auth and duo state. Verified users without duo route to `/parear`. Users with duo and missing name route to naming state. Dashboard shows empty queue and the three-step ritual only after duo is formed and named. Profile can update display name and manage sessions from Plan 04. Duo page can update duo name/timezone/preferences; either member can update them. Do not implement catalog, discovery, sessions, reviews or solo progress.</action>
  <verify>Run `pnpm --filter @queue/web test -- duo-flow` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Direct navigation to authenticated app by verified no-duo user redirects to `/parear`.
    - Already paired user cannot create or join another code.
    - Duo page shows name, two members and pairing date after pairing.
    - Push permission is not requested in Phase 1 preferences.
  </acceptance_criteria>
  <done>Authenticated Phase 1 experience is always duo-scoped.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/web test -- duo` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] Cross-duo app-level tests and database-level tests both exist.
- [ ] No Phase 1 route offers solo progress or later gameplay flows.
</verification>

<success_criteria>
- Users can create/revoke/join pairing codes and form exactly one duo.
- Duo identity, timezone, preferences and profile surfaces are functional.
- The app has no solo authenticated path and no third member path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-05-SUMMARY.md`.
</output>

