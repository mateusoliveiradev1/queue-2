---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "04"
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - "apps/web/src/platform/auth/server.ts"
  - "apps/web/src/platform/auth/client.ts"
  - "apps/web/src/platform/auth/session.ts"
  - "apps/web/src/platform/auth/email.ts"
  - "apps/web/src/platform/auth/rate-limit.ts"
  - "apps/web/src/platform/auth/actions.ts"
  - "apps/web/src/app/api/auth/[...all]/route.ts"
  - "apps/web/src/app/(public)/login/page.tsx"
  - "apps/web/src/app/(public)/cadastro/page.tsx"
  - "apps/web/src/app/(public)/verificar-email/page.tsx"
  - "apps/web/src/app/(public)/recuperar-senha/page.tsx"
  - "apps/web/proxy.ts"
  - "apps/web/tests/auth-flow.test.ts"
  - "apps/web/tests/auth-security.test.ts"
  - ".env.example"
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, AUTH-07, SEC-02, SEC-03, SEC-04, SEC-05, SEC-07, SAFE-05, SAFE-07, SAFE-08, META-02]
user_setup:
  - service: auth-and-email
    why: "Better Auth and email verification/reset require deployment-specific secrets and a transactional email sender."
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: "Generate a strong production secret"
      - name: BETTER_AUTH_URL
        source: "Canonical app URL for the deployed environment"
      - name: BETTER_AUTH_TRUSTED_ORIGINS
        source: "Comma-separated trusted origins for local, preview and production"
      - name: RESEND_API_KEY
        source: "Resend dashboard API key or the selected transactional email provider"
      - name: EMAIL_FROM
        source: "Verified sender address for verification and reset emails"
must_haves:
  truths:
    - "AUTH-01 and D-01: Signup accepts email, password and display name."
    - "AUTH-02 and D-02: Unverified users are restricted to verification UI with resend, correct email and logout."
    - "AUTH-03 and D-06: Email/password login works, but unverified login is blocked and routed to verification."
    - "AUTH-04: User can request and complete password reset through an email link."
    - "AUTH-05: Session persists across browser refresh through secure cookie-based session validation."
    - "AUTH-06: User can list and revoke active sessions."
    - "AUTH-07: User can log out from the authenticated app."
    - "D-03: A valid verification link signs the user in and routes them to pairing."
    - "D-04: Email correction preserves the account, invalidates old verification and sends a new link."
    - "D-05: Password requirements appear as a progressive checklist while typing."
    - "D-07 and D-08: Verification resend uses neutral confirmation with cooldown, and expired/used links offer a new send without technical leakage."
    - "SEC-02 and SEC-03: Server actions, route handlers and protected pages validate session server-side; proxy is UX only."
    - "SEC-04 and SAFE-07: Auth endpoints use persistent rate limits suitable for serverless."
    - "SEC-05 and SAFE-08: Trusted origins, secure cookies and security headers are configured for deployed environments."
    - "SEC-07 and SAFE-05: Auth errors/logs redact sensitive values and email/auth secrets stay server-side."
  artifacts:
    - path: "apps/web/src/platform/auth/server.ts"
      provides: "Better Auth server configuration"
      min_lines: 80
    - path: "apps/web/src/platform/auth/actions.ts"
      provides: "Server actions for auth forms"
      min_lines: 100
    - path: "apps/web/src/app/api/auth/[...all]/route.ts"
      provides: "Better Auth route handler"
      exports: ["GET", "POST"]
    - path: "apps/web/tests/auth-flow.test.ts"
      provides: "Auth flow tests"
      min_lines: 80
  key_links:
    - from: "apps/web/src/app/api/auth/[...all]/route.ts"
      to: "apps/web/src/platform/auth/server.ts"
      via: "Better Auth handler import"
      pattern: "auth"
    - from: "apps/web/src/platform/auth/server.ts"
      to: "packages/db"
      via: "Drizzle adapter"
      pattern: "drizzleAdapter|provider.*pg"
    - from: "apps/web/src/app/(public)/login/page.tsx"
      to: "apps/web/src/platform/auth/actions.ts"
      via: "form action"
      pattern: "signIn|login"
---

<objective>
Implement Better Auth email/password flows and server authorization gates.

Purpose: Users must create accounts, verify email, reset passwords, persist/revoke sessions and reach pairing only through a secure custom QUEUE/2 auth experience.
Output: Better Auth platform module, route handler, auth server actions, email sender adapter, persistent rate-limit config, auth UI wiring and auth tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-02-SUMMARY.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Better Auth with Drizzle and secure runtime settings</name>
  <files>apps/web/src/platform/auth/server.ts, apps/web/src/platform/auth/client.ts, apps/web/src/platform/auth/session.ts, apps/web/src/platform/auth/email.ts, apps/web/src/app/api/auth/[...all]/route.ts, .env.example</files>
  <read_first>.planning/SECURITY.md, .planning/research/STACK.md, packages/db/src/schema/index.ts</read_first>
  <action>Configure Better Auth self-hosted with email/password enabled, required email verification, Drizzle adapter using Postgres, secure trusted origins, secure cookie defaults for production, server-only email callbacks and database-backed or custom persistent rate-limit storage. Expose only server-safe helpers from `server.ts` and client-safe helpers from `client.ts`. Create the `[...all]` route handler by delegating to Better Auth; do not put product business rules in the route handler.</action>
  <verify>Run `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Better Auth config has `emailAndPassword.enabled` and `requireEmailVerification`.
    - Better Auth uses Drizzle adapter with Postgres provider.
    - Email callbacks are implemented in server-only code and use `RESEND_API_KEY` or provider abstraction only on the server.
    - Auth route handler exports GET and POST by delegating to the auth handler.
  </acceptance_criteria>
  <done>Better Auth is configured without leaking secrets or domain rules.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auth forms and verification/reset states</name>
  <files>apps/web/src/platform/auth/actions.ts, apps/web/src/app/(public)/login/page.tsx, apps/web/src/app/(public)/cadastro/page.tsx, apps/web/src/app/(public)/verificar-email/page.tsx, apps/web/src/app/(public)/recuperar-senha/page.tsx</files>
  <read_first>.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md, apps/web/src/platform/auth/server.ts</read_first>
  <action>Replace placeholder auth form behavior with typed server actions. Signup collects email, password and display name. Password checklist remains client-side UI but final validation is server-side. Login with unverified email returns a verification-state response. Verification page supports resend with cooldown messaging, email correction, logout, expired/used link recovery and automatic route to `/parear` after valid verification. Reset page supports request and completion states with neutral responses that do not enumerate accounts.</action>
  <verify>Run `pnpm --filter @queue/web test -- auth-flow`.</verify>
  <acceptance_criteria>
    - Signup, login, resend verification, email correction, reset request, reset completion and logout actions exist.
    - Unverified login cannot reach authenticated app routes.
    - Email correction invalidates previous verification token or records a new verification state according to Better Auth capabilities.
    - User-facing error messages are Portuguese, neutral and do not reveal sensitive implementation details.
  </acceptance_criteria>
  <done>Auth screens perform real server-side auth operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add session, proxy and auth security tests</name>
  <files>apps/web/proxy.ts, apps/web/src/platform/auth/session.ts, apps/web/tests/auth-flow.test.ts, apps/web/tests/auth-security.test.ts</files>
  <read_first>.planning/SECURITY.md, apps/web/src/platform/auth/server.ts</read_first>
  <action>Add protected-route helpers that validate the session server-side for pages/actions. Proxy may perform redirect optimization but must not be the only protection. Implement session listing/revocation support for profile UI. Add tests for persistent session after refresh, active session list, revoke session, logout, unverified user restriction, reset flow, rate-limit configuration, trusted origins, secure cookie config and error redaction.</action>
  <verify>Run `pnpm --filter @queue/web test -- auth-security` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Protected helpers call Better Auth session validation on the server.
    - Proxy comments or implementation state that proxy is UX-only, not authorization.
    - Tests fail if session validation is removed from protected helpers.
    - Tests assert auth rate-limit storage is not process memory in production mode.
  </acceptance_criteria>
  <done>Auth security gates are executable and tied to protected app access.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- auth` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] No auth secret or email provider key is referenced by a Client Component.
</verification>

<success_criteria>
- Users can complete account, verification, login, reset, session revoke and logout flows.
- Unverified users are contained in verification state.
- Protected auth checks happen server-side and rate limits are persistent enough for serverless.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-04-SUMMARY.md`.
</output>

