---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "pnpm-workspace.yaml"
  - "pnpm-lock.yaml"
  - "turbo.json"
  - "tsconfig.base.json"
  - ".gitignore"
  - "apps/web/package.json"
  - "apps/web/next.config.ts"
  - "apps/web/tsconfig.json"
  - "apps/web/src/app/layout.tsx"
  - "apps/web/src/app/page.tsx"
  - "apps/web/src/modules/duo/index.ts"
  - "apps/web/src/platform/server-only.ts"
  - "packages/config/package.json"
  - "packages/config/eslint/index.js"
  - "packages/config/typescript/base.json"
  - "packages/ui/package.json"
  - "packages/ui/src/index.ts"
  - "packages/db/package.json"
  - "packages/db/src/index.ts"
  - "scripts/check-architecture.mjs"
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05, ARCH-06, ARCH-07, SAFE-05]
user_setup: []
must_haves:
  truths:
    - "ARCH-01: The repo is a pnpm workspace with one Next.js app at apps/web and shared packages packages/db, packages/ui and packages/config."
    - "ARCH-02: Domain modules expose public index.ts entrypoints and automated checks reject deep imports into another module internals."
    - "ARCH-03: Domain rule files do not import Next.js, React, Drizzle, Better Auth, browser APIs or external SDKs."
    - "ARCH-04: Route files are composition-only and cannot contain duo/auth business decisions."
    - "ARCH-05: Cross-domain communication is prepared through public contracts and event types, not direct internal imports."
    - "ARCH-06: Shared packages cannot import from apps/web domain modules."
    - "ARCH-07: `pnpm check:architecture` fails on forbidden imports, workspace dependency leaks and client/server boundary leaks."
    - "SAFE-05: Server-only modules are marked and secrets are never imported by Client Components."
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace package boundaries"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Monorepo task graph"
      contains: "check:architecture"
    - path: "scripts/check-architecture.mjs"
      provides: "Automated modular boundary validation"
      min_lines: 80
    - path: "apps/web/src/modules/duo/index.ts"
      provides: "Initial duo module public entrypoint"
      min_lines: 5
  key_links:
    - from: "package.json"
      to: "turbo.json"
      via: "root scripts run Turborepo tasks"
      pattern: "turbo run"
    - from: "scripts/check-architecture.mjs"
      to: "apps/web/src/modules"
      via: "source scan"
      pattern: "src/modules"
---

<objective>
Create the QUEUE/2 modular monorepo foundation.

Purpose: Phase 1 depends on verified module boundaries before auth, duo, database and UI implementation can be trusted.
Output: Root workspace, Next.js app shell, shared packages, server-only markers and an architecture check command.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pnpm/Turborepo workspace</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .gitignore</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/research/STACK.md</read_first>
  <action>Create a root private workspace for QUEUE/2 using pnpm and Turborepo. Add root scripts for `dev`, `build`, `lint`, `typecheck`, `test`, `test:integration`, `check:architecture` and `verify`. Configure package globs for `apps/*` and `packages/*`. Use `workspace:*` for local package dependencies. Configure `turbo.json` so `build`, `lint`, `typecheck`, `test`, `test:integration` and `check:architecture` are first-class tasks.</action>
  <verify>Run `pnpm --version` if available, then inspect that root scripts call `turbo run` for graph-aware tasks.</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` includes `apps/*` and `packages/*`.
    - `package.json` is private and has scripts for `check:architecture` and `verify`.
    - `turbo.json` declares tasks for build, lint, typecheck, test, test:integration and check:architecture.
  </acceptance_criteria>
  <done>Workspace metadata exists and reflects the architecture contract.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the Next.js app and shared packages</name>
  <files>apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, packages/config/package.json, packages/config/eslint/index.js, packages/config/typescript/base.json, packages/ui/package.json, packages/ui/src/index.ts, packages/db/package.json, packages/db/src/index.ts</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/research/STACK.md</read_first>
  <action>Create `apps/web` as the only app and `packages/db`, `packages/ui`, `packages/config` as the only shared packages. Add package exports so shared packages expose public paths only. In `apps/web`, create a minimal App Router layout and public root page that does not implement the later landing; it may link to auth/pairing routes as future Phase 1 surfaces. Keep shared packages free of domain logic. Add `server-only` where server-only package or app modules are introduced.</action>
  <verify>Run `pnpm --filter @queue/web typecheck` when dependencies are installed; otherwise verify file structure and package exports manually.</verify>
  <acceptance_criteria>
    - `apps/web/src/app/layout.tsx` exists and exports a root layout.
    - `packages/ui/src/index.ts` exports no app domain code.
    - `packages/db/src/index.ts` is server-only or documents server-only use.
    - Package dependencies between local packages use `workspace:*`.
  </acceptance_criteria>
  <done>App and shared package skeletons exist without violating ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated architecture boundary checks</name>
  <files>scripts/check-architecture.mjs, package.json, turbo.json</files>
  <read_first>.planning/ARCHITECTURE.md, .planning/SECURITY.md</read_first>
  <action>Implement `scripts/check-architecture.mjs` as a deterministic Node script. It must scan TypeScript/TSX files and fail on: module deep imports across `apps/web/src/modules/*/(domain|application|infrastructure|presentation|events)`, Client Components importing `packages/db`, `apps/web/src/platform/auth/server`, `server-only`, Drizzle, Better Auth server config, Neon, `pg`, or secret-bearing integration modules, domain files importing Next.js/React/Drizzle/Better Auth/browser APIs, and `packages/ui` or `packages/db` importing from `apps/web`. Wire it to `pnpm check:architecture`.</action>
  <verify>Run `pnpm check:architecture`; with only the skeleton it should pass. Add script fixtures or inline self-tests if the checker can validate known forbidden sample strings without committing bad files.</verify>
  <acceptance_criteria>
    - `scripts/check-architecture.mjs` contains checks for `src/modules`, `use client`, `packages/ui` and `packages/db`.
    - `pnpm check:architecture` executes the script.
    - The checker exits non-zero when a forbidden import is present.
  </acceptance_criteria>
  <done>Architecture checks are executable and connected to root verification.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm check:architecture` passes on the scaffold.
- [ ] `pnpm typecheck` passes once dependencies are installed.
- [ ] No route, UI or shared package contains duo/auth business decisions.
- [ ] No Client Component imports server-only modules.
</verification>

<success_criteria>
- Root workspace and package structure match `.planning/ARCHITECTURE.md`.
- Architecture check is substantive and fails on forbidden imports.
- Future plans can import only supported package and module public entrypoints.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-01-SUMMARY.md`.
</output>

