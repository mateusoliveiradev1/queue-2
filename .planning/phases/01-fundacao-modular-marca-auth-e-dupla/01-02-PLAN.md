---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - "packages/db/drizzle.config.ts"
  - "packages/db/src/client.ts"
  - "packages/db/src/schema/auth.ts"
  - "packages/db/src/schema/app.ts"
  - "packages/db/src/schema/ops.ts"
  - "packages/db/src/schema/index.ts"
  - "packages/db/src/migrations/0001_foundation.sql"
  - "packages/db/src/rls/membership.sql"
  - "packages/db/src/rls/policies.sql"
  - "packages/db/src/roles.sql"
  - "packages/db/src/testing/migrate-empty.ts"
  - "packages/db/src/testing/rls-test-context.ts"
  - "packages/db/tests/migrations.test.ts"
  - "packages/db/tests/rls-isolation.test.ts"
  - "packages/db/tests/pairing-concurrency.test.ts"
  - ".env.example"
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09, DATA-10, DATA-11, DATA-12, DUO-04, DUO-09, DUO-10, SEC-01, SAFE-09]
user_setup:
  - service: neon
    why: "Neon branches and separate credentials are required for development, preview/test and production."
    env_vars:
      - name: DATABASE_URL
        source: "Neon pooled app runtime connection string for local development"
      - name: DIRECT_DATABASE_URL
        source: "Neon direct connection string for migrations"
      - name: TEST_DATABASE_URL
        source: "Neon branch or local Postgres URL used by integration tests"
    dashboard_config:
      - task: "Create or identify separate Neon branches"
        location: "Neon Console"
        details: "Use separate development, preview/test and production branches before production launch."
must_haves:
  truths:
    - "DATA-01: Auth tables are owned by schema `auth`, duo product tables by `app`, and operational tables by `ops`."
    - "DATA-02: Duo and pairing tables enforce primary keys, foreign keys, nullability, unique constraints and check constraints."
    - "DATA-03: Every duo-scoped table has `duo_id` and has RLS enabled and forced."
    - "DATA-04: The web runtime role is non-owner, does not have BYPASSRLS and cannot apply migrations."
    - "DATA-05: Application database identity is set transaction-locally and reset per transaction."
    - "DATA-06: SECURITY DEFINER membership helpers use schema-qualified references, fixed search_path and restricted EXECUTE grants."
    - "DATA-07 and DUO-04: Concurrent pairing attempts cannot create a third duo member."
    - "DATA-08: Operational audit/domain event facts created in this phase are append-only from normal application flows."
    - "DATA-09 and DATA-10: Migrations apply to an empty database and upgrade path through a direct connection, not the web runtime pooled role."
    - "DATA-11: Hot pairing and RLS predicates have reviewed indexes."
    - "DATA-12 and SAFE-09: Restore strategy and Neon branch separation are documented and testable."
    - "DUO-09: Cross-duo reads and writes fail at the database layer under the app runtime role."
    - "DUO-10: The database model has no solo progress/session tables or solo progress path in Phase 1."
  artifacts:
    - path: "packages/db/src/migrations/0001_foundation.sql"
      provides: "Foundation SQL migration with schemas, tables, constraints, roles and RLS"
      min_lines: 120
    - path: "packages/db/src/rls/membership.sql"
      provides: "Restricted duo membership helper"
      contains: "SECURITY DEFINER"
    - path: "packages/db/tests/rls-isolation.test.ts"
      provides: "Database-layer cross-duo isolation tests"
      min_lines: 80
    - path: "packages/db/tests/pairing-concurrency.test.ts"
      provides: "Concurrent pairing invariant tests"
      min_lines: 80
  key_links:
    - from: "packages/db/src/client.ts"
      to: "packages/db/src/testing/rls-test-context.ts"
      via: "transaction-local app user id context"
      pattern: "set_config|SET LOCAL"
    - from: "packages/db/src/migrations/0001_foundation.sql"
      to: "packages/db/src/rls/policies.sql"
      via: "RLS policies applied to duo-scoped tables"
      pattern: "FORCE ROW LEVEL SECURITY"
---

<objective>
Build the Postgres foundation for auth, duo identity, RLS and recovery.

Purpose: The `/2` product promise depends on database invariants that survive forged requests and concurrency.
Output: Drizzle schema, SQL migrations, roles/RLS helpers, integration tests, environment contract and restore strategy hooks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define schemas, migrations and role contract</name>
  <files>packages/db/drizzle.config.ts, packages/db/src/schema/auth.ts, packages/db/src/schema/app.ts, packages/db/src/schema/ops.ts, packages/db/src/schema/index.ts, packages/db/src/migrations/0001_foundation.sql, packages/db/src/roles.sql, .env.example</files>
  <read_first>.planning/SECURITY.md, .planning/research/STACK.md, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Create the Phase 1 database schema. Auth schema supports Better Auth generated tables and rate-limit storage. App schema includes profiles, duos, duo_members, pairing_codes and duo_preferences. Ops schema includes audit/domain events and idempotency keys needed by Phase 1. Add role SQL for owner/migrator, app runtime, worker and read-only tooling. Add `.env.example` entries for direct migration URL, runtime URL and test URL. Do not place domain product tables in the `auth` schema.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and inspect SQL for schemas `auth`, `app`, `ops`, role grants and no runtime migration privilege.</verify>
  <acceptance_criteria>
    - Migration SQL creates `auth`, `app` and `ops`.
    - Duo tables include explicit ownership comments or schema grouping.
    - Runtime role SQL does not include `BYPASSRLS` and does not grant schema ownership.
    - `.env.example` includes `DATABASE_URL`, `DIRECT_DATABASE_URL` and `TEST_DATABASE_URL`.
  </acceptance_criteria>
  <done>Database ownership and role model are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RLS helpers and duo invariants</name>
  <files>packages/db/src/rls/membership.sql, packages/db/src/rls/policies.sql, packages/db/src/migrations/0001_foundation.sql, packages/db/src/client.ts</files>
  <read_first>.planning/SECURITY.md, .planning/ARCHITECTURE.md</read_first>
  <action>Add a restricted `app.has_duo_membership(uid, duo_id)` helper and transaction-local identity utilities. Every duo-scoped table must enable and force RLS. Policies must be default-deny except for authenticated users whose transaction-local user id is a member of the row duo. Enforce max-two membership with constraints plus an atomic pairing claim strategy, using locks or a SQL function where needed. Add indexes for pairing code lookup, duo membership checks, foreign keys and RLS predicates.</action>
  <verify>Run migration tests and manually inspect that SQL contains `ENABLE ROW LEVEL SECURITY`, `FORCE ROW LEVEL SECURITY`, `SECURITY DEFINER`, fixed `search_path`, and restricted grants.</verify>
  <acceptance_criteria>
    - RLS is enabled and forced for every table with `duo_id`.
    - `SECURITY DEFINER` helper uses schema-qualified references and fixed search_path.
    - Pairing code claim path cannot insert a third member under concurrent calls.
    - RLS predicate indexes exist for user/duo membership lookups.
  </acceptance_criteria>
  <done>Database prevents cross-duo access and pairing races independently of UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add database verification tests and restore notes</name>
  <files>packages/db/src/testing/migrate-empty.ts, packages/db/src/testing/rls-test-context.ts, packages/db/tests/migrations.test.ts, packages/db/tests/rls-isolation.test.ts, packages/db/tests/pairing-concurrency.test.ts, packages/db/RESTORE.md</files>
  <read_first>.planning/SECURITY.md, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md</read_first>
  <action>Add Vitest integration tests for empty migration, upgrade migration placeholder, runtime role restrictions, app-user transaction context, cross-duo read/write denial and concurrent pairing. Add `packages/db/RESTORE.md` documenting the Neon branch restore rehearsal expected before production launch, including how to prove the restore branch can run the Phase 1 migration and isolation tests.</action>
  <verify>Run `pnpm --filter @queue/db test:integration` against `TEST_DATABASE_URL` when available; otherwise tests must skip with a clear missing-env message rather than falsely pass.</verify>
  <acceptance_criteria>
    - Migration test fails if migration cannot apply to an empty database.
    - RLS test creates two duos and proves each cannot read/write the other's duo-scoped rows.
    - Concurrency test attempts simultaneous code claims and proves only two members exist.
    - Restore documentation references Neon branches and restore-window rehearsal.
  </acceptance_criteria>
  <done>Database gate is testable and not dependent on manual inspection alone.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/db test:integration` passes when `TEST_DATABASE_URL` is configured.
- [ ] Runtime role cannot bypass RLS or apply migrations.
- [ ] Cross-duo database denial and concurrent pairing tests exist and are meaningful.
</verification>

<success_criteria>
- Database foundation enforces the `/2` invariant under concurrency.
- Duo-scoped data is isolated by forced RLS and least-privileged runtime roles.
- Migration and restore work are covered by executable tests or explicit environment-gated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-02-SUMMARY.md`.
</output>

