---
phase: 01-fundacao-modular-marca-auth-e-dupla
plan: "03"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - "packages/ui/src/tokens.css"
  - "packages/ui/src/fonts.ts"
  - "packages/ui/src/brand/wordmark.tsx"
  - "packages/ui/src/brand/mark.tsx"
  - "packages/ui/src/brand/loading.tsx"
  - "packages/ui/src/feedback/toast.tsx"
  - "packages/ui/src/index.ts"
  - "apps/web/src/app/globals.css"
  - "apps/web/src/app/(public)/login/page.tsx"
  - "apps/web/src/app/(public)/cadastro/page.tsx"
  - "apps/web/src/app/(public)/verificar-email/page.tsx"
  - "apps/web/src/app/(public)/recuperar-senha/page.tsx"
  - "apps/web/src/app/(public)/parear/page.tsx"
  - "apps/web/src/app/app/page.tsx"
  - "apps/web/src/app/app/perfil/page.tsx"
  - "apps/web/src/app/app/dupla/page.tsx"
  - "apps/web/src/components/app-shell.tsx"
  - "apps/web/tests/brand-ui.test.tsx"
autonomous: true
requirements: [BRND-01, BRND-02, BRND-03, BRND-04, BRND-05, BRND-06, BRND-11, BRND-13, META-02]
user_setup: []
must_haves:
  truths:
    - "BRND-01 and D-29: User sees QUEUE/2 wordmark with Archivo Black `QUEUE` and acid-lime JetBrains Mono `/2`, with restrained energy."
    - "BRND-02: User sees the `/2` mark in loading and app icon/fav icon placeholders."
    - "BRND-03: Graphite, off-white, acid lime, violet shock and rarity tokens are defined consistently in OKLCH."
    - "BRND-04: Display, body and mono font contracts are represented in the UI package and app globals."
    - "BRND-05: Roulette pointer motif exists as a reusable divider/list marker without implementing roulette."
    - "BRND-06: Loading state uses a `/2` SVG stroke animation, not a generic spinner."
    - "BRND-11: Single-line and stacked QUEUE/2 logo variants exist for dark and light contexts."
    - "BRND-13 and D-33: Toast feedback is accessible, QUEUE/2-styled and reserves high-impact variants for special moments."
    - "META-02: Visitors can reach custom login, signup and pairing pages."
    - "D-11: Pairing code UI presents a large code, copy action and invite validity."
    - "D-12: Pairing success UI can show a short branded celebration that says the queue became ours."
    - "D-13 and D-15: Pairing error UI has simple neutral states for invalid, inactive and attempt-limited codes."
    - "D-26, D-27 and D-32: Initial dashboard shows duo context, empty queue state and the three-step ritual descobrir, sortear, zerar."
    - "D-28: Future capabilities appear as honest locked next steps without fake clickable placeholders."
    - "D-34 and D-35: Phase 1 authenticated pages are pairing, dashboard, profile and duo; future duo reviews are not implemented."
  artifacts:
    - path: "packages/ui/src/brand/wordmark.tsx"
      provides: "QUEUE/2 wordmark variants"
      min_lines: 40
    - path: "packages/ui/src/brand/loading.tsx"
      provides: "/2 SVG stroke loading state"
      min_lines: 30
    - path: "packages/ui/src/feedback/toast.tsx"
      provides: "Accessible branded toast foundation"
      min_lines: 50
    - path: "apps/web/src/app/(public)/parear/page.tsx"
      provides: "Custom pairing page surface"
      min_lines: 60
  key_links:
    - from: "apps/web/src/app/globals.css"
      to: "packages/ui/src/tokens.css"
      via: "shared token import or copied token contract"
      pattern: "--primary|oklch"
    - from: "apps/web/src/app/(public)/parear/page.tsx"
      to: "packages/ui/src/brand/wordmark.tsx"
      via: "brand component composition"
      pattern: "Wordmark|QueueWordmark"
---

<objective>
Implement the QUEUE/2 brand primitives and Phase 1 route surfaces.

Purpose: Auth and pairing must feel like QUEUE/2 from the first screen without turning Phase 1 into the full public launch polish phase.
Output: UI package brand components, tokens, loading/toast primitives, public auth pages, pairing page, app shell, empty dashboard, profile and duo surfaces.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-RESEARCH.md
@.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build brand primitives in packages/ui</name>
  <files>packages/ui/src/tokens.css, packages/ui/src/fonts.ts, packages/ui/src/brand/wordmark.tsx, packages/ui/src/brand/mark.tsx, packages/ui/src/brand/loading.tsx, packages/ui/src/feedback/toast.tsx, packages/ui/src/index.ts</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Create QUEUE/2 tokens and components: wordmark single-line and stacked variants, `/2` mark, roulette pointer motif, `/2` SVG stroke loader, and Sonner-based toast wrapper with calm and special variants. Use Archivo Black for display, Inter Tight for body and JetBrains Mono for `/2` and numerals. Keep `packages/ui` free of app domain imports.</action>
  <verify>Run `pnpm --filter @queue/ui typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - `tokens.css` defines `--bg`, `--surface`, `--ink`, `--ink-muted`, `--primary`, `--accent` and rarity tokens in OKLCH.
    - Wordmark exposes single-line and stacked variants.
    - Loading component draws `/2` using SVG stroke animation with reduced-motion fallback.
    - Toast component exposes calm default and restrained special variant.
  </acceptance_criteria>
  <done>Brand primitives are reusable and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Compose public auth and pairing screens</name>
  <files>apps/web/src/app/globals.css, apps/web/src/app/(public)/login/page.tsx, apps/web/src/app/(public)/cadastro/page.tsx, apps/web/src/app/(public)/verificar-email/page.tsx, apps/web/src/app/(public)/recuperar-senha/page.tsx, apps/web/src/app/(public)/parear/page.tsx</files>
  <read_first>.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md, packages/ui/src/index.ts</read_first>
  <action>Create custom Portuguese screens for login, cadastro, verification, password reset and pairing. Forms may call placeholder server actions until Plan 04/05 wires behavior, but they must use stable names and accessible labels that later actions can bind to. Pairing page must support the create-code and enter-code modes, show a large code state, copy action, validity copy and neutral error copy states.</action>
  <verify>Run component tests or Testing Library smoke tests for accessible labels and page rendering.</verify>
  <acceptance_criteria>
    - `/login`, `/cadastro`, `/verificar-email`, `/recuperar-senha` and `/parear` pages exist.
    - Signup screen shows progressive password checklist UI hooks.
    - Verification screen includes resend, correct email and logout actions.
    - Pairing page contains "criar dupla", "entrar com codigo", copy and validity UI.
  </acceptance_criteria>
  <done>Visitors can reach custom auth and pairing pages with QUEUE/2 visual identity.</done>
</task>

<task type="auto">
  <name>Task 3: Build authenticated shell, empty dashboard, profile and duo surfaces</name>
  <files>apps/web/src/components/app-shell.tsx, apps/web/src/app/app/page.tsx, apps/web/src/app/app/perfil/page.tsx, apps/web/src/app/app/dupla/page.tsx, apps/web/tests/brand-ui.test.tsx</files>
  <read_first>.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-CONTEXT.md</read_first>
  <action>Create the Phase 1 authenticated app shell and pages. Dashboard must show duo identity placeholders, empty queue state and the ritual words "descobrir", "sortear" and "zerar". Profile page must reserve display name, active sessions and logout. Duo page must reserve duo name, members, pairing date, timezone and shared preferences. Future capabilities must appear as locked next steps with honest copy and no fake enabled buttons.</action>
  <verify>Run `pnpm --filter @queue/web test` for rendering tests and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Dashboard contains the three-step ritual text exactly.
    - Profile page contains display name and active sessions sections.
    - Duo page contains duo name, members, paired date, timezone and shared preferences sections.
    - Tests verify basic rendering and absence of inaccessible unlabeled form controls.
  </acceptance_criteria>
  <done>Phase 1 app surfaces are ready for Plan 04/05 behavior wiring.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/ui typecheck` passes.
- [ ] `pnpm --filter @queue/web test` passes for brand and route smoke tests.
- [ ] `pnpm check:architecture` passes.
- [ ] UI copy is Brazilian Portuguese and does not expose fake enabled future features.
</verification>

<success_criteria>
- QUEUE/2 has a reusable brand system and Phase 1 route surfaces.
- Auth and pairing pages are custom, accessible and ready for behavior wiring.
- Full landing/PWA polish remains deferred to Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-modular-marca-auth-e-dupla/01-03-SUMMARY.md`.
</output>

