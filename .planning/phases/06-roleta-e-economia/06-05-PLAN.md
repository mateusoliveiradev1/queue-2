---
phase: "06-roleta-e-economia"
plan: "05"
type: "execute"
wave: 4
depends_on:
  - "06-04"
files_modified:
  - "apps/web/src/app/app/roleta/page.tsx"
  - "apps/web/src/modules/roulette/presentation/roulette-reel.tsx"
  - "apps/web/src/modules/roulette/presentation/roulette-audio-control.tsx"
  - "apps/web/src/modules/roulette/presentation/result-panel.tsx"
  - "apps/web/src/modules/roulette/presentation/compact-history.tsx"
  - "apps/web/src/app/globals.css"
  - "apps/web/tests/roulette-ui.test.tsx"
  - "apps/web/tests/phase-6-e2e.spec.ts"
autonomous: true
requirements:
  - "ROUL-01"
  - "ROUL-03"
  - "ROUL-04"
  - "ROUL-05"
  - "ROUL-09"
must_haves:
  truths:
    - "Normal reveal renders exactly 60 stable decorative cover slots with a fixed central pointer."
    - "D-23: on mobile, `/app/roleta` renders a horizontal full-bleed reel with the fixed pointer over the visual center and controls below the reel, never inside a tiny decorative card."
    - "Reveal motion lasts approximately 5.5 seconds with `cubic-bezier(.15,.85,.25,1)`."
    - "D-17: reveal audio uses controlled editorial tension with dry ticks, heavier cadence near the pointer and restrained non-casino fanfare."
    - "D-19: reduced-motion users receive staged reveal states without long reel travel, particles or pulsing."
    - "D-20/ROUL-05: rarity borders use the existing rarity tokens, and Legendary uses contained particles plus static/reduced-motion seal fallback."
    - "Audio is opt-in after interaction, muteable and respects reduced motion/accessibility."
    - "The `/app/roleta` route renders reel, audio, result and compact history from server state/view models instead of leaving components isolated."
    - "D-25: pending result and compact history use commitment/trust copy, not casino, prize or individual economy language."
  artifacts:
    - path: "apps/web/src/modules/roulette/presentation/roulette-reel.tsx"
      provides: "60-cover reel and reduced-motion reveal"
    - path: "apps/web/src/modules/roulette/presentation/roulette-audio-control.tsx"
      provides: "opt-in muteable audio controller"
    - path: "apps/web/src/modules/roulette/presentation/result-panel.tsx"
      provides: "persisted result invitation"
    - path: "apps/web/src/app/app/roleta/page.tsx"
      provides: "route-level component composition"
  key_links:
    - from: "roulette-reel.tsx"
      to: "packages/ui/src/brand/mark.tsx"
      via: "RoulettePointer central marker"
      pattern: "RoulettePointer"
    - from: "apps/web/src/app/app/roleta/page.tsx"
      to: "apps/web/src/modules/roulette/presentation"
      via: "server state/view-model props"
      pattern: "RouletteReel.*RouletteAudioControl.*ResultPanel.*CompactHistory"
---

<objective>
Implement the reveal UI, audio, reduced-motion path, result panel and compact history.

Purpose: The persisted server round needs a memorable but accessible QUEUE/2 ritual.
Output: Reel, audio control, result panel, compact history, roulette CSS and UI/E2E coverage.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-roleta-e-economia/06-UI-SPEC.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/components/action-feedback.tsx
@apps/web/src/modules/discovery/presentation/discovery-deck.tsx
@apps/web/src/modules/discovery/presentation/match-celebration.tsx
@apps/web/src/modules/play/presentation/notification-center.tsx
@apps/web/src/app/app/roleta/page.tsx
@apps/web/src/modules/roulette/presentation/view-models.ts
@packages/ui/src/brand/mark.tsx
@packages/ui/src/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build reel, audio and roulette CSS</name>
  <files>apps/web/src/modules/roulette/presentation/roulette-reel.tsx, apps/web/src/modules/roulette/presentation/roulette-audio-control.tsx, apps/web/src/app/globals.css, apps/web/tests/roulette-ui.test.tsx</files>
  <read_first>
    - `.planning/phases/06-roleta-e-economia/06-UI-SPEC.md`
    - `apps/web/src/modules/discovery/presentation/discovery-deck.tsx`
    - `packages/ui/src/brand/mark.tsx`
    - `packages/ui/src/tokens.css`
    - `apps/web/tests/roulette-ui.test.tsx`
  </read_first>
  <action>
    Build `RouletteReel` as a client component using `motion/react` and `useReducedMotion`. Use `RoulettePointer tone="primary"` as the fixed center marker and `tone="accent"` only for boosted/special states. Render exactly 60 visual slots with `aspect-ratio: 3 / 4`, `aria-hidden` decorative covers and one live result announcement. Each reel slot and result-facing class must carry a textual `data-rarity` state and CSS border token mapping: `common -> var(--rarity-common)`, `rare -> var(--rarity-rare)`, `epic -> var(--rarity-epic)`, `legendary -> var(--rarity-legendary)`. Normal motion uses `5500ms` and `cubic-bezier(.15,.85,.25,1)`. Reduced motion shows `A fila esta escolhendo`, `Resultado guardado`, `Revelado` and skips long travel, particles and pulsing. Implement the D-23 mobile layout in `globals.css`: the `.roulette-reel-band` (or equivalent route-scoped selector) spans full bleed with `width: 100vw`, `margin-inline: calc(50% - 50vw)`, horizontal overflow clipped only by the reel viewport, and no card wrapper; the `.roulette-pointer-anchor` (or equivalent) is `position: absolute`, `left: 50%`, `transform: translateX(-50%)`, centered over the reel; `.roulette-controls` sits below the reel band in normal document flow with 44px+ controls and safe-area spacing. Build `RouletteAudioControl` with synthesized dry ticks, restrained drumroll and short non-casino fanfare only after explicit user interaction. Tick cadence must become heavier near the pointer without slot-machine/jackpot language or casino-style excess. The visible mute control uses UI-SPEC copy, a `defaultEnabled` prop from the server-read duo audio preference and a persist callback that calls the route audio preference action from Plan 06-04. It must not instantiate or start Web Audio on mount, when `defaultEnabled=false`, or before a user gesture. Add roulette-scoped CSS using 4/8/16/24/32/48/64 spacing, typography sizes 14/16/20/40, no negative letter spacing and no full-page violet wash. Add contained `.roulette-legendary-particles` (or equivalent) inside the reel/result area only; `prefers-reduced-motion: reduce` must disable particles and show a static Legendary seal/border instead.
  </action>
  <acceptance_criteria>
    - `rg -n "5500|cubic-bezier\\(\\.15,\\.85,\\.25,1\\)|useReducedMotion|RoulettePointer|Som da roleta ligado|Som da roleta desligado" apps/web/src/modules/roulette/presentation apps/web/src/app/globals.css apps/web/tests/roulette-ui.test.tsx` returns implementation and tests.
    - `rg -n "roulette-reel-band|width:\\s*100vw|margin-inline:\\s*calc\\(50% - 50vw\\)|roulette-pointer-anchor|left:\\s*50%|translateX\\(-50%\\)|roulette-controls" apps/web/src/app/globals.css apps/web/tests/roulette-ui.test.tsx` proves the D-23 full-bleed mobile reel, fixed pointer and controls-below layout.
    - `rg -n "data-rarity|--rarity-common|--rarity-rare|--rarity-epic|--rarity-legendary|legendary-particles|static Legendary|prefers-reduced-motion" apps/web/src/modules/roulette/presentation apps/web/src/app/globals.css apps/web/tests/roulette-ui.test.tsx` proves ROUL-05/D-20 implementation and fallback coverage.
    - `rg -n "dry tick|heavier cadence|near the pointer|restrained|non-casino|fanfare" apps/web/src/modules/roulette/presentation/roulette-audio-control.tsx apps/web/tests/roulette-ui.test.tsx .planning/phases/06-roleta-e-economia/06-05-PLAN.md` proves D-17 cadence is explicit.
    - `rg -n "defaultEnabled|updateRouletteAudioPreferenceAction|AudioContext|no autoplay|audio preference" apps/web/src/modules/roulette/presentation/roulette-audio-control.tsx apps/web/tests/roulette-ui.test.tsx` proves D-18 default, no-autoplay and persist behavior.
    - `rg -n "aria-hidden|aria-live|role=\"region\"|tabIndex" apps/web/src/modules/roulette/presentation/roulette-reel.tsx` returns accessibility hooks.
    - `rg -n "letter-spacing:\\s*-" apps/web/src/app/globals.css` returns no roulette CSS lines.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-ui</automated>
  </verify>
  <done>The route can reveal a persisted result with normal motion/audio and an equivalent reduced-motion flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build result panel, compact history and E2E scaffold coverage</name>
  <files>apps/web/src/app/app/roleta/page.tsx, apps/web/src/modules/roulette/presentation/result-panel.tsx, apps/web/src/modules/roulette/presentation/compact-history.tsx, apps/web/src/app/globals.css, apps/web/tests/roulette-ui.test.tsx, apps/web/tests/phase-6-e2e.spec.ts</files>
  <read_first>
    - `.planning/phases/06-roleta-e-economia/06-UI-SPEC.md`
    - `apps/web/src/modules/discovery/presentation/match-celebration.tsx`
    - `apps/web/src/modules/play/presentation/notification-center.tsx`
    - `apps/web/src/app/app/roleta/page.tsx`
    - `apps/web/src/modules/roulette/presentation/view-models.ts`
    - `apps/web/tests/phase-5-e2e.spec.ts`
  </read_first>
  <action>
    Build `ResultPanel` and `CompactHistory`. The panel must show cover, title, rarity text seal, persisted status, replay disclaimer, action slots for lock/discard and exact invitation copy `A fila apontou para este. Voces travam como Principal?`. ResultPanel must apply the same rarity token border mapping as the reel, communicate rarity by text seal plus border color, and for Legendary results render a contained particle layer in normal motion plus a static Legendary seal/border in reduced motion. Keep lock/discard behavior as props consumed from the route. Compact history must show recent rounds with rarity, boost/pity flags and locked/discarded outcome while staying limited to the compact Phase 6 trust surface. Update `apps/web/src/app/app/roleta/page.tsx` so the authenticated route renders `RouletteReel`, `RouletteAudioControl`, `ResultPanel` and `CompactHistory` from `getRouletteState`/`getRouletteHistory` server state and `view-models.ts` props, passing the server-read duo `audioEnabled` default and audio preference action into `RouletteAudioControl`. The page composition must keep the reel band full-bleed on mobile, keep the fixed pointer centered over the reel, and render the start/boost/audio controls below the reel rather than beside/over it. Extend E2E scaffold for route load, start persisted result, partner resumes same result, replay does not redraw, persisted result visibly appears on `/app/roleta`, mobile full-bleed reel with controls below, mobile no-overlap, Legendary border/particle/static states and reduced-motion path.
  </action>
  <acceptance_criteria>
    - `rg -n "A fila apontou para este|Travar como Principal|Replay nao e novo sorteio|Historico da roleta|Os sorteios aparecem aqui" apps/web/src/modules/roulette apps/web/tests/roulette-ui.test.tsx` returns exact copy.
    - `rg -n "RouletteReel|RouletteAudioControl|ResultPanel|CompactHistory|getRouletteState|getRouletteHistory" apps/web/src/app/app/roleta/page.tsx apps/web/tests/roulette-ui.test.tsx apps/web/tests/phase-6-e2e.spec.ts` proves route-level composition and prevents isolated components.
    - `rg -n "full-bleed|controls below|fixed pointer|no tiny card|roulette-reel-band|roulette-controls" apps/web/src/app/app/roleta/page.tsx apps/web/tests/roulette-ui.test.tsx apps/web/tests/phase-6-e2e.spec.ts` proves D-23 is asserted at route and browser-test level.
    - `rg -n "rarity seal|legendary|Legendary|particle|static seal|reduced motion|data-rarity|--rarity-legendary" apps/web/src/modules/roulette/presentation/result-panel.tsx apps/web/tests/roulette-ui.test.tsx apps/web/tests/phase-6-e2e.spec.ts` proves ROUL-05/D-20 result-state implementation.
    - `rg -n "E2E_READY_USER|E2E_READY_PARTNER|E2E_OTHER_DUO_USER|BLOCKED setup" apps/web/tests/phase-6-e2e.spec.ts` returns fixture guards.
    - `rg -n "jackpot|premio|aposta|sorte grande|casino|comprar|loja|skin|meu XP|seu XP|ranking" apps/web/src/modules/roulette apps/web/src/app/globals.css` returns no visible forbidden copy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-ui</automated>
    <automated>pnpm --filter @queue/web test:e2e -- tests/phase-6-e2e.spec.ts</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Reveal, audio, pending result and compact history UI are rendered by `/app/roleta`; server lock/discard behavior is wired in Plan 06-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client reveal -> persisted round | UI renders a saved result and cannot select or alter it. |
| browser audio -> user device | Audio starts only after interaction and carries no private data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | reel UI | mitigate | Reel consumes persisted entries and announces one server result. |
| T-06-05-02 | Denial of Service | motion/audio | mitigate | Reduced-motion path avoids long travel and audio is muteable. |
| T-06-05-03 | Information Disclosure | compact history | mitigate | History displays only current duo server facts returned by the route. |
| T-06-05-04 | Repudiation | replay copy | mitigate | UI states replay is not a new draw. |
</threat_model>

<verification>
Run roulette UI tests, Phase 6 E2E scaffold and architecture checks.
</verification>

<success_criteria>
- The reveal surface satisfies ROUL-01, ROUL-03, ROUL-04 and ROUL-05.
- ROUL-05/D-20 is implemented with rarity borders, contained Legendary particles and static/reduced-motion fallback.
- D-17 is implemented with restrained editorial audio cadence and a manual validation note in the summary.
- D-23 is implemented: mobile `/app/roleta` uses a horizontal full-bleed reel, fixed centered pointer and controls below the reel.
- Reduced motion and audio behavior are equivalent and accessible.
- `/app/roleta` reveals persisted server results through the composed reel/audio/result/history tree.
- Visible copy matches UI-SPEC and avoids forbidden casino/individual economy language.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-05-SUMMARY.md`.
</output>
