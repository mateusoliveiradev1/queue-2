---
phase: "06-roleta-e-economia"
plan: "06"
type: "execute"
wave: 3
depends_on:
  - "06-03"
files_modified:
  - "apps/web/src/modules/play/application/replace-playing-game.ts"
  - "apps/web/src/modules/play/application/ports.ts"
  - "apps/web/src/modules/play/infrastructure/play-repository.ts"
  - "apps/web/src/modules/play/index.ts"
  - "apps/web/tests/play-application.test.ts"
autonomous: true
requirements:
  - "ROUL-09"
  - "ROUL-10"
  - "SAFE-06"
must_haves:
  truths:
    - "A full Jogando queue requires an explicit selected replacement; nothing pauses automatically."
    - "The existing `play-policy.ts` replacement policy is reused unchanged."
    - "The new Play public contract is narrow and atomic for roulette handoff needs."
  artifacts:
    - path: "apps/web/src/modules/play/application/replace-playing-game.ts"
      provides: "selected replacement use case"
    - path: "apps/web/src/modules/play/index.ts"
      provides: "public replacement wrapper"
    - path: "apps/web/tests/play-application.test.ts"
      provides: "replacement policy coverage"
  key_links:
    - from: "replacePlayingGame"
      to: "apps/web/src/modules/play/domain/play-policy.ts"
      via: "read-only existing `getFourthGameDecision` behavior"
      pattern: "autoPause: false"
---

<objective>
Add the narrow Play public replacement contract needed by roulette.

Purpose: Roulette locking must be able to pause the user-selected current game and activate the result as Principal without deep-importing Play internals.
Output: Public Play replacement use case, repository support and application tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/modules/play/domain/play-policy.ts
@apps/web/src/modules/play/application/activate-playing-game.ts
@apps/web/src/modules/play/application/promote-playing-game.ts
@apps/web/src/modules/play/application/reorder-playing-games.ts
@apps/web/src/modules/play/application/ports.ts
@apps/web/src/modules/play/infrastructure/play-repository.ts
@apps/web/tests/play-application.test.ts
</context>

<interfaces>
`apps/web/src/modules/play/domain/play-policy.ts` is read-only in Phase 6. Reuse existing `getFourthGameDecision` behavior (`availableActions: ["pause", "replace", "cancel"]`, `autoPause: false`) unchanged.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement selected replacement public contract</name>
  <files>apps/web/src/modules/play/application/replace-playing-game.ts, apps/web/src/modules/play/application/ports.ts, apps/web/src/modules/play/infrastructure/play-repository.ts, apps/web/src/modules/play/index.ts, apps/web/tests/play-application.test.ts</files>
  <read_first>
    - `apps/web/src/modules/play/domain/play-policy.ts`
    - `apps/web/src/modules/play/application/activate-playing-game.ts`
    - `apps/web/src/modules/play/application/promote-playing-game.ts`
    - `apps/web/src/modules/play/infrastructure/play-repository.ts`
    - `apps/web/tests/play-application.test.ts`
  </read_first>
  <action>
    Add `replacePlayingGameUseCase` and export wrapper `replacePlayingGame(input: { userId: string; incomingLibraryGameId: string; pausedLibraryGameId: string; makePrincipal: true })`. Resolve membership, verify both library games belong to the current duo, verify the paused game is currently active, pause only the selected active game, activate the incoming game as Principal at position 1, and rewrite remaining active roles/positions so there is exactly one Principal and at most two secondaries. Use one Play-owned transaction. Do not modify `play-policy.ts`; reuse its existing no-auto-pause replacement classification.
  </action>
  <acceptance_criteria>
    - `rg -n "replacePlayingGameUseCase|replacePlayingGame|pausedLibraryGameId|incomingLibraryGameId|makePrincipal" apps/web/src/modules/play apps/web/tests/play-application.test.ts` returns implementation and tests.
    - `rg -n "autoPause:\\s*true|automatically pause|automatic pause" apps/web/src/modules/play apps/web/src/modules/roulette` returns no lines.
    - `git diff -- apps/web/src/modules/play/domain/play-policy.ts` shows no Phase 6 changes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- play-application</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Play exposes an atomic selected-replacement contract that roulette can call through the public entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roulette -> play | Roulette may call only Play's public contract; Play owns active-game mutation. |
| browser -> replacement input | Selected replacement id is untrusted and revalidated server-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Elevation of Privilege | `replacePlayingGameUseCase` | mitigate | Verify both library game ids belong to the current duo and paused game is active. |
| T-06-06-02 | Tampering | active queue | mitigate | One transaction rewrites roles/positions and preserves one Principal/up to three Jogando. |
| T-06-06-03 | Repudiation | replacement action | mitigate | Repository records actor/update facts through existing Play audit patterns. |
</threat_model>

<verification>
Run Play application tests, typecheck and architecture checks.
</verification>

<success_criteria>
- Roulette can request an explicit selected replacement without deep imports.
- `play-policy.ts` remains unchanged and reused as the authoritative no-auto-pause policy.
- Play invariants stay enforced by Play-owned code.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-06-SUMMARY.md`.
</output>
