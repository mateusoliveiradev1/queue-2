---
phase: "06-roleta-e-economia"
plan: "02"
type: "execute"
wave: 1
depends_on:
  - "06-00"
files_modified:
  - "apps/web/src/modules/roulette/index.ts"
  - "apps/web/src/modules/roulette/domain/roulette-policy.ts"
  - "apps/web/src/modules/roulette/application/ports.ts"
  - "apps/web/src/modules/roulette/infrastructure/roulette-repository.ts"
  - "apps/web/tests/roulette-domain.test.ts"
autonomous: true
requirements:
  - "ROUL-01"
  - "ROUL-02"
  - "ROUL-06"
  - "ROUL-07"
  - "ROUL-08"
  - "ROUL-10"
must_haves:
  truths:
    - "D-01/D-02 eligibility uses only Wishlist and Pausado."
    - "D-03/D-04 policy produces 60 visual slots and blocks fewer than 3 eligible games."
    - "D-05/D-06/D-10/D-12/D-13/D-14 constants are explicit and deterministic."
    - "D-08/D-09 boost balance is separate from lifetime XP and level."
  artifacts:
    - path: "apps/web/src/modules/roulette/domain/roulette-policy.ts"
      provides: "pure eligibility, weights, pity, boost, cooldown and reel policies"
    - path: "apps/web/src/modules/roulette/application/ports.ts"
      provides: "repository and use-case contracts"
    - path: "apps/web/src/modules/roulette/index.ts"
      provides: "server-only public module entrypoint"
  key_links:
    - from: "roulette-policy.ts"
      to: "apps/web/tests/roulette-domain.test.ts"
      via: "deterministic policy tests"
      pattern: "ROULETTE_REEL_SLOT_COUNT"
---

<objective>
Create the roulette module contracts and pure policies.

Purpose: Domain rules must be testable without Next.js, React, Drizzle, Better Auth, browser APIs or infrastructure.
Output: Public roulette module entrypoint, pure policy constants/functions, ports and repository skeleton.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-RESEARCH.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/modules/library/domain/library-policy.ts
@apps/web/src/modules/gamification/domain/gamification-policy.ts
@apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
@apps/web/tests/roulette-domain.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pure roulette policy</name>
  <files>apps/web/src/modules/roulette/domain/roulette-policy.ts, apps/web/tests/roulette-domain.test.ts</files>
  <read_first>
    - `.planning/phases/06-roleta-e-economia/06-CONTEXT.md`
    - `.planning/phases/06-roleta-e-economia/06-RESEARCH.md`
    - `apps/web/src/modules/library/domain/library-policy.ts`
    - `apps/web/src/modules/gamification/domain/gamification-policy.ts`
    - `apps/web/tests/roulette-domain.test.ts`
  </read_first>
  <action>
    Implement pure exported constants and functions: `ROULETTE_ELIGIBLE_STATUSES`, `ROULETTE_MINIMUM_ELIGIBLE_GAMES`, `ROULETTE_REEL_SLOT_COUNT`, `ROULETTE_BASE_WEIGHTS`, `ROULETTE_BOOSTED_WEIGHTS`, `ROULETTE_PITY_THRESHOLD`, `ROULETTE_BOOST_COST`, `ROULETTE_BOOST_BALANCE_CAP`, `ROULETTE_COOLDOWN_ROUNDS`, `ROULETTE_COOLDOWN_MULTIPLIER`, `ROULETTE_WEEKEND_BOOST_EARN_MULTIPLIER`, `getEligiblePoolPolicy`, `selectRouletteResult`, `buildVisualReel`, `applyPityTransition`, `calculateBoostEarnAmount`, `applyBoostCostPolicy`, `applyCooldownWeights` and `getRoundResumePolicy`. Use rarity names exactly `common`, `rare`, `epic`, `legendary`. Accept injected rolls/seeds so tests assert exact outcomes.
  </action>
  <acceptance_criteria>
    - `rg -n "ROULETTE_ELIGIBLE_STATUSES|ROULETTE_REEL_SLOT_COUNT|ROULETTE_BOOST_COST|ROULETTE_PITY_THRESHOLD|ROULETTE_BOOST_BALANCE_CAP|ROULETTE_WEEKEND_BOOST_EARN_MULTIPLIER" apps/web/src/modules/roulette/domain/roulette-policy.ts` returns all constants.
    - `rg -n "selectRouletteResult|buildVisualReel|applyPityTransition|calculateBoostEarnAmount|applyCooldownWeights" apps/web/src/modules/roulette/domain/roulette-policy.ts` returns implemented pure functions.
    - `rg -n "from \"(next|react|drizzle-orm|better-auth|server-only|@queue/db)" apps/web/src/modules/roulette/domain/roulette-policy.ts` returns no lines.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-domain</automated>
  </verify>
  <done>Domain policy tests pass and all constants selected in `06-RESEARCH.md` are executable.</done>
</task>

<task type="auto">
  <name>Task 2: Add public contracts and repository shell</name>
  <files>apps/web/src/modules/roulette/index.ts, apps/web/src/modules/roulette/application/ports.ts, apps/web/src/modules/roulette/infrastructure/roulette-repository.ts</files>
  <read_first>
    - `apps/web/src/modules/gamification/infrastructure/gamification-repository.ts`
    - `apps/web/src/modules/play/infrastructure/play-repository.ts`
    - `apps/web/src/modules/play/application/ports.ts`
    - `.planning/ARCHITECTURE.md`
  </read_first>
  <action>
    Create `index.ts` with `import "server-only"` and export only public wrappers/types. Define `RouletteRepository` and `RouletteRepositoryTransaction` in `application/ports.ts` for membership resolution, eligible pool reads, active round reads, boost materialization, round persistence, replay, lock/discard and history reads. Add `createRouletteRepository(pool)` and `rouletteRepository` shell using the existing `withAppUserTransaction(userId, callback)` pattern; transaction internals are completed in Plan 06-03.
  </action>
  <acceptance_criteria>
    - `rg -n "import \"server-only\"|getRouletteState|startRouletteRound|replayRouletteRound|lockRouletteResultAsPrincipal|discardRouletteResult" apps/web/src/modules/roulette/index.ts apps/web/src/modules/roulette/application/ports.ts` returns public contracts.
    - `rg -n "withUserTransaction|set_config\\('queue2.user_id'" apps/web/src/modules/roulette/infrastructure/roulette-repository.ts` returns transaction identity wiring.
    - `rg -n "from \"\\.\\/infrastructure" apps/web/src/modules/roulette/domain apps/web/src/modules/roulette/application 2>$null` returns no domain/application dependency on infrastructure.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Roulette has a public module boundary and contracts ready for transaction implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route/application -> domain | Domain policy receives typed server data and injected rolls, not browser authority. |
| roulette -> repository | Application ports define allowed persistence operations before SQL is written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `roulette-policy.ts` | mitigate | Policy ignores client result/economy facts and is covered by deterministic tests. |
| T-06-02-02 | Elevation of Privilege | module boundary | mitigate | Public entrypoint exports use cases only; architecture check blocks deep imports. |
| T-06-02-03 | Repudiation | constants | mitigate | Constants are named, tested and later validated by simulation output. |
</threat_model>

<verification>
Run roulette domain tests, web typecheck and architecture checks.
</verification>

<success_criteria>
- Pure roulette rules are green and framework-free.
- Public contracts are explicit.
- Module scope stays limited to Phase 6 roulette and economy behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-02-SUMMARY.md`.
</output>
