---
phase: "06-roleta-e-economia"
plan: "03"
type: "execute"
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - "apps/web/src/modules/roulette/application/get-roulette-state.ts"
  - "apps/web/src/modules/roulette/application/get-roulette-history.ts"
  - "apps/web/src/modules/roulette/application/start-roulette-round.ts"
  - "apps/web/src/modules/roulette/application/replay-roulette-round.ts"
  - "apps/web/src/modules/roulette/application/ports.ts"
  - "apps/web/src/modules/roulette/infrastructure/roulette-repository.ts"
  - "apps/web/src/modules/roulette/index.ts"
  - "apps/web/tests/roulette-application.test.ts"
  - "packages/db/tests/roulette-concurrency.test.ts"
autonomous: true
requirements:
  - "ROUL-02"
  - "ROUL-06"
  - "ROUL-07"
  - "ROUL-08"
  - "ROUL-10"
  - "SAFE-06"
must_haves:
  truths:
    - "D-16: a start request returns an existing active/pending round before creating a new draw."
    - "D-21: result, boost spend, pity transition, cooldown and 60-slot snapshot persist before reveal and are shared/resumable for both members."
    - "Boost is materialized from `app.duo_xp_awards` through append-only ledger entries."
    - "D-07: an unaccepted revealed result remains a pending invitation and does not move library status automatically."
    - "D-11: state/view models expose clear pity progress copy without formulas or heavy math."
    - "D-15: pre-persistence boost failure refunds automatically, while post-persistence failure resumes the saved round."
    - "D-22: replay returns the persisted round and never redraws."
    - "D-24: backgrounding, disconnects and refreshes resume authoritative state instead of offering a new spin."
    - "Roulette state includes the current duo audio preference for D-18 before UI audio controls render."
  artifacts:
    - path: "apps/web/src/modules/roulette/application/start-roulette-round.ts"
      provides: "authoritative start transaction use case"
    - path: "apps/web/src/modules/roulette/application/get-roulette-state.ts"
      provides: "blocked/ready/resumable/pending state read model"
    - path: "apps/web/src/modules/roulette/infrastructure/roulette-repository.ts"
      provides: "Postgres transaction implementation"
  key_links:
    - from: "startRouletteRoundUseCase"
      to: "app.roulette_round_entries"
      via: "persisted 60-slot snapshot"
      pattern: "roulette_round_entries"
---

<objective>
Implement authoritative roulette round creation and state reads.

Purpose: The server must select and persist the result/economy facts before any browser animation starts.
Output: State/history/replay/start use cases, transaction repository methods and focused application/concurrency tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-RESEARCH.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/modules/roulette/domain/roulette-policy.ts
@apps/web/src/modules/roulette/application/ports.ts
@apps/web/src/modules/roulette/infrastructure/roulette-repository.ts
@apps/web/tests/roulette-application.test.ts
@packages/db/tests/roulette-concurrency.test.ts
</context>

<interfaces>
Public functions after this plan:
- `getRouletteState(input: { userId: string })`
- `getRouletteHistory(input: { userId: string; limit?: number })`
- `startRouletteRound(input: { userId: string; idempotencyKey: string; useBoost: boolean })`
- `replayRouletteRound(input: { userId: string; roundId: string })`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement state, history and replay use cases</name>
  <files>apps/web/src/modules/roulette/application/get-roulette-state.ts, apps/web/src/modules/roulette/application/get-roulette-history.ts, apps/web/src/modules/roulette/application/replay-roulette-round.ts, apps/web/src/modules/roulette/application/ports.ts, apps/web/src/modules/roulette/index.ts, apps/web/tests/roulette-application.test.ts</files>
  <read_first>
    - `apps/web/src/modules/roulette/application/ports.ts`
    - `apps/web/src/modules/roulette/domain/roulette-policy.ts`
    - `apps/web/tests/roulette-application.test.ts`
    - `apps/web/src/modules/library/application/get-library-queue.ts`
    - `apps/web/src/modules/gamification/application/get-gamification-dashboard.ts`
  </read_first>
  <action>
    Add state, history and replay use cases. State must resolve membership from `userId`, read eligible `wishlist`/`pausado` games, return `blocked-pool` with semantic CTAs when fewer than 3 are eligible, return active/revealing/pending rounds before ready state, expose simple pity progress copy without formula details, include boost balance/cap from server state, and include `audioEnabled` from the existing duo preference row for D-18. Replay must require a persisted round, set `isReplay: true`, and must not select, spend, refund or update pity/history.
  </action>
  <acceptance_criteria>
    - `rg -n "blocked-pool|pending_invitation|isReplay|getRouletteStateUseCase|getRouletteHistoryUseCase|replayRouletteRoundUseCase" apps/web/src/modules/roulette/application apps/web/tests/roulette-application.test.ts` returns matches.
    - `rg -n "audioEnabled|duo_preferences|D-18|audio preference" apps/web/src/modules/roulette/application apps/web/src/modules/roulette/infrastructure/roulette-repository.ts apps/web/tests/roulette-application.test.ts` proves roulette state reads the duo audio preference from the server.
    - `rg -n "wishlist|pausado" apps/web/src/modules/roulette/application/get-roulette-state.ts` returns eligible status filtering.
    - `rg -n "Descobrir jogos|Buscar no Catalogo|Abrir Biblioteca" apps/web/src/modules/roulette/application` returns no UI copy in application files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-application</automated>
  </verify>
  <done>State, history and replay are exported through the public module and covered by fake-repository tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement idempotent start transaction</name>
  <files>apps/web/src/modules/roulette/application/start-roulette-round.ts, apps/web/src/modules/roulette/application/ports.ts, apps/web/src/modules/roulette/infrastructure/roulette-repository.ts, apps/web/src/modules/roulette/index.ts, apps/web/tests/roulette-application.test.ts, packages/db/tests/roulette-concurrency.test.ts</files>
  <read_first>
    - `apps/web/src/modules/roulette/domain/roulette-policy.ts`
    - `apps/web/src/modules/roulette/infrastructure/roulette-repository.ts`
    - `apps/web/src/modules/gamification/infrastructure/gamification-repository.ts`
    - `packages/db/tests/roulette-concurrency.test.ts`
  </read_first>
  <action>
    Implement `startRouletteRoundUseCase` and transaction methods. The transaction must resolve membership from `userId`, lock roulette balance/pity/active-round state, materialize missing boost earn ledger entries from `app.duo_xp_awards` with `earn:` keys, calculate earn as `floor(amount * 0.2 * weekendFactor)` capped at `600`, use weekend factor `1.2` on Saturday/Sunday in duo timezone, return existing active/revealing/pending rounds before new selection, spend boost with `spend:` idempotency key only when requested, compute server selection from base/boost/pity/cooldown, persist `roulette_rounds`, all `roulette_round_entries`, pity transition, cooldown and `roulette_history_events` before returning. A pre-persistence boosted failure must refund; a post-persistence failure must resume.
  </action>
  <acceptance_criteria>
    - `rg -n "startRouletteRoundUseCase|idempotencyKey|spend:|earn:|duo_xp_awards|FOR UPDATE|roulette_round_entries|roulette_history_events" apps/web/src/modules/roulette` returns implementation.
    - `rg -n "app.duos.xp|UPDATE app.duos|level" apps/web/src/modules/roulette/infrastructure/roulette-repository.ts` returns no lifetime XP/level mutation.
    - `rg -n "resultCatalogGameId|resultLibraryGameId|pity|boostBalance|duoId" apps/web/src/modules/roulette/application/start-roulette-round.ts` shows no trusted client-supplied authority fields.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-application</automated>
    <automated>pnpm --filter @queue/db test:integration -- roulette-concurrency</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Round start is server-authoritative, idempotent and concurrency-safe; boost/pity/history apply exactly once per persisted result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> start use case | `idempotencyKey` and `useBoost` are proposals; result/economy facts are server-derived. |
| roulette transaction -> Postgres | RLS, row locks and unique indexes enforce state under concurrency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | `startRouletteRoundUseCase` | mitigate | Ignore browser result, duoId, pity, balance and weekend inputs. |
| T-06-03-02 | Denial of Service | concurrent starts | mitigate | Row locks plus partial unique index and idempotency keys converge repeated requests. |
| T-06-03-03 | Repudiation | boost/pity/history | mitigate | Unique ledger/history keys record actor, round and source facts. |
| T-06-03-04 | Information Disclosure | state/history reads | mitigate | Membership and forced RLS scope all reads to the current duo. |
</threat_model>

<verification>
Run roulette application tests, DB concurrency tests, typecheck and architecture checks.
</verification>

<success_criteria>
- Result is persisted before reveal and remains resumable.
- Boost balance materializes from XP facts through append-only ledger entries.
- Pity, cooldown, history and boost spend cannot duplicate under replay or concurrency.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-03-SUMMARY.md`.
</output>
