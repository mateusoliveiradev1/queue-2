---
phase: "06-roleta-e-economia"
plan: "01"
type: "execute"
wave: 1
depends_on:
  - "06-00"
files_modified:
  - "packages/db/src/schema/app.ts"
  - "packages/db/src/rls/policies.sql"
  - "packages/db/src/migrations/0015_roulette_core.sql"
  - "packages/db/src/migrations/meta/_journal.json"
  - "packages/db/tests/roulette-migrations.test.ts"
  - "packages/db/tests/roulette-rls.test.ts"
  - "packages/db/tests/roulette-concurrency.test.ts"
autonomous: true
requirements:
  - "ROUL-09"
  - "ROUL-02"
  - "ROUL-06"
  - "ROUL-07"
  - "ROUL-08"
  - "ROUL-10"
  - "SAFE-06"
must_haves:
  truths:
    - "Roulette result, boost, pity, cooldown and history facts are persisted before browser reveal."
    - "Every roulette table is duo-scoped with forced RLS and least-privileged grants."
    - "The database enforces one active/revealing/pending invitation per duo."
    - "Central da Dupla storage accepts roulette lock/discard operational notification types before server use cases emit them."
  artifacts:
    - path: "packages/db/src/migrations/0015_roulette_core.sql"
      provides: "roulette schema, constraints, RLS, grants and comments"
    - path: "packages/db/src/schema/app.ts"
      provides: "Drizzle table mirrors"
    - path: "packages/db/tests/roulette-concurrency.test.ts"
      provides: "exactly-once concurrency coverage"
    - path: "packages/db/tests/roulette-migrations.test.ts"
      provides: "Play notification constraint coverage for roulette outcomes"
  key_links:
    - from: "packages/db/src/rls/policies.sql"
      to: "app.has_duo_membership"
      via: "forced RLS membership policies"
      pattern: "roulette_.*FORCE ROW LEVEL SECURITY"
---

<objective>
Create the roulette database and RLS foundation.

Purpose: Phase 6 requires durable server-owned roulette facts before domain and UI work can depend on them.
Output: SQL migration, Drizzle mirrors, RLS policies, grants and DB test coverage.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-RESEARCH.md
@.planning/phases/06-roleta-e-economia/06-VALIDATION.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@packages/db/src/schema/app.ts
@packages/db/src/rls/policies.sql
@packages/db/src/migrations/meta/_journal.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add roulette schema, migration and RLS</name>
  <files>packages/db/src/schema/app.ts, packages/db/src/rls/policies.sql, packages/db/src/migrations/0015_roulette_core.sql, packages/db/src/migrations/meta/_journal.json, packages/db/tests/roulette-migrations.test.ts, packages/db/tests/roulette-rls.test.ts, packages/db/tests/roulette-concurrency.test.ts</files>
  <read_first>
    - `packages/db/src/schema/app.ts`
    - `packages/db/src/rls/policies.sql`
    - `packages/db/src/migrations/0011_gamification_core.sql`
    - `packages/db/src/migrations/0014_gamification_catalog_seeds.sql`
    - `packages/db/tests/roulette-migrations.test.ts`
    - `packages/db/tests/roulette-rls.test.ts`
    - `packages/db/tests/roulette-concurrency.test.ts`
  </read_first>
  <action>
    Implement `app.roulette_boost_balances`, `app.roulette_boost_ledger`, `app.roulette_pity_state`, `app.roulette_rounds`, `app.roulette_round_entries`, `app.roulette_cooldowns` and `app.roulette_history_events`. Use `duo_id`, actor fields, timestamps, comments, hot-path indexes and least-privileged grants. Add a partial unique index on `roulette_rounds(duo_id)` where status is `active`, `revealing` or `pending_invitation`, unique `(duo_id, idempotency_key)`, unique `(duo_id, ledger_key)`, balance cap `600`, non-zero signed ledger deltas, non-negative pity/cooldown checks and forced RLS membership policies for every roulette table. Update the existing `app.play_notifications` type check in `packages/db/src/schema/app.ts` and `0015_roulette_core.sql` so Central da Dupla can store `roulette-result-locked` and `roulette-result-discarded` facts required by D-31; do not create a parallel roulette notification table.
  </action>
  <acceptance_criteria>
    - `rg -n "roulette_boost_balances|roulette_boost_ledger|roulette_pity_state|roulette_rounds|roulette_round_entries|roulette_cooldowns|roulette_history_events" packages/db/src/schema/app.ts packages/db/src/migrations/0015_roulette_core.sql` returns every table.
    - `rg -n "ENABLE ROW LEVEL SECURITY|FORCE ROW LEVEL SECURITY|has_duo_membership" packages/db/src/rls/policies.sql | rg "roulette"` returns policy lines for every roulette table.
    - `rg -n "0015_roulette_core" packages/db/src/migrations/meta/_journal.json` returns one journal entry.
    - `rg -n "queue2_app_runtime|queue2_worker|queue2_readonly|COMMENT ON TABLE app.roulette" packages/db/src/migrations/0015_roulette_core.sql` returns grants and comments.
    - `rg -n "roulette-result-locked|roulette-result-discarded|app_play_notifications_type_chk" packages/db/src/schema/app.ts packages/db/src/migrations/0015_roulette_core.sql packages/db/tests/roulette-migrations.test.ts` returns the Central notification constraint update and test.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/db drizzle:generate</automated>
    <automated>pnpm --filter @queue/db typecheck</automated>
    <automated>pnpm --filter @queue/db drizzle:migrate</automated>
    <automated>pnpm --filter @queue/db test:integration -- roulette-migrations roulette-rls roulette-concurrency</automated>
  </verify>
  <done>Schema, migration journal, RLS and grants are present; missing DB URLs are recorded as named BLOCKED evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roulette module -> Postgres | Runtime writes use user-scoped transactions and forced RLS. |
| boost ledger -> XP facts | Boost balance derives from XP facts without mutating lifetime XP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | roulette tables | mitigate | Add `duo_id`, forced RLS and cross-duo DB tests for every table. |
| T-06-01-02 | Tampering | `roulette_rounds` | mitigate | Partial unique index enforces one active/revealing/pending round per duo. |
| T-06-01-03 | Repudiation | boost/history facts | mitigate | Append-only ledgers include actor, source, round and unique keys. |
| T-06-01-04 | Elevation of Privilege | runtime grants | mitigate | Runtime grants exclude owner/migrator privileges and `BYPASSRLS`. |
| T-06-01-05 | Tampering | `play_notifications` constraint | mitigate | Extend the existing type check only for locked/discarded roulette operational facts and cover it in migration tests. |
</threat_model>

<verification>
Run DB generation, migration, typecheck and focused roulette integration tests. Missing `DATABASE_URL` or `TEST_DATABASE_URL` is blocker evidence, not success.
</verification>

<success_criteria>
- Roulette persistence enforces server authority, RLS isolation and exactly-once economy/history invariants.
- Existing Central da Dupla notification storage accepts roulette lock/discard outcome facts without adding a second notification surface.
- Drizzle schema and SQL migration match.
- Plan 06-03 can implement transactions against stable DB objects.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-01-SUMMARY.md`.
</output>
