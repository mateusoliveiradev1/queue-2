---
phase: "06-roleta-e-economia"
plan: "04"
type: "execute"
wave: 3
depends_on:
  - "06-03"
files_modified:
  - "apps/web/src/app/app/roleta/page.tsx"
  - "apps/web/src/app/app/roleta/actions.ts"
  - "apps/web/src/modules/roulette/presentation/view-models.ts"
  - "apps/web/src/modules/duo/application/update-duo-audio-preference.ts"
  - "apps/web/src/modules/duo/application/ports.ts"
  - "apps/web/src/modules/duo/infrastructure/duo-repository.ts"
  - "apps/web/src/modules/duo/index.ts"
  - "apps/web/src/components/app-shell.tsx"
  - "apps/web/src/app/globals.css"
  - "apps/web/tests/roulette-ui.test.tsx"
  - "apps/web/tests/duo-flow.test.ts"
autonomous: true
requirements:
  - "ROUL-01"
  - "ROUL-02"
  - "ROUL-04"
must_haves:
  truths:
    - "`/app/roleta` is an authenticated playable surface, not a landing page."
    - "Navigation exposes Roleta after Biblioteca and before Conquistas."
    - "Nine authenticated mobile nav items remain readable with 44px+ touch targets and no clipping."
    - "Start/replay actions use authoritative server sessions and never trust client-owned result/economy fields."
    - "Duo audio preference is read from server state and persisted by an authoritative public Duo contract."
  artifacts:
    - path: "apps/web/src/app/app/roleta/page.tsx"
      provides: "authenticated roulette route composition"
    - path: "apps/web/src/app/app/roleta/actions.ts"
      provides: "start/replay server actions"
    - path: "apps/web/src/modules/roulette/presentation/view-models.ts"
      provides: "UI-state mapping and exact copy values"
    - path: "apps/web/src/app/globals.css"
      provides: "mobile app-shell nav layout for nine items"
    - path: "apps/web/src/modules/duo/application/update-duo-audio-preference.ts"
      provides: "public Duo audio preference mutation"
  key_links:
    - from: "page.tsx"
      to: "apps/web/src/modules/roulette/index.ts"
      via: "getRouletteState/getRouletteHistory public API"
      pattern: "getRouletteState"
    - from: "actions.ts"
      to: "apps/web/src/modules/duo/index.ts"
      via: "updateDuoAudioPreference public API"
      pattern: "updateDuoAudioPreference"
---

<objective>
Build the authenticated `/app/roleta` route shell and route actions.

Purpose: The duo needs a real route that renders server-owned roulette state before reveal components are layered in.
Output: Route, start/replay/audio-preference actions, view models, navigation entry, mobile nav CSS and route-level UI tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-roleta-e-economia/06-UI-SPEC.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/app/app/descobrir/page.tsx
@apps/web/src/app/app/descobrir/actions.ts
@apps/web/src/components/app-shell.tsx
@apps/web/src/modules/roulette/index.ts
@apps/web/src/modules/duo/index.ts
@apps/web/src/modules/duo/application/update-duo-settings.ts
@apps/web/src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route, view models and navigation</name>
  <files>apps/web/src/app/app/roleta/page.tsx, apps/web/src/modules/roulette/presentation/view-models.ts, apps/web/src/components/app-shell.tsx, apps/web/src/app/globals.css, apps/web/tests/roulette-ui.test.tsx</files>
  <read_first>
    - `.planning/phases/06-roleta-e-economia/06-UI-SPEC.md`
    - `apps/web/src/app/app/descobrir/page.tsx`
    - `apps/web/src/components/app-shell.tsx`
    - `apps/web/src/app/globals.css`
    - `apps/web/src/modules/roulette/index.ts`
    - `apps/web/tests/roulette-ui.test.tsx`
  </read_first>
  <action>
    Create `/app/roleta` as an authenticated route using `requireVerifiedSession()`, `getRouletteState` and `getRouletteHistory`. Render exactly one first-viewport state: blocked pool, ready roulette, resumable reveal, pending invitation or history-backed empty trust state. Add `Roleta` to `AppShellPage` and navigation order `Fila`, `Catalogo`, `Descobrir`, `Biblioteca`, `Roleta`, `Conquistas`, `Desafios`, `Dupla`, `Perfil`. Update the app-shell mobile bottom nav CSS in `globals.css` so the nine-item track is horizontally scrollable with stable item sizing: track `overflow-x: auto`, each nav item `min-width: 72px`, `min-height: 52px`, readable labels, 44px+ target area and `env(safe-area-inset-bottom)` padding so labels never overlap or clip. Add view-model builders that provide the exact UI-SPEC copy for route title, helper, blocked state, boost, pity, persisted status, history labels and the server-read `audioEnabled` default from Plan 06-03.
  </action>
  <acceptance_criteria>
    - `rg -n "Roleta da dupla|A fila escolhe agora|A roleta usa Wishlist e Pausado|A roleta precisa de tres jogos|Sortear da fila" apps/web/src/app/app/roleta apps/web/src/modules/roulette/presentation apps/web/tests/roulette-ui.test.tsx` returns exact copy.
    - `rg -n "roleta|Roleta" apps/web/src/components/app-shell.tsx` shows the type and navigation entry.
    - `rg -n "requireVerifiedSession|getRouletteState|getRouletteHistory|currentPage=\"roleta\"" apps/web/src/app/app/roleta/page.tsx` returns route composition.
    - `rg -n "overflow-x:\\s*auto|min-width:\\s*72px|min-height:\\s*52px|safe-area-inset-bottom|scroll-padding" apps/web/src/app/globals.css` returns the nine-item mobile nav layout.
    - `pnpm --filter @queue/web test -- roulette-ui` asserts the mobile nav renders all nine labels with 44px+ targets and no overlap/clipping.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-ui</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
  </verify>
  <done>The route is reachable through authenticated navigation and renders server-derived roulette states with exact Portuguese copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add start, replay and audio preference actions</name>
  <files>apps/web/src/app/app/roleta/actions.ts, apps/web/src/modules/duo/application/update-duo-audio-preference.ts, apps/web/src/modules/duo/application/ports.ts, apps/web/src/modules/duo/infrastructure/duo-repository.ts, apps/web/src/modules/duo/index.ts, apps/web/tests/roulette-ui.test.tsx, apps/web/tests/duo-flow.test.ts</files>
  <read_first>
    - `apps/web/src/app/app/descobrir/actions.ts`
    - `apps/web/src/modules/roulette/index.ts`
    - `apps/web/src/modules/duo/application/update-duo-settings.ts`
    - `apps/web/src/modules/duo/infrastructure/duo-repository.ts`
    - `.planning/SECURITY.md`
  </read_first>
  <action>
    Add `startRouletteRoundAction` and `replayRouletteRoundAction` using `requireAuthoritativeVerifiedSession()`. Start may accept only `idempotencyKey` and `useBoost`; replay may accept only `roundId`. Add a narrow Duo public contract `updateDuoAudioPreference(input: { userId: string; audioEnabled: boolean })` that resolves membership, updates only `app.duo_preferences.audio_enabled` through the existing Duo repository/RLS path and is exported from `apps/web/src/modules/duo/index.ts`. Add `updateRouletteAudioPreferenceAction` in `roleta/actions.ts` using `requireAuthoritativeVerifiedSession()` and that public Duo contract; it may accept only `audioEnabled`. Actions must return serializable result objects for enhanced forms and must not trust FormData for `duoId`, selected result, pity, boost balance, result rarity, cost or weekend context.
  </action>
  <acceptance_criteria>
    - `rg -n "requireAuthoritativeVerifiedSession|startRouletteRound|replayRouletteRound" apps/web/src/app/app/roleta/actions.ts` returns server action wiring.
    - `rg -n "updateDuoAudioPreference|updateRouletteAudioPreferenceAction|audioEnabled|audio_enabled" apps/web/src/app/app/roleta/actions.ts apps/web/src/modules/duo apps/web/tests/duo-flow.test.ts apps/web/tests/roulette-ui.test.tsx` returns the D-18 persistence path and tests.
    - `rg -n "duoId|resultLibraryGameId|resultCatalogGameId|pity|boostBalance|resultRarity" apps/web/src/app/app/roleta/actions.ts` shows no trusted FormData extraction for server-owned facts.
    - `pnpm --filter @queue/web test -- roulette-ui duo-flow` covers busy feedback, action return shape and authoritative audio preference persistence.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-ui duo-flow</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Start, replay and audio preference paths are route-wired without shifting result/economy or duo preference authority to the browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> route actions | FormData is untrusted and limited to idempotency/replay proposals. |
| route -> roulette module | Route composes public use cases and contains no business rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | `actions.ts` | mitigate | Ignore result, duoId, pity and balance inputs; derive session server-side. |
| T-06-04-02 | Spoofing | `/app/roleta` route | mitigate | Page requires verified session and duo state from public roulette API. |
| T-06-04-03 | Information Disclosure | navigation/route | mitigate | Route reads only current duo state under existing auth and RLS paths. |
| T-06-04-04 | Tampering | audio preference action | mitigate | Persist audio preference through the Duo public contract with authoritative session checks and no client duo id. |
</threat_model>

<verification>
Run roulette UI tests, web typecheck and architecture checks.
</verification>

<success_criteria>
- `/app/roleta` exists and is navigable.
- Mobile app-shell navigation can hold nine items with readable labels, no overlap/clipping and 44px+ touch targets.
- Route actions can persist D-18 audio preference through a public Duo contract.
- The route surfaces server state and exact UI-SPEC copy.
- Start/replay server actions do not accept client authority over result/economy facts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-04-SUMMARY.md`.
</output>
