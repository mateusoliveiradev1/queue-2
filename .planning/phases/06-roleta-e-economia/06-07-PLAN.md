---
phase: "06-roleta-e-economia"
plan: "07"
type: "execute"
wave: 4
depends_on:
  - "06-03"
  - "06-06"
files_modified:
  - "apps/web/src/modules/roulette/application/lock-roulette-result-as-principal.ts"
  - "apps/web/src/modules/roulette/application/discard-roulette-result.ts"
  - "apps/web/src/modules/roulette/application/ports.ts"
  - "apps/web/src/modules/roulette/infrastructure/roulette-repository.ts"
  - "apps/web/src/modules/roulette/index.ts"
  - "apps/web/src/modules/play/application/create-operational-notification.ts"
  - "apps/web/src/modules/play/infrastructure/play-repository.ts"
  - "apps/web/src/modules/play/index.ts"
  - "apps/web/tests/roulette-application.test.ts"
  - "apps/web/tests/play-application.test.ts"
autonomous: true
requirements:
  - "ROUL-09"
  - "ROUL-10"
  - "SAFE-06"
must_haves:
  truths:
    - "D-26: any duo member can lock the persisted result as Principal with actor/audit information."
    - "D-27: a full Jogando queue returns replacement-required without changing state before selection."
    - "D-29: new roulette rounds remain blocked until the pending invitation is locked or discarded."
    - "D-30: discard is non-punitive, closes the invitation, records history/cooldown and does not refund a persisted boost spend."
    - "D-31: lock/discard server behavior creates Central da Dupla notification facts and attempts push only through existing preference-gated Play paths."
  artifacts:
    - path: "apps/web/src/modules/roulette/application/lock-roulette-result-as-principal.ts"
      provides: "lock result use case"
    - path: "apps/web/src/modules/roulette/application/discard-roulette-result.ts"
      provides: "discard result use case"
    - path: "apps/web/tests/roulette-application.test.ts"
      provides: "lock/discard idempotency coverage"
    - path: "apps/web/src/modules/play/application/create-operational-notification.ts"
      provides: "public Central da Dupla notification collaborator for roulette outcomes"
  key_links:
    - from: "lockRouletteResultAsPrincipalUseCase"
      to: "apps/web/src/modules/play/index.ts"
      via: "public `replacePlayingGame` / activation contract"
      pattern: "replacePlayingGame"
    - from: "lock/discard roulette use cases"
      to: "apps/web/src/modules/play/index.ts"
      via: "public `createOperationalPlayNotification` contract"
      pattern: "roulette-result-locked|roulette-result-discarded"
---

<objective>
Implement roulette lock and discard application behavior.

Purpose: A persisted roulette result becomes a real shared commitment only through server-authoritative invitation resolution.
Output: Lock/discard use cases, repository transitions, public exports and application tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/06-roleta-e-economia/06-CONTEXT.md
@.planning/phases/06-roleta-e-economia/06-PATTERNS.md
@apps/web/src/modules/roulette/application/ports.ts
@apps/web/src/modules/roulette/infrastructure/roulette-repository.ts
@apps/web/src/modules/play/index.ts
@apps/web/src/modules/play/domain/play-policy.ts
@apps/web/src/modules/play/application/ports.ts
@apps/web/src/modules/library/application/move-library-game.ts
@apps/web/tests/roulette-application.test.ts
@apps/web/tests/play-application.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lock and discard use cases</name>
  <files>apps/web/src/modules/roulette/application/lock-roulette-result-as-principal.ts, apps/web/src/modules/roulette/application/discard-roulette-result.ts, apps/web/src/modules/roulette/application/ports.ts, apps/web/src/modules/roulette/infrastructure/roulette-repository.ts, apps/web/src/modules/roulette/index.ts, apps/web/src/modules/play/application/create-operational-notification.ts, apps/web/src/modules/play/infrastructure/play-repository.ts, apps/web/src/modules/play/index.ts, apps/web/tests/roulette-application.test.ts, apps/web/tests/play-application.test.ts</files>
  <read_first>
    - `apps/web/src/modules/roulette/application/ports.ts`
    - `apps/web/src/modules/roulette/infrastructure/roulette-repository.ts`
    - `apps/web/src/modules/play/index.ts`
    - `apps/web/src/modules/play/domain/play-policy.ts`
    - `apps/web/src/modules/play/application/ports.ts`
    - `apps/web/tests/roulette-application.test.ts`
    - `apps/web/tests/play-application.test.ts`
  </read_first>
  <action>
    Implement `lockRouletteResultAsPrincipalUseCase` and `discardRouletteResultUseCase`. Lock must verify membership, pending invitation status, result library ownership and actor user id. It must call Play/Library only through public contracts. Add a Play-owned public collaborator `createOperationalPlayNotification(input)` backed by `apps/web/src/modules/play/application/create-operational-notification.ts`; it must validate notification type with a narrow local union containing exactly `roulette-result-locked` and `roulette-result-discarded`, rely on the `app_play_notifications_type_chk` database constraint added in Plan 06-01, insert Central da Dupla facts through existing `play-repository.ts` notification persistence methods, use unique action refs for idempotency and attempt push only through existing product-push preference/subscription checks. Do not modify `apps/web/src/modules/play/application/ports.ts`; if the existing Play repository contract needs a type helper, define it locally in `create-operational-notification.ts` and keep the public wrapper in `play/index.ts`. `play-policy.ts` is read-only in this plan and remains the source reference for the existing no-auto-pause replacement behavior only. When the active queue has space, lock moves the selected result to `jogando` as Principal, closes the round, writes handoff history, creates `roulette-result-locked` notification facts server-side per D-31, and returns redirect `/app?estado=roleta-principal`. When the active queue is full, return `replacement-required` with current active games and `autoPause: false`; if `replacementLibraryGameId` is provided, call the public Play replacement contract from Plan 06-06 and then create the locked notification fact after the replacement succeeds. Discard must leave the game in `wishlist` or `pausado`, record a 3-round cooldown fact, write discard history, close the invitation, create `roulette-result-discarded` notification facts server-side per D-31, and not refund persisted boost spend.
  </action>
  <acceptance_criteria>
    - `rg -n "lockRouletteResultAsPrincipalUseCase|discardRouletteResultUseCase|replacement-required|autoPause: false|roleta-principal|roulette_history_events" apps/web/src/modules/roulette apps/web/tests/roulette-application.test.ts` returns implementation and tests.
    - `rg -n "createOperationalPlayNotification|roulette-result-locked|roulette-result-discarded|insertNotificationItem|push" apps/web/src/modules/play apps/web/src/modules/roulette apps/web/tests/roulette-application.test.ts apps/web/tests/play-application.test.ts` proves Central notification facts are server-side and preference-gated.
    - `git diff -- apps/web/src/modules/play/domain/play-policy.ts` shows no Phase 6 changes; `play-policy.ts` is a read-only replacement-policy reference.
    - `git diff -- apps/web/src/modules/play/application/ports.ts` shows no Phase 6 changes; the notification collaborator uses existing Play persistence methods or local helper types.
    - `rg -n "refund" apps/web/src/modules/roulette/application/discard-roulette-result.ts apps/web/src/modules/roulette/infrastructure/roulette-repository.ts` does not show persisted-discard refund behavior.
    - `rg -n "from \"\\.\\./\\.\\./play/(domain|infrastructure)|from \"\\.\\./\\.\\./library/(domain|infrastructure)" apps/web/src/modules/roulette` returns no deep cross-module imports.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @queue/web test -- roulette-application play-application</automated>
    <automated>pnpm --filter @queue/web typecheck</automated>
    <automated>pnpm check:architecture</automated>
  </verify>
  <done>Roulette invitations can be locked, blocked for replacement or discarded with exactly-once history and no automatic pause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> resolve use cases | Round id and replacement id are untrusted and reauthorized. |
| roulette -> play/library | Cross-domain handoff uses public contracts only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | lock use case | mitigate | Re-read pending round/result under membership and ignore client result/status fields. |
| T-06-07-02 | Elevation of Privilege | replacement branch | mitigate | Validate selected replacement through Play public contract. |
| T-06-07-03 | Repudiation | lock/discard history | mitigate | Append actor, outcome, round id and timestamps to history facts. |
| T-06-07-04 | Denial of Service | repeated resolves | mitigate | Idempotent status transitions prevent duplicate handoff/history. |
| T-06-07-05 | Repudiation | Central notifications | mitigate | Lock/discard create unique server-side `roulette-result-*` notification facts with actor/outcome metadata through Play's public contract. |
</threat_model>

<verification>
Run roulette and Play application tests, typecheck and architecture checks.
</verification>

<success_criteria>
- Lock, replacement-required and discard behaviors satisfy D-26 through D-30.
- Central da Dupla notification behavior satisfies D-31 in server-owned lock/discard code with push gated by existing preferences.
- New rounds remain blocked until the pending invitation is resolved.
- Cross-module calls preserve public boundary rules.
</success_criteria>

<output>
After completion, create `.planning/phases/06-roleta-e-economia/06-07-SUMMARY.md`.
</output>
