---
phase: 05-gamificacao-coletiva
plan: 11
type: execute
wave: 10
depends_on:
  - "05-08"
  - "05-09"
files_modified:
  - apps/web/src/modules/gamification/application/ports.ts
  - apps/web/src/modules/gamification/application/apply-gamification-fact.ts
  - apps/web/src/modules/gamification/application/run-streak-jobs.ts
  - apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
  - apps/web/tests/gamification-rewards.test.ts
  - packages/db/tests/gamification-concurrency.test.ts
autonomous: true
gap_closure: true
requirements:
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-09
  - GAME-10
  - GAME-11
  - GAME-12
must_haves:
  truths:
    - "Dois fatos concorrentes distintos somam todo o XP e persistem o nivel correspondente ao XP final."
    - "Dois fatos concorrentes distintos avancam a quest duas vezes, enquanto retry do mesmo source avanca uma vez."
    - "Cruzar um nivel ou concluir uma quest sob concorrencia concede o efeito derivado exatamente uma vez."
  artifacts:
    - path: "apps/web/src/modules/gamification/infrastructure/gamification-repository.ts"
      provides: "Lock da projection e incremento atomico/idempotente de quest"
      contains: "FOR UPDATE"
    - path: "apps/web/src/modules/gamification/application/apply-gamification-fact.ts"
      provides: "Derivacao serializada de XP, level e quest"
      contains: "lockProjection"
    - path: "packages/db/tests/gamification-concurrency.test.ts"
      provides: "Regressoes concorrentes com banco real"
      contains: "Promise.all"
  key_links:
    - from: "apps/web/src/modules/gamification/application/apply-gamification-fact.ts"
      to: "app.gamification_projection"
      via: "lock por dupla antes de leituras derivadas"
      pattern: "lockProjection"
    - from: "apps/web/src/modules/gamification/infrastructure/gamification-repository.ts"
      to: "app.quest_progress"
      via: "incremento SQL condicionado por source key"
      pattern: "advanceQuestProgress"
---

<objective>
Serializar os efeitos derivados por dupla e tornar o progresso de quest aditivo/idempotente sob concorrencia.

Purpose: Fechar a condicao de corrida que pode perder nivel ou incremento de quest mesmo quando o ledger de XP permanece atomico.
Output: Lock de projection, API atomica de quest e testes concorrentes com banco real.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
@.planning/phases/05-gamificacao-coletiva/05-RESEARCH.md
@.planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
@.planning/phases/05-gamificacao-coletiva/05-08-SUMMARY.md
@.planning/phases/05-gamificacao-coletiva/05-09-SUMMARY.md

Per D-29, D-37 e D-38, ledger e projection devem ser autoritativos e idempotentes. `05-VALIDATION.md` permanece ausente; os testes concorrentes abaixo sao a regressao concreta exigida pelo gate, nao cobertura Nyquist integral.

<interfaces>
Estado atual identificado pela verificacao:
- XP usa incremento SQL atomico, mas `level` vem de uma projection lida antes das escritas.
- Quest usa `GREATEST(existing, excluded)` com valor absoluto calculado na aplicacao.
- A idempotency key/source key ja identifica o fato confirmado e deve ser reutilizada.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bloquear projection por dupla antes de derivar XP, nivel e freezes</name>
  <files>apps/web/src/modules/gamification/application/ports.ts, apps/web/src/modules/gamification/application/apply-gamification-fact.ts, apps/web/src/modules/gamification/application/run-streak-jobs.ts, apps/web/src/modules/gamification/infrastructure/gamification-repository.ts, apps/web/tests/gamification-rewards.test.ts</files>
  <read_first>
    apps/web/src/modules/gamification/application/ports.ts
    apps/web/src/modules/gamification/application/apply-gamification-fact.ts
    apps/web/src/modules/gamification/application/run-streak-jobs.ts
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
    apps/web/tests/gamification-rewards.test.ts
    .planning/ARCHITECTURE.md
    .planning/SECURITY.md
  </read_first>
  <behavior>
    - "Transacao de fato obtem lock exclusivo da projection da dupla antes de ler estado derivado."
    - "Segundo fato espera e calcula nivel a partir do XP ja atualizado pelo primeiro."
    - "Freeze de marco de nivel e concedido uma vez."
    - "Fluxo de streak usa a mesma ordem de lock para evitar deadlock."
  </behavior>
  <action>
Adicione ao port transacional `lockProjection(duoId)` e implemente com `SELECT ... FOR UPDATE` sobre a linha da projection, criando-a de forma idempotente antes do lock quando ausente. Em `apply-gamification-fact.ts`, adquira esse lock imediatamente apos validar membership e antes de ler projection, quests, streak ou achievements que dependam dela.

Altere a atualizacao da projection para calcular e retornar o estado autoritativo dentro da transacao a partir do XP bloqueado e do delta efetivamente inserido no ledger. Derive `levelUp`, novo nivel e freezes do par locked-before/returned-after; nao envie um `level` absoluto calculado fora do repositorio.

Padronize `run-streak-jobs.ts` na mesma ordem de locks por dupla. Mantenha transacoes curtas e uma unica dupla por transacao para limitar contencao e evitar deadlock.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-rewards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Projection e bloqueada antes de qualquer derivacao dependente.
    - Nivel final e calculado do XP final persistido.
    - Level-up/freeze nao e duplicado em retries.
    - Fato e streak seguem a mesma ordem de lock.
  </acceptance_criteria>
  <done>A projection deixa de depender de snapshots stale da aplicacao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Avancar quest por source key com incremento SQL atomico</name>
  <files>apps/web/src/modules/gamification/application/ports.ts, apps/web/src/modules/gamification/application/apply-gamification-fact.ts, apps/web/src/modules/gamification/infrastructure/gamification-repository.ts, apps/web/tests/gamification-rewards.test.ts</files>
  <read_first>
    apps/web/src/modules/gamification/application/ports.ts
    apps/web/src/modules/gamification/application/apply-gamification-fact.ts
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
    apps/web/tests/gamification-rewards.test.ts
    apps/web/src/modules/gamification/domain/quest-catalog.ts
  </read_first>
  <behavior>
    - "Source A e source B avancam a mesma quest de N para N+2."
    - "Retry de source A deixa a quest em N+1."
    - "Incremento nunca ultrapassa goal."
    - "Quest que cruza goal concede XP e reward uma vez."
  </behavior>
  <action>
Substitua o upsert absoluto baseado em `GREATEST` por `advanceQuestProgress({ duoId, questId, sourceKey, increment, goal })`. No SQL, acrescente `sourceKey` somente se ainda nao estiver presente e incremente `current_value` pelo delta no mesmo statement, limitado por `goal`. Retorne `advanced`, `completedNow` e o registro persistido.

Em `apply-gamification-fact.ts`, use a idempotency/source key do fato confirmado. So conceda reward/XP da quest quando `completedNow` for verdadeiro; vincule o reward ID ao progresso na mesma transacao e torne a vinculacao idempotente. Nao use o valor pre-lido para escrever valor absoluto.

Atualize testes de unidade para fontes distintas, retry da mesma fonte, cap no goal e resumo de recompensa emitido uma vez.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-rewards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Incrementos de fontes distintas sao aditivos.
    - A mesma source key e aplicada no maximo uma vez.
    - `current_value` nunca excede `goal`.
    - Conclusao e premio acontecem uma vez e na mesma transacao.
  </acceptance_criteria>
  <done>Quest progress deixa de perder incrementos concorrentes e preserva idempotencia por fato.</done>
</task>

<task type="auto">
  <name>Task 3: Executar regressoes concorrentes de nivel e quest em banco real</name>
  <files>packages/db/tests/gamification-concurrency.test.ts, apps/web/tests/gamification-rewards.test.ts</files>
  <read_first>
    packages/db/tests/gamification-concurrency.test.ts
    apps/web/tests/gamification-rewards.test.ts
    apps/web/src/modules/gamification/application/apply-gamification-fact.ts
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
    apps/web/src/modules/gamification/domain/gamification-rules.ts
  </read_first>
  <action>
Adicione testes de integracao com duas conexoes/transacoes reais executadas por `Promise.all`.

Cenario de nivel: prepare XP imediatamente abaixo de um limiar, aplique dois fatos distintos que em conjunto o cruzam e confirme ledger com dois lancamentos, XP exatamente igual a soma, `level = getLevelForXp(finalXp)` e um unico evento/reward de level-up.

Cenario de quest: prepare progresso a dois pontos do goal, aplique duas source keys distintas simultaneamente e confirme `current_value` acrescido em dois, quest completa e um unico premio. Repita uma das source keys e confirme nenhuma alteracao adicional. Use timeouts explicitos para detectar deadlock e limpe fixtures por dupla.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-rewards.test.ts &amp;&amp; pnpm --filter @queue/db test:integration -- gamification-concurrency</automated>
  </verify>
  <acceptance_criteria>
    - O teste concorrente de nivel preserva os dois deltas e um unico level-up.
    - O teste concorrente de quest preserva os dois incrementos e um unico premio.
    - Retry da mesma source nao altera XP ou quest.
    - Nenhum teste excede o timeout por deadlock.
  </acceptance_criteria>
  <done>As duas condicoes de corrida documentadas em `05-VERIFICATION.md` falham antes da correcao e passam com a implementacao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent facts -> duo projection | Duas transacoes validas disputam o mesmo estado derivado. |
| Source key -> quest progress | Identidade do fato controla idempotencia do incremento. |
| Quest completion -> reward ledger | Uma transicao concorrente pode tentar conceder premio duas vezes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | T | gamification projection | mitigate | Lock por linha da dupla antes de ler e atualizar estado derivado. |
| T-05-11-02 | T | quest progress | mitigate | Incremento condicionado por source key em um unico statement SQL. |
| T-05-11-03 | R | reward issuance | mitigate | `completedNow` e reward ID persistidos na mesma transacao. |
| T-05-11-04 | D | transaction locking | mitigate | Ordem unica de lock, uma dupla por transacao, operacoes curtas e teste com timeout. |
</threat_model>

<verification>
Execute testes de recompensa e os dois cenarios concorrentes com banco real. Nao aceite mocks como prova do gap e nao declare a fase Nyquist-validada enquanto `05-VALIDATION.md` estiver ausente.
</verification>

<success_criteria>
- XP, nivel e freezes permanecem consistentes sob dois fatos simultaneos.
- Quest progress soma fontes distintas e ignora retry da mesma fonte.
- Level-up e conclusao de quest premiam exatamente uma vez.
- Testes concorrentes reais passam sem deadlock.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gamificacao-coletiva/05-11-SUMMARY.md`
</output>
