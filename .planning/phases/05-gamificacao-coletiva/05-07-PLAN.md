---
phase: 05-gamificacao-coletiva
plan: 07
type: execute
wave: 7
depends_on:
  - "05-05"
  - "05-06"
files_modified:
  - packages/db/src/migrations/0012_gamification_job_producer.sql
  - packages/db/src/migrations/meta/_journal.json
  - packages/db/src/rls/policies.sql
  - packages/db/tests/gamification-migrations.test.ts
  - packages/db/tests/gamification-rls.test.ts
  - apps/web/src/modules/gamification/application/ports.ts
  - apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
  - apps/web/src/modules/gamification/jobs.ts
  - apps/web/tests/gamification-jobs.test.ts
  - scripts/phase-5-gate.mjs
  - .planning/phases/05-gamificacao-coletiva/05-USER-SETUP.md
autonomous: true
gap_closure: true
requirements:
  - PLAY-05
  - GAME-09
  - GAME-10
  - GAME-11
  - GAME-12
  - GAME-13
  - GAME-16
  - SAFE-03
user_setup:
  - service: neon
    why: "O produtor e os consumidores de jobs precisam de uma credencial worker separada da credencial web sujeita a RLS por usuario."
    env_vars:
      - name: WORKER_DATABASE_URL
        source: "Neon Console -> connection string pooled do role queue2_worker"
      - name: DIRECT_DATABASE_URL
        source: "Neon Console -> connection string direta usada apenas para migrations"
must_haves:
  truths:
    - "Cada dupla pronta recebe exatamente um job inicial semanal, mensal, sazonal e de verificacao de streak."
    - "Duplas incompletas ou ainda sem nome nao recebem jobs de gamificacao."
    - "O produtor pode enumerar alvos com uma credencial worker, enquanto a credencial web continua incapaz de ler outra dupla."
  artifacts:
    - path: "packages/db/src/migrations/0012_gamification_job_producer.sql"
      provides: "Politicas RLS e privilegios minimos para o produtor de jobs"
      contains: "queue2_worker"
    - path: "apps/web/src/modules/gamification/infrastructure/gamification-repository.ts"
      provides: "Enumeracao de duplas prontas e insercao idempotente dos jobs iniciais"
      contains: "ensureGamificationJobs"
    - path: "apps/web/tests/gamification-jobs.test.ts"
      provides: "Regressao automatizada do bootstrap idempotente"
      contains: "gamification-quest-rotation"
  key_links:
    - from: "apps/web/src/modules/gamification/jobs.ts"
      to: "apps/web/src/modules/gamification/infrastructure/gamification-repository.ts"
      via: "ensureGamificationJobs antes de claim"
      pattern: "ensureGamificationJobs"
    - from: "apps/web/src/modules/gamification/infrastructure/gamification-repository.ts"
      to: "ops.scheduled_jobs"
      via: "insert idempotente por dupla, tipo e janela"
      pattern: "scheduled_jobs"
---

<objective>
Criar a fundacao segura e idempotente que produz os jobs iniciais de quests e streaks para todas as duplas prontas.

Purpose: Fechar a ausencia de produtores encontrada em `05-VERIFICATION.md` sem ampliar a leitura da credencial web nem depender de insercao manual.
Output: Migration de acesso worker, contratos de producao, implementacao do bootstrap e testes de RLS/idempotencia.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
@.planning/phases/05-gamificacao-coletiva/05-RESEARCH.md
@.planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
@.planning/phases/05-gamificacao-coletiva/05-USER-SETUP.md
@.planning/phases/05-gamificacao-coletiva/05-05-SUMMARY.md
@.planning/phases/05-gamificacao-coletiva/05-06-SUMMARY.md

`05-VALIDATION.md` nao existe por decisao explicita do gate. Os testes deste plano sao evidencia de regressao focada para o gap, nao substituem nem alegam a cobertura Nyquist ausente.

<interfaces>
O repositorio existente ja separa transacoes de usuario, claim, complete, fail e rebuild. Preserve essas APIs e acrescente contratos de produtor; nao mova regras de negocio para `jobs.ts`.

Tipos de jobs existentes:
- `gamification-quest-rotation`
- `gamification-streak-check`

O estado de dupla pronta deve ser derivado de fatos autoritativos: `paired_at` preenchido, nome nao vazio e exatamente dois membros.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conceder leitura minima ao worker e provar isolamento da credencial web</name>
  <files>packages/db/src/migrations/0012_gamification_job_producer.sql, packages/db/src/migrations/meta/_journal.json, packages/db/src/rls/policies.sql, packages/db/tests/gamification-migrations.test.ts, packages/db/tests/gamification-rls.test.ts</files>
  <read_first>
    packages/db/src/migrations/meta/_journal.json
    packages/db/src/rls/policies.sql
    packages/db/tests/gamification-migrations.test.ts
    packages/db/tests/gamification-rls.test.ts
    .planning/SECURITY.md
    .planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
  </read_first>
  <action>
Crie a migration `0012_gamification_job_producer.sql` e registre-a no journal. Adicione policies `SELECT` exclusivas para `queue2_worker` em `app.duos` e `app.duo_members`, com `USING (true)`, mantendo `FORCE ROW LEVEL SECURITY`. Nao conceda essas policies a `queue2_app_runtime`, nao altere as policies de membro e nao conceda escrita nessas tabelas ao worker. Espelhe a definicao em `packages/db/src/rls/policies.sql`.

Amplie os testes para executar como os dois roles: o runtime autenticado continua sem enxergar uma dupla alheia; o worker consegue enumerar somente as colunas necessarias para determinar `duo_id`, `paired_at`, nome, quantidade de membros e um `created_by_user_id`. Verifique tambem que o worker nao consegue inserir, atualizar ou excluir duplas/membros. Use comentarios SQL que vinculem a migration ao gap de produtor de jobs.
  </action>
  <verify>
    <automated>pnpm --filter @queue/db test:integration -- gamification-migrations gamification-rls</automated>
  </verify>
  <acceptance_criteria>
    - A migration e o espelho RLS definem acesso global somente para `queue2_worker`.
    - `queue2_app_runtime` permanece isolado por dupla em leitura.
    - O worker pode ler os dados minimos de prontidao e nao pode escrever em `app.duos` ou `app.duo_members`.
    - Os testes de migration e RLS passam contra um banco de integracao.
  </acceptance_criteria>
  <done>O produtor possui uma fronteira de leitura worker verificavel sem enfraquecer o isolamento da aplicacao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar contratos e bootstrap idempotente dos quatro jobs</name>
  <files>apps/web/src/modules/gamification/application/ports.ts, apps/web/src/modules/gamification/infrastructure/gamification-repository.ts, apps/web/tests/gamification-jobs.test.ts</files>
  <read_first>
    apps/web/src/modules/gamification/application/ports.ts
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
    apps/web/tests/gamification-jobs.test.ts
    packages/db/src/migrations/0012_gamification_job_producer.sql
    .planning/ARCHITECTURE.md
    .planning/SECURITY.md
    .planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
  </read_first>
  <behavior>
    - "Uma dupla pronta sem jobs pendentes recebe weekly, monthly, seasonal e streak."
    - "Executar o bootstrap novamente nao duplica jobs pendentes, claimed ou failed da mesma especie."
    - "Dupla sem pareamento, sem nome ou sem exatamente dois membros recebe zero jobs."
    - "Operacoes normais de usuario continuam usando a pool runtime e nao exigem WORKER_DATABASE_URL."
  </behavior>
  <action>
Em `ports.ts`, adicione `GamificationJobScheduleKind = "weekly" | "monthly" | "seasonal" | "streak"`, o alvo pronto com `duoId`, `timezone` e `createdByUserId`, e os metodos `ensureGamificationJobs(now)` e `enqueueGamificationJob(input)` no contrato de jobs.

Em `gamification-repository.ts`, aceite pools injetaveis para testes, mantenha a pool runtime em todos os metodos de usuario e crie a pool worker de forma lazy a partir de `WORKER_DATABASE_URL` apenas em metodos de job. Consulte duplas com `paired_at IS NOT NULL`, nome nao vazio e exatamente dois membros; escolha deterministicamente o membro de menor `member_slot` como `createdByUserId`.

Para cada alvo sem job equivalente ainda ativo, insira quatro jobs: tres `gamification-quest-rotation`, cada um com um unico `questType`, e um `gamification-streak-check`. Use chave `gamification-bootstrap:<duoId>:<kind>:<YYYY-MM-DD>`, `ON CONFLICT DO NOTHING` e uma verificacao `NOT EXISTS` para estados `pending`, `claimed` ou `failed` da mesma especie. O retorno deve informar quantos jobs foram produzidos. Nao coloque calculo de quests/streaks no repositorio.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os quatro tipos de bootstrap sao criados para cada dupla pronta.
    - Reexecucao e concorrencia nao criam duplicatas logicas.
    - Duplas incompletas sao ignoradas.
    - A pool worker nao e criada em fluxos normais da aplicacao.
  </acceptance_criteria>
  <done>O repositorio oferece um produtor inicial idempotente, separado da credencial runtime e coberto por testes focados.</done>
</task>

<task type="auto">
  <name>Task 3: Executar o produtor antes dos consumidores e bloquear release sem migration aplicada</name>
  <files>apps/web/src/modules/gamification/jobs.ts, apps/web/tests/gamification-jobs.test.ts, scripts/phase-5-gate.mjs, .planning/phases/05-gamificacao-coletiva/05-USER-SETUP.md</files>
  <read_first>
    apps/web/src/modules/gamification/jobs.ts
    apps/web/tests/gamification-jobs.test.ts
    scripts/phase-5-gate.mjs
    .planning/phases/05-gamificacao-coletiva/05-USER-SETUP.md
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
    packages/db/src/migrations/0012_gamification_job_producer.sql
  </read_first>
  <action>
No runner de manutencao, chame `ensureGamificationJobs(now)` antes de qualquer claim e inclua `producedJobs` no resultado observavel. Preserve a ordem consumidor de quest seguido do consumidor de streak e a autenticacao existente do cron.

Atualize o gate da fase para executar os testes focados deste plano e falhar quando `WORKER_DATABASE_URL` ou `DIRECT_DATABASE_URL` nao estiverem disponiveis para as verificacoes de banco; nao converta banco ausente em sucesso. Documente em `05-USER-SETUP.md` a criacao do role least-privileged, a configuracao da URL pooled do worker e a aplicacao da migration.

Aplique a migration com o script Drizzle existente e rode o verificador de drift. Se a credencial estiver ausente ou a migration falhar, interrompa o plano como bloqueado; nao marque o gap como fechado.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-jobs.test.ts &amp;&amp; pnpm --filter @queue/db test:integration -- gamification-migrations gamification-rls &amp;&amp; pnpm --filter @queue/db drizzle:migrate &amp;&amp; node ./node_modules/@gsd-build/sdk/dist/cli.js query verify.schema-drift 05</automated>
  </verify>
  <acceptance_criteria>
    - O runner produz antes de consumir e informa a quantidade produzida.
    - O gate falha de forma explicita sem as duas credenciais de banco.
    - A migration e aplicada e o verificador nao encontra drift.
    - `05-USER-SETUP.md` contem os passos exatos de configuracao do worker.
  </acceptance_criteria>
  <done>O bootstrap passa pelo runner real e a mudanca de banco e um gate bloqueante e verificavel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron -> worker repository | Uma chamada autenticada inicia leitura global e insercao na fila operacional. |
| Worker role -> duo data | A credencial operacional atravessa RLS de membro para localizar todas as duplas prontas. |
| Producer -> scheduled_jobs | Reexecucoes e concorrencia podem tentar criar o mesmo trabalho. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | E | `queue2_worker` policies | mitigate | Conceder somente SELECT nas duas tabelas de alvo e provar ausencia de escrita em teste RLS. |
| T-05-07-02 | I | `WORKER_DATABASE_URL` | mitigate | Manter a URL exclusivamente server-side e criar a pool somente nos metodos de job. |
| T-05-07-03 | T | `ops.scheduled_jobs` | mitigate | Chaves deterministicas, `NOT EXISTS` por especie e `ON CONFLICT DO NOTHING`. |
| T-05-07-04 | D | maintenance runner | mitigate | Uma linha por dupla/especie, consulta limitada a duplas prontas e operacao idempotente. |
</threat_model>

<verification>
Execute os testes web e DB focados, aplique a migration e confirme drift zero. Registre que a ausencia de `05-VALIDATION.md` continua sendo risco conhecido; estes checks provam somente o produtor, o isolamento e a aplicacao da migration.
</verification>

<success_criteria>
- Uma dupla pronta sem estado previo recebe quatro jobs iniciais via runner real.
- Reexecucao nao duplica jobs.
- O runtime web continua isolado por dupla e o worker permanece read-only nos dados de alvo.
- Migration aplicada e testes focados verdes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gamificacao-coletiva/05-07-SUMMARY.md`
</output>
