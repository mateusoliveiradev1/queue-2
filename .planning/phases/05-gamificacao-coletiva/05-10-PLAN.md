---
phase: 05-gamificacao-coletiva
plan: 10
type: execute
wave: 7
depends_on:
  - "05-03"
  - "05-06"
files_modified:
  - apps/web/src/app/app/phase-5-reward-token.ts
  - apps/web/src/app/app/phase-5-status.ts
  - apps/web/src/app/app/phase-4-actions.ts
  - apps/web/src/app/app/page.tsx
  - apps/web/src/app/app/jogo/[slug]/page.tsx
  - apps/web/tests/gamification-reward-toast.test.tsx
  - apps/web/tests/gamification-security.test.ts
  - apps/web/tests/phase-5-e2e.spec.ts
autonomous: true
gap_closure: true
requirements:
  - PLAY-05
  - GAME-05
must_haves:
  truths:
    - "Uma mutacao Play que gera XP, level-up, quest ou achievement mostra feedback nao bloqueante derivado do resumo autoritativo."
    - "Editar `recompensa` na URL nao cria um toast falso."
    - "O feedback pertence ao usuario e a dupla atuais e expira rapidamente."
  artifacts:
    - path: "apps/web/src/app/app/phase-5-reward-token.ts"
      provides: "Envelope HMAC curto para transportar somente a view de feedback"
      contains: "timingSafeEqual"
    - path: "apps/web/src/app/app/phase-4-actions.ts"
      provides: "Propagacao do reward summary da mutacao ate o redirect"
      contains: "reward"
    - path: "apps/web/tests/phase-5-e2e.spec.ts"
      provides: "Fluxo real que gera e exibe recompensa"
      contains: "RewardToast"
  key_links:
    - from: "apps/web/src/app/app/phase-4-actions.ts"
      to: "apps/web/src/app/app/phase-5-reward-token.ts"
      via: "assina a view derivada do resultado autoritativo antes do redirect"
      pattern: "createRewardToken"
    - from: "apps/web/src/app/app/jogo/[slug]/page.tsx"
      to: "apps/web/src/modules/gamification/presentation/reward-toast"
      via: "verifica token apos auth e renderiza RewardToast"
      pattern: "RewardToast"
---

<objective>
Transportar o resumo autoritativo de recompensa das mutacoes Play ate o toast, eliminando o status forjavel por query string.

Purpose: Fechar o gap de feedback descartado sem transformar o toast em fonte de verdade ou bloquear a jornada.
Output: Token HMAC de curta duracao, actions que preservam reward, paginas verificadoras e testes de seguranca/E2E.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
@.planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
@.planning/phases/05-gamificacao-coletiva/05-03-SUMMARY.md
@.planning/phases/05-gamificacao-coletiva/05-06-SUMMARY.md

Implementar per D-03, D-08, D-26 e D-28 a D-31: feedback especial, nao bloqueante, baseado no servidor e na mesma recompensa persistida. Os testes sao regressao focada; `05-VALIDATION.md` continua ausente.

<interfaces>
From gamification presentation:
- `toRewardToastView(summary)` ja prioriza level-up, achievement, quest concluida e XP.
- `RewardToast` ja oferece toast/estado inline e reduced motion.

From Play actions:
- Os helpers atuais recebem `result.reward`, mas os exports publicos retornam `Promise&lt;void&gt;` e descartam esse valor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assinar e verificar feedback curto vinculado a usuario e dupla</name>
  <files>apps/web/src/app/app/phase-5-reward-token.ts, apps/web/tests/gamification-security.test.ts</files>
  <read_first>
    apps/web/src/app/app/phase-5-status.ts
    apps/web/tests/gamification-security.test.ts
    apps/web/src/modules/gamification/presentation/reward-toast.ts
    apps/web/src/modules/gamification/application/ports.ts
    .planning/SECURITY.md
    .planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
  </read_first>
  <behavior>
    - "Token valido retorna a RewardToastViewModel original por no maximo cinco minutos."
    - "Payload adulterado, assinatura adulterada ou token expirado retorna null."
    - "Token emitido para outro usuario ou outra dupla retorna null."
    - "O token nao contem identificadores crus, saldo de XP autoritativo nem segredo."
  </behavior>
  <action>
Crie um modulo `server-only` que receba `GamificationRewardSummary`, derive `RewardToastViewModel` com `toRewardToastView`, e produza um envelope versionado, base64url, assinado com HMAC-SHA-256 usando `BETTER_AUTH_SECRET`. Inclua `iat`, expiracao maxima de cinco minutos e um subject opaco derivado por HMAC de `userId + duoId`; nao serialize IDs crus.

Na verificacao, recalcule o subject para a sessao/dupla atuais, valide versao e tempo e compare assinaturas com `timingSafeEqual` somente apos checar comprimentos. Retorne `null` para qualquer entrada malformada. O payload serve apenas para feedback visual: nao o use para conceder XP, nivel, quest ou achievement.

Escreva testes para tamper de payload/assinatura, expiracao, subject errado e formato invalido. Injete relogio/segredo nos testes sem expor o segredo em client bundles.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-security.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O modulo e server-only e usa HMAC-SHA-256 e `timingSafeEqual`.
    - Token expira em no maximo cinco minutos e e vinculado a usuario+dupla.
    - Identificadores crus e valores autoritativos nao aparecem no envelope.
    - Todos os casos de adulteracao retornam ausencia de feedback.
  </acceptance_criteria>
  <done>O redirect pode carregar uma view de recompensa autenticada sem criar nova autoridade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preservar reward nas actions e renderizar somente token verificado</name>
  <files>apps/web/src/app/app/phase-5-status.ts, apps/web/src/app/app/phase-4-actions.ts, apps/web/src/app/app/page.tsx, apps/web/src/app/app/jogo/[slug]/page.tsx, apps/web/tests/gamification-reward-toast.test.tsx</files>
  <read_first>
    apps/web/src/app/app/phase-4-actions.ts
    apps/web/src/app/app/page.tsx
    apps/web/src/app/app/jogo/[slug]/page.tsx
    apps/web/src/app/app/phase-5-status.ts
    apps/web/tests/gamification-reward-toast.test.tsx
    apps/web/src/app/app/phase-5-reward-token.ts
    apps/web/src/modules/gamification/presentation/reward-toast.ts
  </read_first>
  <behavior>
    - "Confirmacao Play com reward visivel redireciona com token assinado."
    - "Reward vazio preserva o redirect normal sem parametro."
    - "Dashboard e detalhe do jogo verificam auth/duo antes de mostrar o toast."
    - "`?recompensa=level-up` sem token valido nao mostra nada."
  </behavior>
  <action>
Amplie o resultado interno das mutacoes Play para preservar `result.reward` nos fluxos de sessao, capitulo, agendado e terminal. Depois da persistencia bem-sucedida, quando `toRewardToastView` produzir feedback, assine-o e redirecione para `/app/jogo/<gameSlug-validado>?estado=<estado>&recompensa=<token>`. Use somente `gameSlug` validado ou fallback `/app`; nao aceite uma URL arbitraria do formulario.

No dashboard e no detalhe do jogo, autentique sessao e dupla primeiro, verifique o token para esse subject e renderize `RewardToast` apenas em caso valido. Remova o mapa estatico de `phase-5-status.ts` do caminho de producao ou reduza o arquivo a funcoes que nao aceitam labels livres. Um valor legado como `level-up` deve ser ignorado.

Mantenha o toast nao bloqueante, a ordem de prioridade existente e reduced motion, per D-03. Nao leia o token em componente cliente para validacao.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-reward-toast.test.tsx gamification-security.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Todos os quatro tipos de mutacao Play preservam o reward autoritativo.
    - Apenas token valido para a sessao/dupla atual gera toast.
    - Query string legada ou adulterada nao gera feedback.
    - Redirects permanecem restritos a rotas internas derivadas de slug validado.
  </acceptance_criteria>
  <done>O feedback persistido chega ao usuario sem confiar em estado forjavel do navegador.</done>
</task>

<task type="auto">
  <name>Task 3: Cobrir spoofing e recompensa real no fluxo E2E</name>
  <files>apps/web/tests/gamification-reward-toast.test.tsx, apps/web/tests/gamification-security.test.ts, apps/web/tests/phase-5-e2e.spec.ts</files>
  <read_first>
    apps/web/tests/gamification-reward-toast.test.tsx
    apps/web/tests/gamification-security.test.ts
    apps/web/tests/phase-5-e2e.spec.ts
    apps/web/src/app/app/phase-4-actions.ts
    apps/web/src/app/app/jogo/[slug]/page.tsx
  </read_first>
  <action>
Substitua o E2E que navega manualmente para `?recompensa=level-up` por dois cenarios. Primeiro, abra essa URL forjada e prove que nenhum toast aparece. Segundo, execute uma mutacao terminal real que gere recompensa, aguarde o redirect e confirme a copy do `RewardToast` correspondente ao resumo persistido.

Adicione cobertura de reduced motion e confirme que o toast nao bloqueia navegacao ou acoes da pagina. Nos testes server/component, cubra reward vazio, token expirado e token pertencente a outro subject.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-reward-toast.test.tsx gamification-security.test.ts &amp;&amp; pnpm --filter @queue/web test:e2e -- phase-5-e2e.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - URL forjada nao mostra toast.
    - Fluxo terminal real mostra a recompensa produzida pelo servidor.
    - Reduced motion permanece respeitado.
    - O toast nao bloqueia a pagina.
  </acceptance_criteria>
  <done>O gap de feedback possui prova negativa de spoofing e prova positiva end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server action -> browser redirect | Uma view de recompensa atravessa uma URL controlavel pelo usuario. |
| Query string -> server page | Entrada nao confiavel tenta provocar feedback visual. |
| Session/duo -> reward subject | O mesmo token nao pode ser reutilizado por outra conta ou dupla. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | T | reward query parameter | mitigate | HMAC-SHA-256, verificacao temporal e comparacao constante. |
| T-05-10-02 | S | reward subject | mitigate | Vincular token a HMAC de usuario+dupla autenticados. |
| T-05-10-03 | I | reward payload | mitigate | Serializar apenas view de feedback, sem IDs crus ou estado autoritativo. |
| T-05-10-04 | E | redirect target | mitigate | Construir destino apenas de slug validado e fallback interno conhecido. |
</threat_model>

<verification>
Execute testes unitarios/componentes de token/toast e o E2E real. Estes checks fecham o transporte e spoofing do feedback; nao suprem o arquivo `05-VALIDATION.md` ausente.
</verification>

<success_criteria>
- Recompensa real de Play gera toast nao bloqueante.
- Query forjada, token adulterado/expirado ou subject errado nao gera toast.
- Nenhum dado do token concede ou altera gamificacao.
- Testes unitarios, de seguranca e E2E passam.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gamificacao-coletiva/05-10-SUMMARY.md`
</output>
