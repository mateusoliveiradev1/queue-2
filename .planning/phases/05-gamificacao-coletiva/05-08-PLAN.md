---
phase: 05-gamificacao-coletiva
plan: 08
type: execute
wave: 8
depends_on:
  - "05-07"
files_modified:
  - apps/web/src/modules/gamification/domain/gamification-schedule.ts
  - apps/web/src/modules/gamification/domain/quest-catalog.ts
  - apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts
  - apps/web/src/modules/gamification/application/run-streak-jobs.ts
  - apps/web/tests/gamification-domain.test.ts
  - apps/web/tests/gamification-jobs.test.ts
  - packages/db/tests/gamification-concurrency.test.ts
  - scripts/phase-5-gate.mjs
autonomous: true
gap_closure: true
requirements:
  - PLAY-05
  - GAME-09
  - GAME-10
  - GAME-11
  - GAME-12
  - GAME-13
  - GAME-16
  - SAFE-03
must_haves:
  truths:
    - "Janelas semanal, mensal e sazonal sao persistidas a partir da meia-noite local da dupla, nao da meia-noite UTC."
    - "A verificacao de streak e reagendada para as 04:00 locais da dupla."
    - "Um job produzido percorre claim, processamento, conclusao e cria exatamente seu proximo job."
  artifacts:
    - path: "apps/web/src/modules/gamification/domain/gamification-schedule.ts"
      provides: "Conversao timezone-aware e calculo das proximas execucoes"
      exports:
        - getNextQuestRotationAt
        - getNextStreakCheckAt
    - path: "apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts"
      provides: "Processamento e reagendamento de rotacoes"
      contains: "enqueueGamificationJob"
    - path: "packages/db/tests/gamification-concurrency.test.ts"
      provides: "Integracao producer-to-completion com banco real"
      contains: "scheduled_jobs"
  key_links:
    - from: "apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts"
      to: "apps/web/src/modules/gamification/domain/gamification-schedule.ts"
      via: "calculo da janela local e proxima execucao"
      pattern: "getNextQuestRotationAt"
    - from: "apps/web/src/modules/gamification/application/run-streak-jobs.ts"
      to: "ops.scheduled_jobs"
      via: "enqueue seguinte antes de completar o atual"
      pattern: "enqueueGamificationJob"
---

<objective>
Corrigir as fronteiras de timezone e completar o ciclo recorrente dos jobs de quests e streaks.

Purpose: Fechar o gap em que consumidores existem, mas usam meia-noite UTC e nao garantem recorrencia producer-to-completion.
Output: Dominio de agenda local, runners recorrentes e testes unitarios/DB do caminho completo.
</objective>

<execution_context>
@C:/Users/Liiiraa/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
@.planning/phases/05-gamificacao-coletiva/05-RESEARCH.md
@.planning/phases/05-gamificacao-coletiva/05-VERIFICATION.md
@.planning/phases/05-gamificacao-coletiva/05-07-SUMMARY.md

`05-VALIDATION.md` permanece ausente. Os testes deste plano cobrem exclusivamente timezone, recorrencia e producer-to-completion; nao representam validacao Nyquist integral da fase.

<interfaces>
Contratos produzidos por 05-07:
- `ensureGamificationJobs(now)`
- `enqueueGamificationJob(input)`
- `GamificationJobScheduleKind`

Preserve os estados existentes de `ops.scheduled_jobs` e a ordem claim -> trabalho -> proximo enqueue -> complete.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir agenda local deterministica para quests e streaks</name>
  <files>apps/web/src/modules/gamification/domain/gamification-schedule.ts, apps/web/src/modules/gamification/domain/quest-catalog.ts, apps/web/tests/gamification-domain.test.ts</files>
  <read_first>
    apps/web/src/modules/gamification/domain/quest-catalog.ts
    apps/web/tests/gamification-domain.test.ts
    apps/web/src/modules/play/domain/play-rules.ts
    .planning/phases/05-gamificacao-coletiva/05-CONTEXT.md
    .planning/phases/05-gamificacao-coletiva/05-RESEARCH.md
  </read_first>
  <behavior>
    - "Weekly abre na segunda-feira 00:00 da timezone da dupla."
    - "Monthly abre no primeiro dia do mes 00:00 local."
    - "Seasonal usa as fronteiras explicitas do catalogo convertidas da timezone da dupla."
    - "Streak check ocorre na proxima ocorrencia de 04:00 local."
    - "America/New_York permanece correta antes e depois da transicao DST."
  </behavior>
  <action>
Crie `gamification-schedule.ts` com funcoes puras para converter uma data/hora civil em um instante UTC usando `Intl.DateTimeFormat` e ajuste de offset em duas passagens, seguindo o padrao timezone-aware existente no dominio Play. Exporte `getNextQuestRotationAt({ after, timezone, questType })` e `getNextStreakCheckAt({ after, timezone })`.

Weekly deve retornar a proxima segunda 00:00 local; monthly, o proximo primeiro dia 00:00 local; seasonal, a proxima fronteira explicitamente definida pelo catalogo de temporadas, sem inventar temporadas implicitas. Streak deve retornar a proxima ocorrencia estritamente posterior de 04:00 local, per D-16. Valide timezones IANA e falhe de modo controlado para valor invalido.

Escreva primeiro testes para `America/Sao_Paulo` e `America/New_York`, incluindo a transicao DST de 2026. As assercoes devem confirmar tanto o instante UTC quanto a data/hora civil reconstruida; proiba expectativas baseadas em `T00:00:00.000Z`.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-domain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Todas as quatro agendas retornam instantes UTC correspondentes ao horario local requerido.
    - Casos de DST nao deslocam a fronteira civil.
    - Temporadas continuam explicitas, per D-12.
    - Timezone invalida produz erro testado, sem fallback silencioso para UTC.
  </acceptance_criteria>
  <done>O dominio possui uma unica fonte testada para fronteiras locais e proximas execucoes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reagendar cada job concluido de forma idempotente</name>
  <files>apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts, apps/web/src/modules/gamification/application/run-streak-jobs.ts, apps/web/tests/gamification-jobs.test.ts</files>
  <read_first>
    apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts
    apps/web/src/modules/gamification/application/run-streak-jobs.ts
    apps/web/tests/gamification-jobs.test.ts
    apps/web/src/modules/gamification/domain/gamification-schedule.ts
    apps/web/src/modules/gamification/application/ports.ts
    .planning/ARCHITECTURE.md
  </read_first>
  <behavior>
    - "Job weekly concluido cria exatamente o proximo weekly."
    - "Monthly, seasonal e streak seguem a mesma regra."
    - "Retry apos enqueue e antes de complete nao duplica o proximo job."
    - "Payload invalido ou dupla divergente falha o job sem criar sucessor."
  </behavior>
  <action>
Remova `duoWindowDate` e qualquer concatenacao de data local com `Z`. Em cada runner, use `gamification-schedule.ts` para calcular a janela corrente e `runAt` seguinte a partir da timezone autoritativa da dupla.

Depois de processar com sucesso a quest ou streak e antes de marcar o job atual como concluido, chame `enqueueGamificationJob` com uma chave deterministica que inclua `duoId`, especie e ISO de `nextRunAt`. O payload de quest deve conter exatamente um `questType`; o de streak deve conter `checkAt` e o `createdByUserId` validado. O insert idempotente precisa tornar seguro o retry entre enqueue e complete.

Jobs com payload invalido, usuario que nao pertence a dupla ou especie divergente devem ir para o caminho de falha existente e nunca produzir sucessor. Preserve idempotency keys e eventos de dominio das operacoes autoritativas.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cada sucesso cria um unico sucessor da mesma especie.
    - Retry nao duplica o sucessor.
    - Falhas de validacao nao criam sucessor.
    - Nenhum runner converte data local por sufixo UTC.
  </acceptance_criteria>
  <done>Os consumidores mantem uma cadeia recorrente idempotente e timezone-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Provar producer, claim, processamento, complete e recorrencia em banco real</name>
  <files>packages/db/tests/gamification-concurrency.test.ts, apps/web/tests/gamification-jobs.test.ts, scripts/phase-5-gate.mjs</files>
  <read_first>
    packages/db/tests/gamification-concurrency.test.ts
    apps/web/tests/gamification-jobs.test.ts
    scripts/phase-5-gate.mjs
    apps/web/src/modules/gamification/jobs.ts
    apps/web/src/modules/gamification/application/run-quest-rotation-jobs.ts
    apps/web/src/modules/gamification/application/run-streak-jobs.ts
    apps/web/src/modules/gamification/infrastructure/gamification-repository.ts
  </read_first>
  <action>
Adicione um teste de integracao que crie duplas prontas em `America/Sao_Paulo` e `America/New_York`, execute o produtor real, reivindique os jobs com o repositorio worker, processe uma rotacao e uma verificacao de streak, complete os jobs e consulte `ops.scheduled_jobs`.

O teste deve provar: quatro jobs iniciais por dupla; job atual em `completed`; um unico sucessor `pending`; `run_at` do sucessor reconstruido na timezone resulta em 00:00 para quests ou 04:00 para streak; reexecucao nao duplica. Use as pools/roles reais de integracao e nao mocks para este caminho.

Inclua esse teste e os testes unitarios de agenda no `phase-5-gate.mjs`. Mantenha falha explicita quando o banco de integracao nao estiver configurado.
  </action>
  <verify>
    <automated>pnpm --filter @queue/web test -- gamification-domain.test.ts gamification-jobs.test.ts &amp;&amp; pnpm --filter @queue/db test:integration -- gamification-concurrency</automated>
  </verify>
  <acceptance_criteria>
    - O teste percorre produtor, claim, trabalho, complete e successor usando banco real.
    - Sao Paulo e New York preservam os horarios civis esperados.
    - O gate executa os testes e nao aceita skip silencioso.
    - A recorrencia permanece idempotente em uma segunda execucao.
  </acceptance_criteria>
  <done>O caminho completo que estava ausente em `05-VERIFICATION.md` possui prova automatizada com o banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job payload -> application runner | Dados persistidos na fila dirigem mutacoes autoritativas. |
| Civil time -> UTC instant | Timezone e DST alteram o instante executado. |
| Current job -> successor | Retry atravessa duas escritas operacionais relacionadas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | T | job payload validation | mitigate | Validar duo, usuario e especie antes de qualquer trabalho ou enqueue. |
| T-05-08-02 | R | schedule conversion | mitigate | Funcoes puras com testes de horario civil, UTC e DST. |
| T-05-08-03 | T | successor enqueue | mitigate | Chave por duo/especie/instante e insert idempotente antes de complete. |
| T-05-08-04 | D | recurring chain | mitigate | Um sucessor por job bem-sucedido e nenhuma expansao para payload invalido. |
</threat_model>

<verification>
Execute os testes unitarios de timezone/jobs e a integracao real de fila. A frequencia externa do cron continua sujeita ao setup documentado; nao declare esse aspecto operacional validado sem a configuracao real.
</verification>

<success_criteria>
- Janelas de quest respeitam meia-noite local e streak respeita 04:00 local.
- Jobs bem-sucedidos criam um unico sucessor.
- O caminho producer-to-completion passa em banco real para duas timezones.
- Os testes focados sao adicionados ao gate sem alegar o `05-VALIDATION.md` ausente.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gamificacao-coletiva/05-08-SUMMARY.md`
</output>
