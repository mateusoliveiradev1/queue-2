---
phase: 03-descoberta-e-matches
plan: "04"
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - ".env.example"
  - "apps/web/package.json"
  - "pnpm-lock.yaml"
  - "apps/web/src/modules/discovery/application/register-push-subscription.ts"
  - "apps/web/src/modules/discovery/application/send-match-notification.ts"
  - "apps/web/src/modules/discovery/infrastructure/push-service.ts"
  - "apps/web/src/modules/discovery/presentation/live-panel.tsx"
  - "apps/web/src/app/app/descobrir/actions.ts"
  - "apps/web/src/app/api/discovery/live/[sessionId]/route.ts"
  - "apps/web/src/app/api/discovery/push/route.ts"
  - "apps/web/tests/discovery-push.test.ts"
  - "apps/web/tests/discovery-ui.test.tsx"
  - "apps/web/tests/phase-3-e2e.spec.ts"
  - "apps/web/tests/accessibility.spec.ts"
  - "packages/db/tests/discovery-rls.test.ts"
  - "packages/db/tests/discovery-concurrency.test.ts"
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08, DISC-09, DISC-10, DISC-11, DISC-12]
user_setup:
  - service: web-push
    why: "Match Live push notifications require VAPID keys and must remain opt-in."
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: "Generated VAPID public key"
      - name: VAPID_PRIVATE_KEY
        source: "Generated VAPID private key"
      - name: VAPID_SUBJECT
        source: "mailto or HTTPS contact subject for VAPID"
must_haves:
  truths:
    - "DISC-02, D-04 and D-32: Match Live supports shared in-app match updates and opted-in push notification when a match occurs."
    - "D-06 and D-07: Final verification covers animation quality, reduced motion, keyboard and touch accessibility."
    - "D-12 and D-13: Browser flows verify match celebration appears before add/move actions."
    - "D-27, D-28 and D-29: E2E verifies discovered/matched games can move to valid library statuses without duplicates and keep the user in Discovery."
    - "D-30 and D-31: Match history remains focused and does not grow into Hall/reviews/story timeline."
    - "D-32: Push permission is requested only after explicit opt-in, never on initial page load."
    - "D-33 and D-34: E2E covers surprise and autocomplete as Discovery entry points."
    - "D-35, D-36 and D-37: Verification asserts Phase 3 does not implement roulette or add raw seen/skipped/cooled-down games to any roulette pool."
    - "Security: Push endpoints validate session, redact subscription secrets in logs and never expose VAPID private key to the browser."
  artifacts:
    - path: "apps/web/src/modules/discovery/infrastructure/push-service.ts"
      provides: "Server-only web-push adapter"
      contains: "VAPID_PRIVATE_KEY"
    - path: "apps/web/src/app/api/discovery/live/[sessionId]/route.ts"
      provides: "Bounded live polling endpoint"
      contains: "zod"
    - path: "apps/web/tests/phase-3-e2e.spec.ts"
      provides: "Browser coverage for discovery ritual"
      min_lines: 120
    - path: "apps/web/tests/discovery-push.test.ts"
      provides: "Push opt-in, secret boundary and notification behavior tests"
      min_lines: 80
  key_links:
    - from: "apps/web/src/modules/discovery/application/send-match-notification.ts"
      to: "apps/web/src/modules/discovery/infrastructure/push-service.ts"
      via: "server-only push port"
      pattern: "sendMatchNotification"
    - from: "apps/web/src/app/api/discovery/live/[sessionId]/route.ts"
      to: "apps/web/src/modules/discovery/index.ts"
      via: "public live read use case"
      pattern: "getLiveSession"
    - from: "apps/web/tests/phase-3-e2e.spec.ts"
      to: "apps/web/src/app/app/descobrir/page.tsx"
      via: "end-to-end discovery flow"
      pattern: "Quero jogar"
---

<objective>
Harden Match Live, opted-in push and Phase 3 verification.

Purpose: Phase 3 cannot be considered planned/executed until live match updates, optional push, cross-duo isolation, concurrency, accessibility and browser flows are covered end to end.
Output: web-push integration, live polling endpoint, push subscription actions, complete tests and final verification of Discovery requirements.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/03-descoberta-e-matches/03-CONTEXT.md
@.planning/phases/03-descoberta-e-matches/03-RESEARCH.md
@.planning/phases/03-descoberta-e-matches/03-01-SUMMARY.md
@.planning/phases/03-descoberta-e-matches/03-02-SUMMARY.md
@.planning/phases/03-descoberta-e-matches/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opted-in push subscription and notification path</name>
  <files>.env.example, apps/web/package.json, pnpm-lock.yaml, apps/web/src/modules/discovery/application/register-push-subscription.ts, apps/web/src/modules/discovery/application/send-match-notification.ts, apps/web/src/modules/discovery/infrastructure/push-service.ts, apps/web/src/app/api/discovery/push/route.ts, apps/web/src/app/app/descobrir/actions.ts, apps/web/tests/discovery-push.test.ts</files>
  <read_first>.planning/SECURITY.md, apps/web/src/platform/auth/session.ts, apps/web/src/platform/security/audit.ts, scripts/check-secrets.mjs</read_first>
  <action>Add `web-push` to `@queue/web` dependencies and document VAPID envs in `.env.example`. Implement a server-only push adapter that reads private VAPID config only on the server. Add subscription register/disable behavior behind verified session and duo membership. Add match notification sending after a match is created and only for enabled subscriptions. Redact endpoint/auth material from logs/errors. Expose only the VAPID public key to client code.</action>
  <verify>Run `pnpm install`, `pnpm --filter @queue/web test -- discovery-push`, `pnpm check:secrets`, `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Browser never receives `VAPID_PRIVATE_KEY`.
    - Push permission is requested only from an explicit opt-in UI action.
    - Disabled subscriptions do not receive match notifications.
    - Missing VAPID config fails as a server configuration state, not a client secret leak.
  </acceptance_criteria>
  <done>Match Live can notify opted-in users without weakening the secret boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add live polling endpoint and UI refresh behavior</name>
  <files>apps/web/src/app/api/discovery/live/[sessionId]/route.ts, apps/web/src/modules/discovery/application/get-live-session.ts, apps/web/src/modules/discovery/presentation/live-panel.tsx, apps/web/src/modules/discovery/presentation/match-celebration.tsx, apps/web/tests/discovery-ui.test.tsx</files>
  <read_first>apps/web/src/app/api/discovery/search/route.ts, apps/web/src/modules/discovery/application/get-live-session.ts, apps/web/src/modules/discovery/presentation/live-panel.tsx</read_first>
  <action>Add a bounded same-origin live polling endpoint or server-action refresh path. Validate `sessionId`, authorize the current session, return only the current duo's live state and cache-control headers appropriate for short polling. Update the Live panel to refresh while active, stop on expiration/end, and announce new match state accessibly. Keep server/database state authoritative.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-ui`, `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Live route rejects invalid session IDs and sessions outside the user's duo.
    - Polling stops when the session expires or closes.
    - New match state triggers in-app celebration even if push is disabled.
    - Response does not cache private live state across users.
  </acceptance_criteria>
  <done>Match Live updates reliably without a persistent websocket dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Add browser, accessibility and integration coverage</name>
  <files>apps/web/tests/phase-3-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, apps/web/tests/discovery-ui.test.tsx, packages/db/tests/discovery-rls.test.ts, packages/db/tests/discovery-concurrency.test.ts</files>
  <read_first>apps/web/tests/phase-2-e2e.spec.ts, apps/web/tests/accessibility.spec.ts, packages/db/tests/library-rls.test.ts, packages/db/tests/library-concurrency.test.ts</read_first>
  <action>Add Phase 3 browser coverage for deck load, `Quero jogar`, `Agora nao`, `Pular`, reciprocal match, Match Live, surprise, mood quiz, autocomplete, filters, match history and valid library handoff. Extend accessibility coverage for `/app/descobrir`, focus order, source links, keyboard controls and reduced-motion behavior. Ensure database tests cover cross-duo isolation and match concurrency under runtime role. Keep environment-dependent E2E fixtures explicit and honest if missing.</action>
  <verify>Run `pnpm --filter @queue/web test`, `pnpm --filter @queue/web typecheck`, `pnpm check:architecture`, `pnpm check:secrets`, `pnpm --filter @queue/db test:integration -- discovery` when `TEST_DATABASE_URL` is configured, and `pnpm --filter @queue/web test:e2e -- tests/phase-3-e2e.spec.ts` against a local dev server when fixtures are available.</verify>
  <acceptance_criteria>
    - Tests fail if match requires anything other than both members choosing `Quero jogar`.
    - Tests fail if `Pular` affects negative recommendation history.
    - Tests fail if current-duo Discovery state leaks to another duo.
    - Tests fail if valid library handoff duplicates rows or enables Zerado/Dropado.
    - Accessibility coverage includes reduced-motion and keyboard alternatives.
  </acceptance_criteria>
  <done>Phase 3 behavior is covered across domain, database, UI and browser layers.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 3 verification and document residual skips</name>
  <files>.planning/phases/03-descoberta-e-matches/03-04-SUMMARY.md</files>
  <read_first>.planning/SECURITY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Run the complete feasible verification set and document any environment-dependent skips clearly in the summary. Confirm no Phase 4 sessions/progress, Phase 6 roulette draw, or Phase 7 Hall/reviews leaked into Phase 3 implementation. Confirm RAWG/source/freshness metadata remains visible in Discovery cards/details and that no new secret-bearing module is reachable from Client Components.</action>
  <verify>Run `pnpm verify` when local env allows it; otherwise run the available subset and record explicit skips for integration/E2E fixtures.</verify>
  <acceptance_criteria>
    - Summary maps every DISC-01 through DISC-12 requirement to evidence.
    - Summary records any skipped external tests as release follow-ups, not passes.
    - Architecture and secret checks pass.
    - Known critical or high security findings are absent or block completion.
  </acceptance_criteria>
  <done>Phase 3 is ready for verify-work/UAT with evidence.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm install` runs after dependency changes and updates `pnpm-lock.yaml`.
- [ ] `pnpm --filter @queue/web test -- discovery-push` passes.
- [ ] `pnpm --filter @queue/web test` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] `pnpm check:secrets` passes.
- [ ] Discovery integration tests run against `TEST_DATABASE_URL` or report explicit skip output.
- [ ] Phase 3 Playwright specs pass against local dev server or explicit missing-fixture skips are documented.
- [ ] Responsive/reduced-motion browser review confirms deck, live, match celebration, filters and history do not overlap and remain operable.
</verification>

<success_criteria>
- Match Live updates both members in-app and can notify opted-in users.
- Push secrets remain server-only and permission is contextual.
- Phase 3 browser flows cover every DISC requirement.
- RLS, concurrency, architecture and secret gates pass.
- Discovery remains distinct from future Play, Roulette and Hall phases.
</success_criteria>

<output>
After completion, create `.planning/phases/03-descoberta-e-matches/03-04-SUMMARY.md`.
</output>
