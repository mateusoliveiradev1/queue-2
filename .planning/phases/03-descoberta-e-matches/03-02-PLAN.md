---
phase: 03-descoberta-e-matches
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - "apps/web/src/modules/discovery/application/get-discovery-deck.ts"
  - "apps/web/src/modules/discovery/application/record-discovery-decision.ts"
  - "apps/web/src/modules/discovery/application/get-match-history.ts"
  - "apps/web/src/modules/discovery/application/start-live-session.ts"
  - "apps/web/src/modules/discovery/application/get-live-session.ts"
  - "apps/web/src/modules/discovery/application/answer-mood-quiz.ts"
  - "apps/web/src/modules/discovery/application/get-surprise-recommendation.ts"
  - "apps/web/src/modules/discovery/application/search-discovery-games.ts"
  - "apps/web/src/modules/discovery/infrastructure/discovery-repository.ts"
  - "apps/web/src/modules/discovery/presentation/view-models.ts"
  - "apps/web/src/modules/discovery/index.ts"
  - "apps/web/src/app/app/descobrir/actions.ts"
  - "apps/web/src/app/api/discovery/search/route.ts"
  - "apps/web/tests/discovery-application.test.ts"
  - "apps/web/tests/discovery-search.test.ts"
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-06, DISC-07, DISC-08, DISC-09, DISC-10, DISC-11, DISC-12]
user_setup: []
must_haves:
  truths:
    - "DISC-01, D-04, D-08 and D-09: Server-authoritative decision handling records independent member swipes and returns immediate match data when both approve."
    - "DISC-02 and D-32: Match Live state is shared and can surface match celebrations immediately in-app; push delivery remains opt-in and is completed in the hardening plan."
    - "DISC-03 and D-33: Surprise uses unseen, compatible deck eligibility and respects common platform by default."
    - "DISC-04, D-14, D-15, D-16 and D-17: Mood quiz services store member answers, expose preview for one answer and full duo recommendations only after both members answer."
    - "DISC-05 and D-34: Autocomplete/search is a fast entry into Discovery and opens discovery cards/actions, not a duplicate generic catalog."
    - "DISC-06, DISC-07, DISC-08, DISC-09, D-18, D-19 and D-20: Deck/search services accept quick and advanced filters with bounded inputs."
    - "DISC-10, DISC-11, D-22, D-23, D-24, D-25 and D-26: Deck reads rank with tag/practical compatibility, compact reasons, separate negative signal intensity and controlled variety."
    - "DISC-12, D-27, D-28 and D-29: A discovered or matched game can be added/moved only through valid library public APIs and never duplicated if already in the duo library."
    - "D-10, D-11 and D-12: Application results distinguish cooldown, skip/no-op, match celebration and library handoff states."
    - "Architecture: Discovery composes catalog/library public entrypoints and does not deep-import their internals."
  artifacts:
    - path: "apps/web/src/modules/discovery/infrastructure/discovery-repository.ts"
      provides: "Server-only discovery persistence and transactional match creation"
      contains: "withAppUserTransaction"
    - path: "apps/web/src/modules/discovery/application/record-discovery-decision.ts"
      provides: "Decision mutation and match result use case"
      contains: "match"
    - path: "apps/web/src/app/api/discovery/search/route.ts"
      provides: "Bounded autocomplete/search route"
      contains: "zod"
    - path: "apps/web/tests/discovery-application.test.ts"
      provides: "Use-case coverage for deck, decision, match, quiz, surprise and library handoff"
      min_lines: 120
  key_links:
    - from: "apps/web/src/modules/discovery/application/get-discovery-deck.ts"
      to: "apps/web/src/modules/catalog/index.ts"
      via: "catalog public search/detail contract"
      pattern: "searchCatalogGames"
    - from: "apps/web/src/modules/discovery/application/record-discovery-decision.ts"
      to: "apps/web/src/modules/library/index.ts"
      via: "library public add/move contract for explicit handoff only"
      pattern: "addGameToWishlist|moveLibraryGame"
    - from: "apps/web/src/app/app/descobrir/actions.ts"
      to: "apps/web/src/modules/discovery/index.ts"
      via: "thin server action composition"
      pattern: "recordDiscoveryDecision"
---

<objective>
Build the discovery application services and server boundary.

Purpose: Phase 3 needs a stable server contract for deck reads, decisions, matches, live state, quiz, surprise, autocomplete and library handoff before client UI can safely compose the ritual.
Output: discovery repository, use cases, view models, server actions, bounded autocomplete route and focused application tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/phases/03-descoberta-e-matches/03-CONTEXT.md
@.planning/phases/03-descoberta-e-matches/03-RESEARCH.md
@.planning/phases/03-descoberta-e-matches/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement discovery repository and deck read models</name>
  <files>apps/web/src/modules/discovery/infrastructure/discovery-repository.ts, apps/web/src/modules/discovery/application/get-discovery-deck.ts, apps/web/src/modules/discovery/application/search-discovery-games.ts, apps/web/src/modules/discovery/presentation/view-models.ts, apps/web/src/modules/discovery/index.ts, apps/web/tests/discovery-application.test.ts</files>
  <read_first>apps/web/src/modules/catalog/index.ts, apps/web/src/modules/library/index.ts, apps/web/src/modules/library/infrastructure/library-repository.ts, apps/web/src/modules/discovery/application/ports.ts</read_first>
  <action>Implement server-only discovery persistence with `withAppUserTransaction`. Build deck/search read models by composing catalog public facts, library public status data and discovery decision state. Exclude current-member evaluated games by default, include current library status when present, include source/freshness metadata from catalog view models, and return compact recommendation reasons from the domain policy. Do not accept client-supplied duo identity as authoritative.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-application`, `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Default deck excludes games the current member already evaluated.
    - Search/autocomplete can intentionally include already seen games where requested.
    - Deck cards include library status and valid action state without duplicating library rows.
    - Cross-module reads use public exports or plain IDs, not deep imports.
  </acceptance_criteria>
  <done>Server reads can supply the Discovery UI with authoritative card state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement decision, match and library handoff use cases</name>
  <files>apps/web/src/modules/discovery/application/record-discovery-decision.ts, apps/web/src/modules/discovery/application/get-match-history.ts, apps/web/src/modules/discovery/infrastructure/discovery-repository.ts, apps/web/src/modules/discovery/presentation/view-models.ts, apps/web/src/app/app/descobrir/actions.ts, apps/web/tests/discovery-application.test.ts</files>
  <read_first>apps/web/src/app/app/phase-2-actions.ts, apps/web/src/modules/library/index.ts, packages/db/src/schema/ops.ts</read_first>
  <action>Add a verified-session server action layer under the Discovery route. Implement decision recording in one transaction: upsert member decision, check reciprocal `want`, create exactly one match if needed, snapshot recommendation reasons and record a duo domain event. Return typed action states for `card-advanced`, `cooldown-set`, `match-created`, `already-matched`, `library-updated` and error cases. Add explicit library handoff actions that call library public APIs for Wishlist, Jogando and Pausado only.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-application` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Match creation is idempotent under repeated or concurrent approvals.
    - Match result does not add to library until the user chooses a valid library action.
    - Existing library row is moved instead of duplicated.
    - Server actions derive `userId` from `requireVerifiedSession()` and use safe return paths.
  </acceptance_criteria>
  <done>Discovery mutations are authoritative and hand off to library safely.</done>
</task>

<task type="auto">
  <name>Task 3: Implement live, quiz, surprise and autocomplete services</name>
  <files>apps/web/src/modules/discovery/application/start-live-session.ts, apps/web/src/modules/discovery/application/get-live-session.ts, apps/web/src/modules/discovery/application/answer-mood-quiz.ts, apps/web/src/modules/discovery/application/get-surprise-recommendation.ts, apps/web/src/modules/discovery/application/search-discovery-games.ts, apps/web/src/app/app/descobrir/actions.ts, apps/web/src/app/api/discovery/search/route.ts, apps/web/tests/discovery-application.test.ts, apps/web/tests/discovery-search.test.ts</files>
  <read_first>.planning/SECURITY.md, apps/web/src/platform/rate-limit/persistent.ts, apps/web/src/modules/catalog/application/search-catalog.ts</read_first>
  <action>Add use cases for short Match Live sessions, live polling payload, mood quiz answers/results, surprise recommendation and autocomplete. Validate Route Handler input with zod, bound query length and limit, and reuse or mirror persistent rate-limit patterns if the route can be hit repeatedly. Live sessions should expire, return shared match state for both members, and remain server-authoritative. Surprise should only select unseen compatible cards. Quiz should distinguish preview from full duo result.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-search`, `pnpm --filter @queue/web test -- discovery-application`, `pnpm --filter @queue/web typecheck` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - Autocomplete rejects too-short/too-long queries and caps result count.
    - Live payload never leaks another duo's session or matches.
    - Surprise excludes games seen by either member when the product rule requires "neither member has seen".
    - Mood quiz full result requires both members; otherwise returns preview.
  </acceptance_criteria>
  <done>All non-visual Discovery modes have server contracts ready for UI.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- discovery-application` passes.
- [ ] `pnpm --filter @queue/web test -- discovery-search` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] Server actions and Route Handlers validate input and authorize session membership.
</verification>

<success_criteria>
- The server can provide deck, search, surprise, quiz, live and match-history state.
- Decision recording creates matches exactly once and never auto-adds to library.
- Library handoff uses existing valid status rules.
- Search/autocomplete is bounded and secret-free.
- Discovery application code respects module public boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-descoberta-e-matches/03-02-SUMMARY.md`.
</output>
