---
phase: 03-descoberta-e-matches
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "packages/db/src/schema/app.ts"
  - "packages/db/src/migrations/0008_discovery_core.sql"
  - "packages/db/src/rls/policies.sql"
  - "packages/db/src/roles.sql"
  - "packages/db/tests/discovery-rls.test.ts"
  - "packages/db/tests/discovery-concurrency.test.ts"
  - "apps/web/src/modules/discovery/domain/discovery-policy.ts"
  - "apps/web/src/modules/discovery/domain/recommendation-policy.ts"
  - "apps/web/src/modules/discovery/domain/mood-quiz.ts"
  - "apps/web/src/modules/discovery/application/ports.ts"
  - "apps/web/src/modules/discovery/index.ts"
  - "apps/web/tests/discovery-domain.test.ts"
autonomous: true
requirements: [DISC-01, DISC-03, DISC-04, DISC-06, DISC-07, DISC-08, DISC-09, DISC-10, DISC-11]
user_setup: []
must_haves:
  truths:
    - "DISC-01, D-08 and D-09: Member decisions support `Quero jogar`, `Agora nao` and `Pular`; a match is created only when both duo members choose `Quero jogar` for the same game."
    - "D-01, D-05 and D-35: Discovery owns a central deck and default deck reads exclude games already evaluated by the current member."
    - "D-10 and D-25: `Agora nao` is a temporary cooldown signal with stronger negative weight than skip, not a permanent ban."
    - "D-11 and D-25: `Pular` is judgment-free, changes the current card and does not count as approval or rejection."
    - "D-12, D-13 and D-35: Match state is persisted for celebration and history, but does not automatically add the game to the library."
    - "DISC-03 and D-33: Surprise eligibility only chooses compatible unseen games."
    - "DISC-04, D-14, D-15, D-16 and D-17: Mood quiz rules use three questions for energy, commitment and vibe, combine both members when possible, and treat one-member answers as preview only."
    - "DISC-06, DISC-07, DISC-08, DISC-09, D-18, D-19, D-20 and D-21: Discovery filter rules include time, common platform, availability, coop type, mood, year, genre and QUEUE/2 editorial rarity."
    - "DISC-10, DISC-11, D-22, D-23, D-24 and D-26: Recommendation rules start with tag/practical compatibility reasons and gate collaborative influence behind explicit minimum interaction thresholds."
    - "D-36 and D-37: Discovery forms a consented pool for later roulette; seen, skipped or cooled-down games do not enter future roulette unless consciously added to the library."
    - "Security: Discovery tables are duo-scoped, forced-RLS protected and concurrency-tested for single-match creation."
  artifacts:
    - path: "packages/db/src/migrations/0008_discovery_core.sql"
      provides: "Duo-scoped discovery tables, constraints, indexes, RLS enablement and grants"
      contains: "app.discovery_member_decisions"
    - path: "apps/web/src/modules/discovery/domain/discovery-policy.ts"
      provides: "Pure decision, cooldown, skip and match rules"
      min_lines: 80
    - path: "apps/web/src/modules/discovery/domain/recommendation-policy.ts"
      provides: "Cold-start ranking, reason labels and collaborative threshold gate"
      min_lines: 80
    - path: "packages/db/tests/discovery-concurrency.test.ts"
      provides: "Concurrent reciprocal approval creates one match"
      min_lines: 60
  key_links:
    - from: "apps/web/src/modules/discovery/index.ts"
      to: "apps/web/src/modules/discovery/application"
      via: "public discovery module entrypoint"
      pattern: "getDiscoveryDeck|recordDiscoveryDecision"
    - from: "packages/db/src/rls/policies.sql"
      to: "app.discovery_member_decisions"
      via: "duo membership RLS policies"
      pattern: "app.has_duo_membership"
    - from: "apps/web/src/modules/discovery/domain/recommendation-policy.ts"
      to: "apps/web/src/modules/library/domain/match-score.ts"
      via: "compatible factor vocabulary, not deep imports unless same-module inputs are copied"
      pattern: "PC em comum|campanha 2p"
---

<objective>
Create the discovery persistence and pure domain foundation.

Purpose: Phase 3 needs authoritative duo-scoped discovery state before UI, live sessions or recommendations can safely create matches.
Output: discovery database schema, RLS policies, least-privilege grants, pure decision/match/mood/recommendation rules, public module shell and focused domain/database tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE.md
@.planning/SECURITY.md
@.planning/research/STACK.md
@.planning/phases/03-descoberta-e-matches/03-CONTEXT.md
@.planning/phases/03-descoberta-e-matches/03-RESEARCH.md
@.planning/phases/02.1-localizacao-e-qualidade-do-catalogo/02.1-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add discovery database schema, RLS and grants</name>
  <files>packages/db/src/schema/app.ts, packages/db/src/migrations/0008_discovery_core.sql, packages/db/src/rls/policies.sql, packages/db/src/roles.sql, packages/db/tests/discovery-rls.test.ts, packages/db/tests/discovery-concurrency.test.ts</files>
  <read_first>.planning/SECURITY.md, packages/db/src/schema/app.ts, packages/db/src/migrations/0005_library_duo_state.sql, packages/db/src/rls/policies.sql, packages/db/src/roles.sql</read_first>
  <action>Add `app.discovery_member_decisions`, `app.discovery_matches`, `app.discovery_live_sessions`, `app.discovery_mood_quiz_answers` and `app.discovery_push_subscriptions`. Include `duo_id`, membership FKs, catalog FKs, decision/status check constraints, cooldown fields, match reason snapshots, push subscription enable/disable fields, unique guards for one match per duo/game, live expiration indexes and match-history ordering indexes. Enable and force RLS on every duo-scoped table. Policies must let duo members read shared discovery state, let members write only their own decision/quiz/subscription rows, and let match/live rows mutate only through authorized duo membership. Update runtime/worker grants without granting DELETE unless a table explicitly needs disable/update semantics.</action>
  <verify>Run `pnpm --filter @queue/db typecheck` and `pnpm --filter @queue/db test:integration -- discovery` when `TEST_DATABASE_URL` is configured; otherwise preserve explicit skip output.</verify>
  <acceptance_criteria>
    - Cross-duo SELECT/INSERT/UPDATE attempts fail under runtime role and forced RLS.
    - One member cannot overwrite the other member's discovery decision, quiz answer or push subscription rows.
    - Concurrent reciprocal approvals cannot create duplicate rows in `app.discovery_matches`.
    - Hot reads have indexes for deck exclusion, match history, live session lookup and cooldown expiry.
  </acceptance_criteria>
  <done>Discovery state exists with database isolation and concurrency guards.</done>
</task>

<task type="auto">
  <name>Task 2: Implement discovery decision and match domain rules</name>
  <files>apps/web/src/modules/discovery/domain/discovery-policy.ts, apps/web/src/modules/discovery/application/ports.ts, apps/web/src/modules/discovery/index.ts, apps/web/tests/discovery-domain.test.ts</files>
  <read_first>.planning/phases/03-descoberta-e-matches/03-CONTEXT.md, apps/web/src/modules/library/domain/library-policy.ts, apps/web/src/modules/catalog/domain/catalog-policy.ts</read_first>
  <action>Create pure types and rules for discovery decisions (`want`, `not_now`, `skip`), cooldown duration, source modes (`deck`, `live`, `surprise`, `quiz`, `search`), match eligibility, duplicate-match handling, current-card exclusion and valid library handoff statuses. Keep rules independent from React, Next.js, Drizzle, database clients and external SDKs.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-domain` and `pnpm check:architecture`.</verify>
  <acceptance_criteria>
    - `want` can create a match only when the partner already has `want` for the same game.
    - `not_now` returns a cooldown window and negative recommendation signal.
    - `skip` returns no preference weight and no durable match impact.
    - Valid discovery library targets are Wishlist, Jogando and Pausado only.
  </acceptance_criteria>
  <done>Discovery decisions are testable as pure domain rules.</done>
</task>

<task type="auto">
  <name>Task 3: Implement mood, filter and recommendation policies</name>
  <files>apps/web/src/modules/discovery/domain/mood-quiz.ts, apps/web/src/modules/discovery/domain/recommendation-policy.ts, apps/web/src/modules/discovery/application/ports.ts, apps/web/tests/discovery-domain.test.ts</files>
  <read_first>.planning/phases/03-descoberta-e-matches/03-RESEARCH.md, apps/web/src/modules/library/domain/match-score.ts, apps/web/src/modules/library/domain/platforms.ts</read_first>
  <action>Add the three-question mood quiz contract, conservative duo mood merge, filter input validation and cold-start ranking policy. The ranking policy should consume plain catalog/library facts, apply main-flow/common-platform/time/availability/mood/genre/year/rarity filters, generate compact reason labels, reserve a controlled variety band, and expose a collaborative-filtering threshold result that remains disabled or very-low-weight until thresholds are met.</action>
  <verify>Run `pnpm --filter @queue/web test -- discovery-domain` and `pnpm --filter @queue/web typecheck`.</verify>
  <acceptance_criteria>
    - One-member mood answers produce preview state, not a full duo recommendation.
    - Conflicting mood answers prefer middle-ground/flexible games.
    - Common platform is enabled by default and can be disabled intentionally.
    - Collaborative influence is off below the documented minimum data threshold.
    - Recommendation reasons are human-readable labels, not percentages.
  </acceptance_criteria>
  <done>Discovery can rank and explain cards without persistence or UI coupling.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @queue/db typecheck` passes.
- [ ] `pnpm --filter @queue/web typecheck` passes.
- [ ] `pnpm --filter @queue/web test -- discovery-domain` passes.
- [ ] `pnpm check:architecture` passes.
- [ ] Discovery RLS and concurrency tests run against `TEST_DATABASE_URL` or report explicit skip output.
</verification>

<success_criteria>
- Discovery state is duo-scoped, RLS-protected and indexed.
- Decision, match, mood, filter and recommendation rules are pure and tested.
- Default recommendations are cold-start compatible and explainable.
- Collaborative filtering is gated until enough data exists.
- Discovery does not auto-add matches or feed future roulette without explicit library action.
</success_criteria>

<output>
After completion, create `.planning/phases/03-descoberta-e-matches/03-01-SUMMARY.md`.
</output>
