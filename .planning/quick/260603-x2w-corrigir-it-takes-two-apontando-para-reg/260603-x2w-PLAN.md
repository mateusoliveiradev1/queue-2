# Quick Task 260603-x2w: Corrigir It Takes Two apontando para registro RAWG itch

**Date:** 2026-06-04
**Status:** Completed

## Evidence

Producao continha `It Takes Two (itch)` com `rawg_id=179581`, `slug=it-takes-two` e imagem errada. A allowlist local tambem usava `rawgRef: "it-takes-two"`, que aponta para o registro errado da RAWG.

O registro oficial confirmado na RAWG e `rawg_id=455597`, `rawgRef: "it-takes-two-2"`, nome `It Takes Two` e URL `https://rawg.io/games/it-takes-two-2`.

## Plan

- Trocar o allowlist entry de It Takes Two para buscar `rawgRef: "it-takes-two-2"` e manter o slug interno `it-takes-two`.
- Fazer o sync aplicar o slug curado da allowlist em vez do slug bruto da RAWG.
- Validar `expectedName` durante o sync para falhar se a RAWG retornar outro jogo.
- Fazer o repositorio adotar um slug curado existente quando o `rawg_id` mudar, evitando conflito unico em ambientes com o dado antigo.
- Atualizar testes de sync para cobrir slug curado, validacao de nome, adocao de `rawg_id` e o entry real de It Takes Two.
- Corrigir o dado de producao com SQL transacional controlado e verificar a linha resultante.

## Verification

- `pnpm test tests/catalog-sync.test.ts`
- `pnpm --filter @queue/web typecheck`
- `pnpm --filter @queue/web check:architecture`
- `pnpm check:secrets`
- Consulta de producao confirmando `rawg_id=455597`, nome `It Takes Two`, imagem oficial, URL RAWG oficial, plataformas, generos, tempo estimado, localizacao e disponibilidade preservadas.
