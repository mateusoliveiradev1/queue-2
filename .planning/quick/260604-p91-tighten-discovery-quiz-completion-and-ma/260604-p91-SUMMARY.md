---
quick_id: 260604-p91
slug: tighten-discovery-quiz-completion-and-ma
status: complete
completed: 2026-06-04
commit: 9117076
---

# Quick Task 260604-p91 Summary

## Scope

Tightened the Phase 03.1 Discovery flow after visual/UX regression feedback:

- Quiz now disappears from the orbit controls and tray after the current member has answered the active mood round.
- Match Live now has one clear action path: the orbit control navigates to the Live panel, and the panel owns the start action only when no active live exists.
- Active Live sessions are fetched even without a `live=` URL param; starting Live reuses an existing active session instead of creating duplicate sessions.
- Match Recentes mobile now uses an unbounded page-flow layout, smaller cover column, stable two-column status controls, and scroll padding/margins so focused card actions stay above the fixed bottom nav.
- Push opt-in now waits for client mount before browser support detection, preventing server/client hydration drift.

## Impeccable Guidance Applied

Used the local `impeccable` product/polish guidance from `C:\Users\Liiiraa\Documents\estudos\teste\impeccable\skill\SKILL.md`, `reference/product.md`, `reference/polish.md`, and `STYLE.md`.

Applied principles:

- Remove invented or duplicate affordances.
- Make states explicit instead of decorative.
- Keep controls where the action actually happens.
- Polish flow and information architecture, not only surface styling.

## Verification

- `pnpm --filter @queue/web test -- discovery-ui discovery-search discovery-application` - passed, 27 tests.
- `pnpm --filter @queue/web typecheck` - passed.
- `pnpm check:architecture` - passed.
- `pnpm lint` - passed.
- Static mobile visual check at 390x844 with real `tokens.css` + `globals.css`: Match Recentes controls render as grid, no title/control overlap, and focused status controls do not overlap the bottom nav.

## Browser Evidence Note

Authenticated browser verification against `/app/descobrir` remains blocked by the existing missing ready-duo E2E fixtures. A fresh Playwright context redirects to `/login` because `E2E_READY_USER_EMAIL` and `E2E_READY_USER_PASSWORD` are not configured locally. This preserves the existing rule: no test-only auth bypass.
