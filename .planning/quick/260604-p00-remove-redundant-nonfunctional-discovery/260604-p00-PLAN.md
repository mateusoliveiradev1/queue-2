---
quick_id: 260604-p00
slug: remove-redundant-nonfunctional-discovery
status: complete
created: 2026-06-04
---

# Quick Task 260604-p00: Remove redundant nonfunctional Discovery decision badges

## Goal

Remove the fake top decision row from `/app/descobrir` so the Discovery deck has one visible set of decision controls: the real server-action buttons inside the active card. Also fix the Match History card footer links that overlap later cards in the support tray.

## Tasks

1. Remove `.discovery-reaction-badges` markup from `DiscoveryDeck`.
2. Remove related CSS so the UI no longer renders non-interactive labels that look like buttons.
3. Remove the per-match footer link row from Match History; the cover remains the detail link and the block header links to Biblioteca.
4. Update Discovery UI tests to assert the redundant badges and match footer link row do not return.
5. Run focused verification.

## Verification

- `pnpm --filter @queue/web test -- discovery-ui`
- `pnpm --filter @queue/web typecheck`
