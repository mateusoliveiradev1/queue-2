-- QUEUE/2 Drizzle schema baseline.
-- Owner module: database. The project uses hand-authored SQL migrations for
-- operational objects, RLS, policies, grants and comments. This no-op migration
-- lets Drizzle keep a current schema snapshot without recreating existing
-- objects during `drizzle-kit migrate`.

SELECT 1;
